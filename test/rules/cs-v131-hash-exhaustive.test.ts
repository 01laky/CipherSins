// Auto-generated by scripts/generate-v131-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/v131-scan-source.js";

describe("CS-V131 HASH exhaustive edge cases", () => {
	it("CS-V131-HASH-001 HASH-01 createHash md5", async () => {
		const result = await scanSource(
			"case-001.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-002 HASH-01 createHash md4", async () => {
		const result = await scanSource(
			"case-002.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-003 HASH-01 createHash sha1", async () => {
		const result = await scanSource(
			"case-003.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-004 HASH-01 createHash sha-1", async () => {
		const result = await scanSource(
			"case-004.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-005 HASH-01 createHash md2", async () => {
		const result = await scanSource(
			"case-005.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-006 HASH-01 createHash ripemd160", async () => {
		const result = await scanSource(
			"case-006.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-007 HASH-01 pbkdf2 md5", async () => {
		const result = await scanSource(
			"case-007.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-008 HASH-01 pbkdf2 sha1", async () => {
		const result = await scanSource(
			"case-008.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-009 HASH-01 sha512 password clean", async () => {
		const result = await scanSource(
			"case-009.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-010 HASH-01 apiKey context md5 clean", async () => {
		const result = await scanSource(
			"case-010.ts",
			'import { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-011 HASH-02 bcrypt cost 4", async () => {
		const result = await scanSource(
			"case-011.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-012 HASH-02 bcrypt cost 5", async () => {
		const result = await scanSource(
			"case-012.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-013 HASH-02 bcrypt cost 7", async () => {
		const result = await scanSource(
			"case-013.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-014 HASH-02 bcrypt cost 8", async () => {
		const result = await scanSource(
			"case-014.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-015 HASH-02 bcrypt cost 9", async () => {
		const result = await scanSource(
			"case-015.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-016 HASH-02 bcrypt cost 10 clean", async () => {
		const result = await scanSource(
			"case-016.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-017 HASH-02 bcrypt cost 11 clean", async () => {
		const result = await scanSource(
			"case-017.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-018 HASH-02 bcrypt cost 12 clean", async () => {
		const result = await scanSource(
			"case-018.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-019 HASH-02 bcrypt cost 14 clean", async () => {
		const result = await scanSource(
			"case-019.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-020 HASH-02 compare only clean", async () => {
		const result = await scanSource(
			"case-020.ts",
			'import bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-021 HASH-03 pbkdf2 iter 1000", async () => {
		const result = await scanSource(
			"case-021.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-022 HASH-03 pbkdf2 iter 50000", async () => {
		const result = await scanSource(
			"case-022.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-023 HASH-03 pbkdf2 iter 99999", async () => {
		const result = await scanSource(
			"case-023.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-024 HASH-03 pbkdf2 iter 100000 clean", async () => {
		const result = await scanSource(
			"case-024.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-025 HASH-03 pbkdf2 iter 200000 clean", async () => {
		const result = await scanSource(
			"case-025.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-026 HASH-04 scrypt cost=16383 block=8", async () => {
		const result = await scanSource(
			"case-026.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-027 HASH-04 scrypt cost=16384 block=8", async () => {
		const result = await scanSource(
			"case-027.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-028 HASH-04 scrypt cost=16384 block=7", async () => {
		const result = await scanSource(
			"case-028.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-029 HASH-04 scrypt cost=16384 block=8", async () => {
		const result = await scanSource(
			"case-029.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-030 HASH-04 scrypt cost=8192 block=8", async () => {
		const result = await scanSource(
			"case-030.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-031 HASH-04 scrypt default no options clean", async () => {
		const result = await scanSource(
			"case-031.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-032 HASH-05 argon2 time=2 mem=65536", async () => {
		const result = await scanSource(
			"case-032.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-033 HASH-05 argon2 time=3 mem=65536", async () => {
		const result = await scanSource(
			"case-033.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-034 HASH-05 argon2 time=3 mem=32768", async () => {
		const result = await scanSource(
			"case-034.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-035 HASH-05 argon2 time=1 mem=131072", async () => {
		const result = await scanSource(
			"case-035.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-036 HASH-01 require crypto md5", async () => {
		const result = await scanSource(
			"case-036.ts",
			'const { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-037 HASH-02 node-rs bcrypt cost 8", async () => {
		const result = await scanSource(
			"case-037.ts",
			'import bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-038 HASH-01+03 pbkdf2 md5 low iter", async () => {
		const result = await scanSource(
			"case-038.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-039 HASH-02 namespace bcrypt cost 6", async () => {
		const result = await scanSource(
			"case-039.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-040 HASH-02 namespace bcrypt cost 8", async () => {
		const result = await scanSource(
			"case-040.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-041 HASH-02 namespace bcrypt cost 10", async () => {
		const result = await scanSource(
			"case-041.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-042 HASH-02 namespace bcrypt cost 12", async () => {
		const result = await scanSource(
			"case-042.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-043 bcrypt hashSync cost 4", async () => {
		const result = await scanSource(
			"case-043.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-044 bcrypt genSaltSync cost 4", async () => {
		const result = await scanSource(
			"case-044.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-045 bcrypt hashSync cost 5", async () => {
		const result = await scanSource(
			"case-045.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-046 bcrypt genSaltSync cost 5", async () => {
		const result = await scanSource(
			"case-046.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-047 bcrypt hashSync cost 6", async () => {
		const result = await scanSource(
			"case-047.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-048 bcrypt genSaltSync cost 6", async () => {
		const result = await scanSource(
			"case-048.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-049 bcrypt hashSync cost 7", async () => {
		const result = await scanSource(
			"case-049.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-050 bcrypt genSaltSync cost 7", async () => {
		const result = await scanSource(
			"case-050.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-051 bcrypt hashSync cost 8", async () => {
		const result = await scanSource(
			"case-051.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-052 bcrypt genSaltSync cost 8", async () => {
		const result = await scanSource(
			"case-052.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-053 bcrypt hashSync cost 9", async () => {
		const result = await scanSource(
			"case-053.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-054 bcrypt genSaltSync cost 9", async () => {
		const result = await scanSource(
			"case-054.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-055 bcrypt hashSync cost 10", async () => {
		const result = await scanSource(
			"case-055.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-056 bcrypt genSaltSync cost 10", async () => {
		const result = await scanSource(
			"case-056.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-057 bcrypt hashSync cost 11", async () => {
		const result = await scanSource(
			"case-057.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-058 bcrypt genSaltSync cost 11", async () => {
		const result = await scanSource(
			"case-058.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-059 bcrypt hashSync cost 12", async () => {
		const result = await scanSource(
			"case-059.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-060 bcrypt genSaltSync cost 12", async () => {
		const result = await scanSource(
			"case-060.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-061 bcrypt hashSync cost 13", async () => {
		const result = await scanSource(
			"case-061.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-062 bcrypt genSaltSync cost 13", async () => {
		const result = await scanSource(
			"case-062.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-063 bcrypt hashSync cost 14", async () => {
		const result = await scanSource(
			"case-063.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-064 bcrypt genSaltSync cost 14", async () => {
		const result = await scanSource(
			"case-064.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-065 bcrypt hashSync cost 15", async () => {
		const result = await scanSource(
			"case-065.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-066 bcrypt genSaltSync cost 15", async () => {
		const result = await scanSource(
			"case-066.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-067 pbkdf2 iter 1000", async () => {
		const result = await scanSource(
			"case-067.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-068 pbkdf2 iter 10000", async () => {
		const result = await scanSource(
			"case-068.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-069 pbkdf2 iter 50000", async () => {
		const result = await scanSource(
			"case-069.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-070 pbkdf2 iter 90000", async () => {
		const result = await scanSource(
			"case-070.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-071 pbkdf2 iter 99999", async () => {
		const result = await scanSource(
			"case-071.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-072 pbkdf2 iter 100000", async () => {
		const result = await scanSource(
			"case-072.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-073 pbkdf2 iter 150000", async () => {
		const result = await scanSource(
			"case-073.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-074 scrypt cost 1024", async () => {
		const result = await scanSource(
			"case-074.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-075 scrypt cost 4096", async () => {
		const result = await scanSource(
			"case-075.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-076 scrypt cost 8192", async () => {
		const result = await scanSource(
			"case-076.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-077 scrypt cost 16383", async () => {
		const result = await scanSource(
			"case-077.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-078 scrypt cost 16384", async () => {
		const result = await scanSource(
			"case-078.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-079 scrypt cost 32768", async () => {
		const result = await scanSource(
			"case-079.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-080 HASH-01 createHash md5 wrap0", async () => {
		const result = await scanSource(
			"case-080.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-081 HASH-01 createHash md5 wrap1", async () => {
		const result = await scanSource(
			"case-081.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-082 HASH-01 createHash md5 wrap2", async () => {
		const result = await scanSource(
			"case-082.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-083 HASH-01 createHash md5 wrap3", async () => {
		const result = await scanSource(
			"case-083.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-084 HASH-01 createHash md5 wrap4", async () => {
		const result = await scanSource(
			"case-084.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md5").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-085 HASH-01 createHash md4 wrap0", async () => {
		const result = await scanSource(
			"case-085.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-086 HASH-01 createHash md4 wrap1", async () => {
		const result = await scanSource(
			"case-086.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-087 HASH-01 createHash md4 wrap2", async () => {
		const result = await scanSource(
			"case-087.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-088 HASH-01 createHash md4 wrap3", async () => {
		const result = await scanSource(
			"case-088.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-089 HASH-01 createHash md4 wrap4", async () => {
		const result = await scanSource(
			"case-089.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md4").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-090 HASH-01 createHash sha1 wrap0", async () => {
		const result = await scanSource(
			"case-090.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-091 HASH-01 createHash sha1 wrap1", async () => {
		const result = await scanSource(
			"case-091.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-092 HASH-01 createHash sha1 wrap2", async () => {
		const result = await scanSource(
			"case-092.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-093 HASH-01 createHash sha1 wrap3", async () => {
		const result = await scanSource(
			"case-093.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-094 HASH-01 createHash sha1 wrap4", async () => {
		const result = await scanSource(
			"case-094.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-095 HASH-01 createHash sha-1 wrap0", async () => {
		const result = await scanSource(
			"case-095.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-096 HASH-01 createHash sha-1 wrap1", async () => {
		const result = await scanSource(
			"case-096.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-097 HASH-01 createHash sha-1 wrap2", async () => {
		const result = await scanSource(
			"case-097.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-098 HASH-01 createHash sha-1 wrap3", async () => {
		const result = await scanSource(
			"case-098.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-099 HASH-01 createHash sha-1 wrap4", async () => {
		const result = await scanSource(
			"case-099.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha-1").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-100 HASH-01 createHash md2 wrap0", async () => {
		const result = await scanSource(
			"case-100.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-101 HASH-01 createHash md2 wrap1", async () => {
		const result = await scanSource(
			"case-101.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-102 HASH-01 createHash md2 wrap2", async () => {
		const result = await scanSource(
			"case-102.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-103 HASH-01 createHash md2 wrap3", async () => {
		const result = await scanSource(
			"case-103.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-104 HASH-01 createHash md2 wrap4", async () => {
		const result = await scanSource(
			"case-104.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("md2").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-105 HASH-01 createHash ripemd160 wrap0", async () => {
		const result = await scanSource(
			"case-105.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-106 HASH-01 createHash ripemd160 wrap1", async () => {
		const result = await scanSource(
			"case-106.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-107 HASH-01 createHash ripemd160 wrap2", async () => {
		const result = await scanSource(
			"case-107.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-108 HASH-01 createHash ripemd160 wrap3", async () => {
		const result = await scanSource(
			"case-108.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-109 HASH-01 createHash ripemd160 wrap4", async () => {
		const result = await scanSource(
			"case-109.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("ripemd160").update(password).digest("hex");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-110 HASH-01 pbkdf2 md5 wrap0", async () => {
		const result = await scanSource(
			"case-110.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-111 HASH-01 pbkdf2 md5 wrap1", async () => {
		const result = await scanSource(
			"case-111.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-112 HASH-01 pbkdf2 md5 wrap2", async () => {
		const result = await scanSource(
			"case-112.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-113 HASH-01 pbkdf2 md5 wrap3", async () => {
		const result = await scanSource(
			"case-113.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-114 HASH-01 pbkdf2 md5 wrap4", async () => {
		const result = await scanSource(
			"case-114.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-115 HASH-01 pbkdf2 sha1 wrap0", async () => {
		const result = await scanSource(
			"case-115.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-116 HASH-01 pbkdf2 sha1 wrap1", async () => {
		const result = await scanSource(
			"case-116.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-117 HASH-01 pbkdf2 sha1 wrap2", async () => {
		const result = await scanSource(
			"case-117.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-118 HASH-01 pbkdf2 sha1 wrap3", async () => {
		const result = await scanSource(
			"case-118.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-119 HASH-01 pbkdf2 sha1 wrap4", async () => {
		const result = await scanSource(
			"case-119.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha1");\n}',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-120 HASH-01 sha512 password clean wrap0", async () => {
		const result = await scanSource(
			"case-120.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-121 HASH-01 sha512 password clean wrap1", async () => {
		const result = await scanSource(
			"case-121.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-122 HASH-01 sha512 password clean wrap2", async () => {
		const result = await scanSource(
			"case-122.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-123 HASH-01 sha512 password clean wrap3", async () => {
		const result = await scanSource(
			"case-123.ts",
			'import { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-124 HASH-01 sha512 password clean wrap4", async () => {
		const result = await scanSource(
			"case-124.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function hashPassword(password: string) {\n  return createHash("sha512").update(password).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-125 HASH-01 apiKey context md5 clean wrap0", async () => {
		const result = await scanSource(
			"case-125.ts",
			'"use strict";\nimport { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-126 HASH-01 apiKey context md5 clean wrap1", async () => {
		const result = await scanSource(
			"case-126.ts",
			'import { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-127 HASH-01 apiKey context md5 clean wrap2", async () => {
		const result = await scanSource(
			"case-127.ts",
			'// password hashing module\nimport { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-128 HASH-01 apiKey context md5 clean wrap3", async () => {
		const result = await scanSource(
			"case-128.ts",
			'import { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-129 HASH-01 apiKey context md5 clean wrap4", async () => {
		const result = await scanSource(
			"case-129.ts",
			'/* crypto */\nimport { createHash } from "crypto";\nexport function fingerprint(apiKey: string) {\n  return createHash("md5").update(apiKey).digest("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-130 HASH-02 bcrypt cost 4 wrap0", async () => {
		const result = await scanSource(
			"case-130.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-131 HASH-02 bcrypt cost 4 wrap1", async () => {
		const result = await scanSource(
			"case-131.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-132 HASH-02 bcrypt cost 4 wrap2", async () => {
		const result = await scanSource(
			"case-132.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-133 HASH-02 bcrypt cost 4 wrap3", async () => {
		const result = await scanSource(
			"case-133.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-134 HASH-02 bcrypt cost 4 wrap4", async () => {
		const result = await scanSource(
			"case-134.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-135 HASH-02 bcrypt cost 5 wrap0", async () => {
		const result = await scanSource(
			"case-135.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-136 HASH-02 bcrypt cost 5 wrap1", async () => {
		const result = await scanSource(
			"case-136.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-137 HASH-02 bcrypt cost 5 wrap2", async () => {
		const result = await scanSource(
			"case-137.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-138 HASH-02 bcrypt cost 5 wrap3", async () => {
		const result = await scanSource(
			"case-138.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-139 HASH-02 bcrypt cost 5 wrap4", async () => {
		const result = await scanSource(
			"case-139.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-140 HASH-02 bcrypt cost 7 wrap0", async () => {
		const result = await scanSource(
			"case-140.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-141 HASH-02 bcrypt cost 7 wrap1", async () => {
		const result = await scanSource(
			"case-141.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-142 HASH-02 bcrypt cost 7 wrap2", async () => {
		const result = await scanSource(
			"case-142.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-143 HASH-02 bcrypt cost 7 wrap3", async () => {
		const result = await scanSource(
			"case-143.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-144 HASH-02 bcrypt cost 7 wrap4", async () => {
		const result = await scanSource(
			"case-144.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-145 HASH-02 bcrypt cost 8 wrap0", async () => {
		const result = await scanSource(
			"case-145.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-146 HASH-02 bcrypt cost 8 wrap1", async () => {
		const result = await scanSource(
			"case-146.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-147 HASH-02 bcrypt cost 8 wrap2", async () => {
		const result = await scanSource(
			"case-147.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-148 HASH-02 bcrypt cost 8 wrap3", async () => {
		const result = await scanSource(
			"case-148.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-149 HASH-02 bcrypt cost 8 wrap4", async () => {
		const result = await scanSource(
			"case-149.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-150 HASH-02 bcrypt cost 9 wrap0", async () => {
		const result = await scanSource(
			"case-150.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-151 HASH-02 bcrypt cost 9 wrap1", async () => {
		const result = await scanSource(
			"case-151.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-152 HASH-02 bcrypt cost 9 wrap2", async () => {
		const result = await scanSource(
			"case-152.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-153 HASH-02 bcrypt cost 9 wrap3", async () => {
		const result = await scanSource(
			"case-153.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-154 HASH-02 bcrypt cost 9 wrap4", async () => {
		const result = await scanSource(
			"case-154.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-155 HASH-02 bcrypt cost 10 clean wrap0", async () => {
		const result = await scanSource(
			"case-155.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-156 HASH-02 bcrypt cost 10 clean wrap1", async () => {
		const result = await scanSource(
			"case-156.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-157 HASH-02 bcrypt cost 10 clean wrap2", async () => {
		const result = await scanSource(
			"case-157.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-158 HASH-02 bcrypt cost 10 clean wrap3", async () => {
		const result = await scanSource(
			"case-158.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-159 HASH-02 bcrypt cost 10 clean wrap4", async () => {
		const result = await scanSource(
			"case-159.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-160 HASH-02 bcrypt cost 11 clean wrap0", async () => {
		const result = await scanSource(
			"case-160.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-161 HASH-02 bcrypt cost 11 clean wrap1", async () => {
		const result = await scanSource(
			"case-161.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-162 HASH-02 bcrypt cost 11 clean wrap2", async () => {
		const result = await scanSource(
			"case-162.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-163 HASH-02 bcrypt cost 11 clean wrap3", async () => {
		const result = await scanSource(
			"case-163.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-164 HASH-02 bcrypt cost 11 clean wrap4", async () => {
		const result = await scanSource(
			"case-164.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-165 HASH-02 bcrypt cost 12 clean wrap0", async () => {
		const result = await scanSource(
			"case-165.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-166 HASH-02 bcrypt cost 12 clean wrap1", async () => {
		const result = await scanSource(
			"case-166.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-167 HASH-02 bcrypt cost 12 clean wrap2", async () => {
		const result = await scanSource(
			"case-167.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-168 HASH-02 bcrypt cost 12 clean wrap3", async () => {
		const result = await scanSource(
			"case-168.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-169 HASH-02 bcrypt cost 12 clean wrap4", async () => {
		const result = await scanSource(
			"case-169.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-170 HASH-02 bcrypt cost 14 clean wrap0", async () => {
		const result = await scanSource(
			"case-170.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-171 HASH-02 bcrypt cost 14 clean wrap1", async () => {
		const result = await scanSource(
			"case-171.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-172 HASH-02 bcrypt cost 14 clean wrap2", async () => {
		const result = await scanSource(
			"case-172.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-173 HASH-02 bcrypt cost 14 clean wrap3", async () => {
		const result = await scanSource(
			"case-173.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-174 HASH-02 bcrypt cost 14 clean wrap4", async () => {
		const result = await scanSource(
			"case-174.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-175 HASH-02 compare only clean wrap0", async () => {
		const result = await scanSource(
			"case-175.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-176 HASH-02 compare only clean wrap1", async () => {
		const result = await scanSource(
			"case-176.ts",
			'import bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-177 HASH-02 compare only clean wrap2", async () => {
		const result = await scanSource(
			"case-177.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-178 HASH-02 compare only clean wrap3", async () => {
		const result = await scanSource(
			"case-178.ts",
			'import bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-179 HASH-02 compare only clean wrap4", async () => {
		const result = await scanSource(
			"case-179.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function check(plain: string, hash: string) {\n  return bcrypt.compareSync(plain, hash);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-180 HASH-03 pbkdf2 iter 1000 wrap0", async () => {
		const result = await scanSource(
			"case-180.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-181 HASH-03 pbkdf2 iter 1000 wrap1", async () => {
		const result = await scanSource(
			"case-181.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-182 HASH-03 pbkdf2 iter 1000 wrap2", async () => {
		const result = await scanSource(
			"case-182.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-183 HASH-03 pbkdf2 iter 1000 wrap3", async () => {
		const result = await scanSource(
			"case-183.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-184 HASH-03 pbkdf2 iter 1000 wrap4", async () => {
		const result = await scanSource(
			"case-184.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-185 HASH-03 pbkdf2 iter 50000 wrap0", async () => {
		const result = await scanSource(
			"case-185.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-186 HASH-03 pbkdf2 iter 50000 wrap1", async () => {
		const result = await scanSource(
			"case-186.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-187 HASH-03 pbkdf2 iter 50000 wrap2", async () => {
		const result = await scanSource(
			"case-187.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-188 HASH-03 pbkdf2 iter 50000 wrap3", async () => {
		const result = await scanSource(
			"case-188.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-189 HASH-03 pbkdf2 iter 50000 wrap4", async () => {
		const result = await scanSource(
			"case-189.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-190 HASH-03 pbkdf2 iter 99999 wrap0", async () => {
		const result = await scanSource(
			"case-190.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-191 HASH-03 pbkdf2 iter 99999 wrap1", async () => {
		const result = await scanSource(
			"case-191.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-192 HASH-03 pbkdf2 iter 99999 wrap2", async () => {
		const result = await scanSource(
			"case-192.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-193 HASH-03 pbkdf2 iter 99999 wrap3", async () => {
		const result = await scanSource(
			"case-193.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-194 HASH-03 pbkdf2 iter 99999 wrap4", async () => {
		const result = await scanSource(
			"case-194.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-195 HASH-03 pbkdf2 iter 100000 clean wrap0", async () => {
		const result = await scanSource(
			"case-195.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-196 HASH-03 pbkdf2 iter 100000 clean wrap1", async () => {
		const result = await scanSource(
			"case-196.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-197 HASH-03 pbkdf2 iter 100000 clean wrap2", async () => {
		const result = await scanSource(
			"case-197.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-198 HASH-03 pbkdf2 iter 100000 clean wrap3", async () => {
		const result = await scanSource(
			"case-198.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-199 HASH-03 pbkdf2 iter 100000 clean wrap4", async () => {
		const result = await scanSource(
			"case-199.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-200 HASH-03 pbkdf2 iter 200000 clean wrap0", async () => {
		const result = await scanSource(
			"case-200.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-201 HASH-03 pbkdf2 iter 200000 clean wrap1", async () => {
		const result = await scanSource(
			"case-201.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-202 HASH-03 pbkdf2 iter 200000 clean wrap2", async () => {
		const result = await scanSource(
			"case-202.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-203 HASH-03 pbkdf2 iter 200000 clean wrap3", async () => {
		const result = await scanSource(
			"case-203.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-204 HASH-03 pbkdf2 iter 200000 clean wrap4", async () => {
		const result = await scanSource(
			"case-204.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 200000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-205 HASH-04 scrypt cost=16383 block=8 wrap0", async () => {
		const result = await scanSource(
			"case-205.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-206 HASH-04 scrypt cost=16383 block=8 wrap1", async () => {
		const result = await scanSource(
			"case-206.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-207 HASH-04 scrypt cost=16383 block=8 wrap2", async () => {
		const result = await scanSource(
			"case-207.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-208 HASH-04 scrypt cost=16383 block=8 wrap3", async () => {
		const result = await scanSource(
			"case-208.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-209 HASH-04 scrypt cost=16383 block=8 wrap4", async () => {
		const result = await scanSource(
			"case-209.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-210 HASH-04 scrypt cost=16384 block=8 wrap0", async () => {
		const result = await scanSource(
			"case-210.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-211 HASH-04 scrypt cost=16384 block=8 wrap1", async () => {
		const result = await scanSource(
			"case-211.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-212 HASH-04 scrypt cost=16384 block=8 wrap2", async () => {
		const result = await scanSource(
			"case-212.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-213 HASH-04 scrypt cost=16384 block=8 wrap3", async () => {
		const result = await scanSource(
			"case-213.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-214 HASH-04 scrypt cost=16384 block=8 wrap4", async () => {
		const result = await scanSource(
			"case-214.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-215 HASH-04 scrypt cost=16384 block=7 wrap0", async () => {
		const result = await scanSource(
			"case-215.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-216 HASH-04 scrypt cost=16384 block=7 wrap1", async () => {
		const result = await scanSource(
			"case-216.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-217 HASH-04 scrypt cost=16384 block=7 wrap2", async () => {
		const result = await scanSource(
			"case-217.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-218 HASH-04 scrypt cost=16384 block=7 wrap3", async () => {
		const result = await scanSource(
			"case-218.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-219 HASH-04 scrypt cost=16384 block=7 wrap4", async () => {
		const result = await scanSource(
			"case-219.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 7, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-220 HASH-04 scrypt cost=16384 block=8 wrap0", async () => {
		const result = await scanSource(
			"case-220.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-221 HASH-04 scrypt cost=16384 block=8 wrap1", async () => {
		const result = await scanSource(
			"case-221.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-222 HASH-04 scrypt cost=16384 block=8 wrap2", async () => {
		const result = await scanSource(
			"case-222.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-223 HASH-04 scrypt cost=16384 block=8 wrap3", async () => {
		const result = await scanSource(
			"case-223.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-224 HASH-04 scrypt cost=16384 block=8 wrap4", async () => {
		const result = await scanSource(
			"case-224.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 0 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-225 HASH-04 scrypt cost=8192 block=8 wrap0", async () => {
		const result = await scanSource(
			"case-225.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-226 HASH-04 scrypt cost=8192 block=8 wrap1", async () => {
		const result = await scanSource(
			"case-226.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-227 HASH-04 scrypt cost=8192 block=8 wrap2", async () => {
		const result = await scanSource(
			"case-227.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-228 HASH-04 scrypt cost=8192 block=8 wrap3", async () => {
		const result = await scanSource(
			"case-228.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-229 HASH-04 scrypt cost=8192 block=8 wrap4", async () => {
		const result = await scanSource(
			"case-229.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-230 HASH-04 scrypt default no options clean wrap0", async () => {
		const result = await scanSource(
			"case-230.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-231 HASH-04 scrypt default no options clean wrap1", async () => {
		const result = await scanSource(
			"case-231.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-232 HASH-04 scrypt default no options clean wrap2", async () => {
		const result = await scanSource(
			"case-232.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-233 HASH-04 scrypt default no options clean wrap3", async () => {
		const result = await scanSource(
			"case-233.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-234 HASH-04 scrypt default no options clean wrap4", async () => {
		const result = await scanSource(
			"case-234.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-235 HASH-05 argon2 time=2 mem=65536 wrap0", async () => {
		const result = await scanSource(
			"case-235.ts",
			'"use strict";\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-236 HASH-05 argon2 time=2 mem=65536 wrap1", async () => {
		const result = await scanSource(
			"case-236.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-237 HASH-05 argon2 time=2 mem=65536 wrap2", async () => {
		const result = await scanSource(
			"case-237.ts",
			'// password hashing module\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-238 HASH-05 argon2 time=2 mem=65536 wrap3", async () => {
		const result = await scanSource(
			"case-238.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-239 HASH-05 argon2 time=2 mem=65536 wrap4", async () => {
		const result = await scanSource(
			"case-239.ts",
			'/* crypto */\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 2, memoryCost: 65536 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-240 HASH-05 argon2 time=3 mem=65536 wrap0", async () => {
		const result = await scanSource(
			"case-240.ts",
			'"use strict";\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-241 HASH-05 argon2 time=3 mem=65536 wrap1", async () => {
		const result = await scanSource(
			"case-241.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-242 HASH-05 argon2 time=3 mem=65536 wrap2", async () => {
		const result = await scanSource(
			"case-242.ts",
			'// password hashing module\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-243 HASH-05 argon2 time=3 mem=65536 wrap3", async () => {
		const result = await scanSource(
			"case-243.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-244 HASH-05 argon2 time=3 mem=65536 wrap4", async () => {
		const result = await scanSource(
			"case-244.ts",
			'/* crypto */\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 65536 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-245 HASH-05 argon2 time=3 mem=32768 wrap0", async () => {
		const result = await scanSource(
			"case-245.ts",
			'"use strict";\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-246 HASH-05 argon2 time=3 mem=32768 wrap1", async () => {
		const result = await scanSource(
			"case-246.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-247 HASH-05 argon2 time=3 mem=32768 wrap2", async () => {
		const result = await scanSource(
			"case-247.ts",
			'// password hashing module\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-248 HASH-05 argon2 time=3 mem=32768 wrap3", async () => {
		const result = await scanSource(
			"case-248.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-249 HASH-05 argon2 time=3 mem=32768 wrap4", async () => {
		const result = await scanSource(
			"case-249.ts",
			'/* crypto */\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 3, memoryCost: 32768 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-250 HASH-05 argon2 time=1 mem=131072 wrap0", async () => {
		const result = await scanSource(
			"case-250.ts",
			'"use strict";\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-251 HASH-05 argon2 time=1 mem=131072 wrap1", async () => {
		const result = await scanSource(
			"case-251.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-252 HASH-05 argon2 time=1 mem=131072 wrap2", async () => {
		const result = await scanSource(
			"case-252.ts",
			'// password hashing module\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-253 HASH-05 argon2 time=1 mem=131072 wrap3", async () => {
		const result = await scanSource(
			"case-253.ts",
			'import argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-254 HASH-05 argon2 time=1 mem=131072 wrap4", async () => {
		const result = await scanSource(
			"case-254.ts",
			'/* crypto */\nimport argon2 from "argon2";\nexport async function hashPassword(password: string) {\n  return argon2.hash(password, { timeCost: 1, memoryCost: 131072 });\n}',
		);
		const expected = { "CS-HASH-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-255 HASH-01 require crypto md5 wrap0", async () => {
		const result = await scanSource(
			"case-255.ts",
			'"use strict";\nconst { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-256 HASH-01 require crypto md5 wrap1", async () => {
		const result = await scanSource(
			"case-256.ts",
			'const { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-257 HASH-01 require crypto md5 wrap2", async () => {
		const result = await scanSource(
			"case-257.ts",
			'// password hashing module\nconst { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-258 HASH-01 require crypto md5 wrap3", async () => {
		const result = await scanSource(
			"case-258.ts",
			'const { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-259 HASH-01 require crypto md5 wrap4", async () => {
		const result = await scanSource(
			"case-259.ts",
			'/* crypto */\nconst { createHash } = require("crypto");\nexports.hash = function(password) {\n  return createHash("md5").update(password).digest("hex");\n};',
		);
		const expected = { "CS-HASH-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-260 HASH-02 node-rs bcrypt cost 8 wrap0", async () => {
		const result = await scanSource(
			"case-260.ts",
			'"use strict";\nimport bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-261 HASH-02 node-rs bcrypt cost 8 wrap1", async () => {
		const result = await scanSource(
			"case-261.ts",
			'import bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-262 HASH-02 node-rs bcrypt cost 8 wrap2", async () => {
		const result = await scanSource(
			"case-262.ts",
			'// password hashing module\nimport bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-263 HASH-02 node-rs bcrypt cost 8 wrap3", async () => {
		const result = await scanSource(
			"case-263.ts",
			'import bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-264 HASH-02 node-rs bcrypt cost 8 wrap4", async () => {
		const result = await scanSource(
			"case-264.ts",
			'/* crypto */\nimport bcrypt from "@node-rs/bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-265 HASH-01+03 pbkdf2 md5 low iter wrap0", async () => {
		const result = await scanSource(
			"case-265.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-266 HASH-01+03 pbkdf2 md5 low iter wrap1", async () => {
		const result = await scanSource(
			"case-266.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-267 HASH-01+03 pbkdf2 md5 low iter wrap2", async () => {
		const result = await scanSource(
			"case-267.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-268 HASH-01+03 pbkdf2 md5 low iter wrap3", async () => {
		const result = await scanSource(
			"case-268.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-269 HASH-01+03 pbkdf2 md5 low iter wrap4", async () => {
		const result = await scanSource(
			"case-269.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "md5");\n}',
		);
		const expected = { "CS-HASH-01": 1, "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-270 HASH-02 namespace bcrypt cost 6 wrap0", async () => {
		const result = await scanSource(
			"case-270.ts",
			'"use strict";\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-271 HASH-02 namespace bcrypt cost 6 wrap1", async () => {
		const result = await scanSource(
			"case-271.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-272 HASH-02 namespace bcrypt cost 6 wrap2", async () => {
		const result = await scanSource(
			"case-272.ts",
			'// password hashing module\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-273 HASH-02 namespace bcrypt cost 6 wrap3", async () => {
		const result = await scanSource(
			"case-273.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-274 HASH-02 namespace bcrypt cost 6 wrap4", async () => {
		const result = await scanSource(
			"case-274.ts",
			'/* crypto */\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-275 HASH-02 namespace bcrypt cost 8 wrap0", async () => {
		const result = await scanSource(
			"case-275.ts",
			'"use strict";\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-276 HASH-02 namespace bcrypt cost 8 wrap1", async () => {
		const result = await scanSource(
			"case-276.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-277 HASH-02 namespace bcrypt cost 8 wrap2", async () => {
		const result = await scanSource(
			"case-277.ts",
			'// password hashing module\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-278 HASH-02 namespace bcrypt cost 8 wrap3", async () => {
		const result = await scanSource(
			"case-278.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-279 HASH-02 namespace bcrypt cost 8 wrap4", async () => {
		const result = await scanSource(
			"case-279.ts",
			'/* crypto */\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-280 HASH-02 namespace bcrypt cost 10 wrap0", async () => {
		const result = await scanSource(
			"case-280.ts",
			'"use strict";\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-281 HASH-02 namespace bcrypt cost 10 wrap1", async () => {
		const result = await scanSource(
			"case-281.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-282 HASH-02 namespace bcrypt cost 10 wrap2", async () => {
		const result = await scanSource(
			"case-282.ts",
			'// password hashing module\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-283 HASH-02 namespace bcrypt cost 10 wrap3", async () => {
		const result = await scanSource(
			"case-283.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-284 HASH-02 namespace bcrypt cost 10 wrap4", async () => {
		const result = await scanSource(
			"case-284.ts",
			'/* crypto */\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-285 HASH-02 namespace bcrypt cost 12 wrap0", async () => {
		const result = await scanSource(
			"case-285.ts",
			'"use strict";\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-286 HASH-02 namespace bcrypt cost 12 wrap1", async () => {
		const result = await scanSource(
			"case-286.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-287 HASH-02 namespace bcrypt cost 12 wrap2", async () => {
		const result = await scanSource(
			"case-287.ts",
			'// password hashing module\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-288 HASH-02 namespace bcrypt cost 12 wrap3", async () => {
		const result = await scanSource(
			"case-288.ts",
			'import * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-289 HASH-02 namespace bcrypt cost 12 wrap4", async () => {
		const result = await scanSource(
			"case-289.ts",
			'/* crypto */\nimport * as bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-290 bcrypt hashSync cost 4 wrap0", async () => {
		const result = await scanSource(
			"case-290.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-291 bcrypt hashSync cost 4 wrap1", async () => {
		const result = await scanSource(
			"case-291.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-292 bcrypt hashSync cost 4 wrap2", async () => {
		const result = await scanSource(
			"case-292.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-293 bcrypt hashSync cost 4 wrap3", async () => {
		const result = await scanSource(
			"case-293.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-294 bcrypt hashSync cost 4 wrap4", async () => {
		const result = await scanSource(
			"case-294.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 4);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-295 bcrypt genSaltSync cost 4 wrap0", async () => {
		const result = await scanSource(
			"case-295.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-296 bcrypt genSaltSync cost 4 wrap1", async () => {
		const result = await scanSource(
			"case-296.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-297 bcrypt genSaltSync cost 4 wrap2", async () => {
		const result = await scanSource(
			"case-297.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-298 bcrypt genSaltSync cost 4 wrap3", async () => {
		const result = await scanSource(
			"case-298.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-299 bcrypt genSaltSync cost 4 wrap4", async () => {
		const result = await scanSource(
			"case-299.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(4);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-300 bcrypt hashSync cost 5 wrap0", async () => {
		const result = await scanSource(
			"case-300.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-301 bcrypt hashSync cost 5 wrap1", async () => {
		const result = await scanSource(
			"case-301.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-302 bcrypt hashSync cost 5 wrap2", async () => {
		const result = await scanSource(
			"case-302.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-303 bcrypt hashSync cost 5 wrap3", async () => {
		const result = await scanSource(
			"case-303.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-304 bcrypt hashSync cost 5 wrap4", async () => {
		const result = await scanSource(
			"case-304.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 5);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-305 bcrypt genSaltSync cost 5 wrap0", async () => {
		const result = await scanSource(
			"case-305.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-306 bcrypt genSaltSync cost 5 wrap1", async () => {
		const result = await scanSource(
			"case-306.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-307 bcrypt genSaltSync cost 5 wrap2", async () => {
		const result = await scanSource(
			"case-307.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-308 bcrypt genSaltSync cost 5 wrap3", async () => {
		const result = await scanSource(
			"case-308.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-309 bcrypt genSaltSync cost 5 wrap4", async () => {
		const result = await scanSource(
			"case-309.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(5);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-310 bcrypt hashSync cost 6 wrap0", async () => {
		const result = await scanSource(
			"case-310.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-311 bcrypt hashSync cost 6 wrap1", async () => {
		const result = await scanSource(
			"case-311.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-312 bcrypt hashSync cost 6 wrap2", async () => {
		const result = await scanSource(
			"case-312.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-313 bcrypt hashSync cost 6 wrap3", async () => {
		const result = await scanSource(
			"case-313.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-314 bcrypt hashSync cost 6 wrap4", async () => {
		const result = await scanSource(
			"case-314.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 6);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-315 bcrypt genSaltSync cost 6 wrap0", async () => {
		const result = await scanSource(
			"case-315.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-316 bcrypt genSaltSync cost 6 wrap1", async () => {
		const result = await scanSource(
			"case-316.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-317 bcrypt genSaltSync cost 6 wrap2", async () => {
		const result = await scanSource(
			"case-317.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-318 bcrypt genSaltSync cost 6 wrap3", async () => {
		const result = await scanSource(
			"case-318.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-319 bcrypt genSaltSync cost 6 wrap4", async () => {
		const result = await scanSource(
			"case-319.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(6);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-320 bcrypt hashSync cost 7 wrap0", async () => {
		const result = await scanSource(
			"case-320.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-321 bcrypt hashSync cost 7 wrap1", async () => {
		const result = await scanSource(
			"case-321.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-322 bcrypt hashSync cost 7 wrap2", async () => {
		const result = await scanSource(
			"case-322.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-323 bcrypt hashSync cost 7 wrap3", async () => {
		const result = await scanSource(
			"case-323.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-324 bcrypt hashSync cost 7 wrap4", async () => {
		const result = await scanSource(
			"case-324.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 7);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-325 bcrypt genSaltSync cost 7 wrap0", async () => {
		const result = await scanSource(
			"case-325.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-326 bcrypt genSaltSync cost 7 wrap1", async () => {
		const result = await scanSource(
			"case-326.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-327 bcrypt genSaltSync cost 7 wrap2", async () => {
		const result = await scanSource(
			"case-327.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-328 bcrypt genSaltSync cost 7 wrap3", async () => {
		const result = await scanSource(
			"case-328.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-329 bcrypt genSaltSync cost 7 wrap4", async () => {
		const result = await scanSource(
			"case-329.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(7);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-330 bcrypt hashSync cost 8 wrap0", async () => {
		const result = await scanSource(
			"case-330.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-331 bcrypt hashSync cost 8 wrap1", async () => {
		const result = await scanSource(
			"case-331.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-332 bcrypt hashSync cost 8 wrap2", async () => {
		const result = await scanSource(
			"case-332.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-333 bcrypt hashSync cost 8 wrap3", async () => {
		const result = await scanSource(
			"case-333.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-334 bcrypt hashSync cost 8 wrap4", async () => {
		const result = await scanSource(
			"case-334.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 8);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-335 bcrypt genSaltSync cost 8 wrap0", async () => {
		const result = await scanSource(
			"case-335.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-336 bcrypt genSaltSync cost 8 wrap1", async () => {
		const result = await scanSource(
			"case-336.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-337 bcrypt genSaltSync cost 8 wrap2", async () => {
		const result = await scanSource(
			"case-337.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-338 bcrypt genSaltSync cost 8 wrap3", async () => {
		const result = await scanSource(
			"case-338.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-339 bcrypt genSaltSync cost 8 wrap4", async () => {
		const result = await scanSource(
			"case-339.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(8);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-340 bcrypt hashSync cost 9 wrap0", async () => {
		const result = await scanSource(
			"case-340.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-341 bcrypt hashSync cost 9 wrap1", async () => {
		const result = await scanSource(
			"case-341.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-342 bcrypt hashSync cost 9 wrap2", async () => {
		const result = await scanSource(
			"case-342.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-343 bcrypt hashSync cost 9 wrap3", async () => {
		const result = await scanSource(
			"case-343.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-344 bcrypt hashSync cost 9 wrap4", async () => {
		const result = await scanSource(
			"case-344.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 9);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-345 bcrypt genSaltSync cost 9 wrap0", async () => {
		const result = await scanSource(
			"case-345.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-346 bcrypt genSaltSync cost 9 wrap1", async () => {
		const result = await scanSource(
			"case-346.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-347 bcrypt genSaltSync cost 9 wrap2", async () => {
		const result = await scanSource(
			"case-347.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-348 bcrypt genSaltSync cost 9 wrap3", async () => {
		const result = await scanSource(
			"case-348.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-349 bcrypt genSaltSync cost 9 wrap4", async () => {
		const result = await scanSource(
			"case-349.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(9);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = { "CS-HASH-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-350 bcrypt hashSync cost 10 wrap0", async () => {
		const result = await scanSource(
			"case-350.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-351 bcrypt hashSync cost 10 wrap1", async () => {
		const result = await scanSource(
			"case-351.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-352 bcrypt hashSync cost 10 wrap2", async () => {
		const result = await scanSource(
			"case-352.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-353 bcrypt hashSync cost 10 wrap3", async () => {
		const result = await scanSource(
			"case-353.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-354 bcrypt hashSync cost 10 wrap4", async () => {
		const result = await scanSource(
			"case-354.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 10);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-355 bcrypt genSaltSync cost 10 wrap0", async () => {
		const result = await scanSource(
			"case-355.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-356 bcrypt genSaltSync cost 10 wrap1", async () => {
		const result = await scanSource(
			"case-356.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-357 bcrypt genSaltSync cost 10 wrap2", async () => {
		const result = await scanSource(
			"case-357.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-358 bcrypt genSaltSync cost 10 wrap3", async () => {
		const result = await scanSource(
			"case-358.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-359 bcrypt genSaltSync cost 10 wrap4", async () => {
		const result = await scanSource(
			"case-359.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(10);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-360 bcrypt hashSync cost 11 wrap0", async () => {
		const result = await scanSource(
			"case-360.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-361 bcrypt hashSync cost 11 wrap1", async () => {
		const result = await scanSource(
			"case-361.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-362 bcrypt hashSync cost 11 wrap2", async () => {
		const result = await scanSource(
			"case-362.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-363 bcrypt hashSync cost 11 wrap3", async () => {
		const result = await scanSource(
			"case-363.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-364 bcrypt hashSync cost 11 wrap4", async () => {
		const result = await scanSource(
			"case-364.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 11);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-365 bcrypt genSaltSync cost 11 wrap0", async () => {
		const result = await scanSource(
			"case-365.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-366 bcrypt genSaltSync cost 11 wrap1", async () => {
		const result = await scanSource(
			"case-366.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-367 bcrypt genSaltSync cost 11 wrap2", async () => {
		const result = await scanSource(
			"case-367.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-368 bcrypt genSaltSync cost 11 wrap3", async () => {
		const result = await scanSource(
			"case-368.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-369 bcrypt genSaltSync cost 11 wrap4", async () => {
		const result = await scanSource(
			"case-369.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(11);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-370 bcrypt hashSync cost 12 wrap0", async () => {
		const result = await scanSource(
			"case-370.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-371 bcrypt hashSync cost 12 wrap1", async () => {
		const result = await scanSource(
			"case-371.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-372 bcrypt hashSync cost 12 wrap2", async () => {
		const result = await scanSource(
			"case-372.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-373 bcrypt hashSync cost 12 wrap3", async () => {
		const result = await scanSource(
			"case-373.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-374 bcrypt hashSync cost 12 wrap4", async () => {
		const result = await scanSource(
			"case-374.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 12);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-375 bcrypt genSaltSync cost 12 wrap0", async () => {
		const result = await scanSource(
			"case-375.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-376 bcrypt genSaltSync cost 12 wrap1", async () => {
		const result = await scanSource(
			"case-376.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-377 bcrypt genSaltSync cost 12 wrap2", async () => {
		const result = await scanSource(
			"case-377.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-378 bcrypt genSaltSync cost 12 wrap3", async () => {
		const result = await scanSource(
			"case-378.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-379 bcrypt genSaltSync cost 12 wrap4", async () => {
		const result = await scanSource(
			"case-379.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(12);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-380 bcrypt hashSync cost 13 wrap0", async () => {
		const result = await scanSource(
			"case-380.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-381 bcrypt hashSync cost 13 wrap1", async () => {
		const result = await scanSource(
			"case-381.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-382 bcrypt hashSync cost 13 wrap2", async () => {
		const result = await scanSource(
			"case-382.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-383 bcrypt hashSync cost 13 wrap3", async () => {
		const result = await scanSource(
			"case-383.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-384 bcrypt hashSync cost 13 wrap4", async () => {
		const result = await scanSource(
			"case-384.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 13);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-385 bcrypt genSaltSync cost 13 wrap0", async () => {
		const result = await scanSource(
			"case-385.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-386 bcrypt genSaltSync cost 13 wrap1", async () => {
		const result = await scanSource(
			"case-386.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-387 bcrypt genSaltSync cost 13 wrap2", async () => {
		const result = await scanSource(
			"case-387.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-388 bcrypt genSaltSync cost 13 wrap3", async () => {
		const result = await scanSource(
			"case-388.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-389 bcrypt genSaltSync cost 13 wrap4", async () => {
		const result = await scanSource(
			"case-389.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(13);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-390 bcrypt hashSync cost 14 wrap0", async () => {
		const result = await scanSource(
			"case-390.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-391 bcrypt hashSync cost 14 wrap1", async () => {
		const result = await scanSource(
			"case-391.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-392 bcrypt hashSync cost 14 wrap2", async () => {
		const result = await scanSource(
			"case-392.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-393 bcrypt hashSync cost 14 wrap3", async () => {
		const result = await scanSource(
			"case-393.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-394 bcrypt hashSync cost 14 wrap4", async () => {
		const result = await scanSource(
			"case-394.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 14);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-395 bcrypt genSaltSync cost 14 wrap0", async () => {
		const result = await scanSource(
			"case-395.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-396 bcrypt genSaltSync cost 14 wrap1", async () => {
		const result = await scanSource(
			"case-396.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-397 bcrypt genSaltSync cost 14 wrap2", async () => {
		const result = await scanSource(
			"case-397.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-398 bcrypt genSaltSync cost 14 wrap3", async () => {
		const result = await scanSource(
			"case-398.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-399 bcrypt genSaltSync cost 14 wrap4", async () => {
		const result = await scanSource(
			"case-399.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(14);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-400 bcrypt hashSync cost 15 wrap0", async () => {
		const result = await scanSource(
			"case-400.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-401 bcrypt hashSync cost 15 wrap1", async () => {
		const result = await scanSource(
			"case-401.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-402 bcrypt hashSync cost 15 wrap2", async () => {
		const result = await scanSource(
			"case-402.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-403 bcrypt hashSync cost 15 wrap3", async () => {
		const result = await scanSource(
			"case-403.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-404 bcrypt hashSync cost 15 wrap4", async () => {
		const result = await scanSource(
			"case-404.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  return bcrypt.hashSync(password, 15);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-405 bcrypt genSaltSync cost 15 wrap0", async () => {
		const result = await scanSource(
			"case-405.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-406 bcrypt genSaltSync cost 15 wrap1", async () => {
		const result = await scanSource(
			"case-406.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-407 bcrypt genSaltSync cost 15 wrap2", async () => {
		const result = await scanSource(
			"case-407.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-408 bcrypt genSaltSync cost 15 wrap3", async () => {
		const result = await scanSource(
			"case-408.ts",
			'import bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-409 bcrypt genSaltSync cost 15 wrap4", async () => {
		const result = await scanSource(
			"case-409.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function hashPassword(password: string) {\n  const salt = bcrypt.genSaltSync(15);\n  return bcrypt.hashSync(password, salt);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-410 pbkdf2 iter 1000 wrap0", async () => {
		const result = await scanSource(
			"case-410.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-411 pbkdf2 iter 1000 wrap1", async () => {
		const result = await scanSource(
			"case-411.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-412 pbkdf2 iter 1000 wrap2", async () => {
		const result = await scanSource(
			"case-412.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-413 pbkdf2 iter 1000 wrap3", async () => {
		const result = await scanSource(
			"case-413.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-414 pbkdf2 iter 1000 wrap4", async () => {
		const result = await scanSource(
			"case-414.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 1000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-415 pbkdf2 iter 10000 wrap0", async () => {
		const result = await scanSource(
			"case-415.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-416 pbkdf2 iter 10000 wrap1", async () => {
		const result = await scanSource(
			"case-416.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-417 pbkdf2 iter 10000 wrap2", async () => {
		const result = await scanSource(
			"case-417.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-418 pbkdf2 iter 10000 wrap3", async () => {
		const result = await scanSource(
			"case-418.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-419 pbkdf2 iter 10000 wrap4", async () => {
		const result = await scanSource(
			"case-419.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 10000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-420 pbkdf2 iter 50000 wrap0", async () => {
		const result = await scanSource(
			"case-420.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-421 pbkdf2 iter 50000 wrap1", async () => {
		const result = await scanSource(
			"case-421.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-422 pbkdf2 iter 50000 wrap2", async () => {
		const result = await scanSource(
			"case-422.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-423 pbkdf2 iter 50000 wrap3", async () => {
		const result = await scanSource(
			"case-423.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-424 pbkdf2 iter 50000 wrap4", async () => {
		const result = await scanSource(
			"case-424.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 50000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-425 pbkdf2 iter 90000 wrap0", async () => {
		const result = await scanSource(
			"case-425.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-426 pbkdf2 iter 90000 wrap1", async () => {
		const result = await scanSource(
			"case-426.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-427 pbkdf2 iter 90000 wrap2", async () => {
		const result = await scanSource(
			"case-427.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-428 pbkdf2 iter 90000 wrap3", async () => {
		const result = await scanSource(
			"case-428.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-429 pbkdf2 iter 90000 wrap4", async () => {
		const result = await scanSource(
			"case-429.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 90000, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-430 pbkdf2 iter 99999 wrap0", async () => {
		const result = await scanSource(
			"case-430.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-431 pbkdf2 iter 99999 wrap1", async () => {
		const result = await scanSource(
			"case-431.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-432 pbkdf2 iter 99999 wrap2", async () => {
		const result = await scanSource(
			"case-432.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-433 pbkdf2 iter 99999 wrap3", async () => {
		const result = await scanSource(
			"case-433.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-434 pbkdf2 iter 99999 wrap4", async () => {
		const result = await scanSource(
			"case-434.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 99999, 32, "sha256");\n}',
		);
		const expected = { "CS-HASH-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-435 pbkdf2 iter 100000 wrap0", async () => {
		const result = await scanSource(
			"case-435.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-436 pbkdf2 iter 100000 wrap1", async () => {
		const result = await scanSource(
			"case-436.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-437 pbkdf2 iter 100000 wrap2", async () => {
		const result = await scanSource(
			"case-437.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-438 pbkdf2 iter 100000 wrap3", async () => {
		const result = await scanSource(
			"case-438.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-439 pbkdf2 iter 100000 wrap4", async () => {
		const result = await scanSource(
			"case-439.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 100000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-440 pbkdf2 iter 150000 wrap0", async () => {
		const result = await scanSource(
			"case-440.ts",
			'"use strict";\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-441 pbkdf2 iter 150000 wrap1", async () => {
		const result = await scanSource(
			"case-441.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-442 pbkdf2 iter 150000 wrap2", async () => {
		const result = await scanSource(
			"case-442.ts",
			'// password hashing module\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-443 pbkdf2 iter 150000 wrap3", async () => {
		const result = await scanSource(
			"case-443.ts",
			'import { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-444 pbkdf2 iter 150000 wrap4", async () => {
		const result = await scanSource(
			"case-444.ts",
			'/* crypto */\nimport { pbkdf2Sync } from "crypto";\nexport function derive(password: string, salt: Buffer) {\n  return pbkdf2Sync(password, salt, 150000, 32, "sha256");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-445 scrypt cost 1024 wrap0", async () => {
		const result = await scanSource(
			"case-445.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-446 scrypt cost 1024 wrap1", async () => {
		const result = await scanSource(
			"case-446.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-447 scrypt cost 1024 wrap2", async () => {
		const result = await scanSource(
			"case-447.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-448 scrypt cost 1024 wrap3", async () => {
		const result = await scanSource(
			"case-448.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-449 scrypt cost 1024 wrap4", async () => {
		const result = await scanSource(
			"case-449.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 1024, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-450 scrypt cost 4096 wrap0", async () => {
		const result = await scanSource(
			"case-450.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-451 scrypt cost 4096 wrap1", async () => {
		const result = await scanSource(
			"case-451.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-452 scrypt cost 4096 wrap2", async () => {
		const result = await scanSource(
			"case-452.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-453 scrypt cost 4096 wrap3", async () => {
		const result = await scanSource(
			"case-453.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-454 scrypt cost 4096 wrap4", async () => {
		const result = await scanSource(
			"case-454.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 4096, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-455 scrypt cost 8192 wrap0", async () => {
		const result = await scanSource(
			"case-455.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-456 scrypt cost 8192 wrap1", async () => {
		const result = await scanSource(
			"case-456.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-457 scrypt cost 8192 wrap2", async () => {
		const result = await scanSource(
			"case-457.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-458 scrypt cost 8192 wrap3", async () => {
		const result = await scanSource(
			"case-458.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-459 scrypt cost 8192 wrap4", async () => {
		const result = await scanSource(
			"case-459.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 8192, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-460 scrypt cost 16383 wrap0", async () => {
		const result = await scanSource(
			"case-460.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-461 scrypt cost 16383 wrap1", async () => {
		const result = await scanSource(
			"case-461.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-462 scrypt cost 16383 wrap2", async () => {
		const result = await scanSource(
			"case-462.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-463 scrypt cost 16383 wrap3", async () => {
		const result = await scanSource(
			"case-463.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-464 scrypt cost 16383 wrap4", async () => {
		const result = await scanSource(
			"case-464.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16383, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = { "CS-HASH-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-465 scrypt cost 16384 wrap0", async () => {
		const result = await scanSource(
			"case-465.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-466 scrypt cost 16384 wrap1", async () => {
		const result = await scanSource(
			"case-466.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-467 scrypt cost 16384 wrap2", async () => {
		const result = await scanSource(
			"case-467.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-468 scrypt cost 16384 wrap3", async () => {
		const result = await scanSource(
			"case-468.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-469 scrypt cost 16384 wrap4", async () => {
		const result = await scanSource(
			"case-469.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 16384, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-470 scrypt cost 32768 wrap0", async () => {
		const result = await scanSource(
			"case-470.ts",
			'"use strict";\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-471 scrypt cost 32768 wrap1", async () => {
		const result = await scanSource(
			"case-471.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-472 scrypt cost 32768 wrap2", async () => {
		const result = await scanSource(
			"case-472.ts",
			'// password hashing module\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-473 scrypt cost 32768 wrap3", async () => {
		const result = await scanSource(
			"case-473.ts",
			'import { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-HASH-474 scrypt cost 32768 wrap4", async () => {
		const result = await scanSource(
			"case-474.ts",
			'/* crypto */\nimport { scryptSync } from "crypto";\nexport function hashPassword(pwd: string, salt: Buffer) {\n  return scryptSync(pwd, salt, 64, { cost: 32768, blockSize: 8, parallelization: 1 });\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
