// Auto-generated by scripts/generate-v131-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/v131-scan-source.js";

describe("CS-V131 CMP-RNG exhaustive edge cases", () => {
	it("CS-V131-CMP-RNG-001 CMP-01 === token compare", async () => {
		const result = await scanSource(
			"case-001.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-002 CMP-01 == token compare", async () => {
		const result = await scanSource(
			"case-002.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-003 CMP-01 !== token compare", async () => {
		const result = await scanSource(
			"case-003.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-004 CMP-01 != token compare", async () => {
		const result = await scanSource(
			"case-004.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-005 CMP-01 === token compare", async () => {
		const result = await scanSource(
			"case-005.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-006 CMP-01 == token compare", async () => {
		const result = await scanSource(
			"case-006.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-007 CMP-01 !== token compare", async () => {
		const result = await scanSource(
			"case-007.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-008 CMP-01 != token compare", async () => {
		const result = await scanSource(
			"case-008.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-009 CMP-01 === token compare", async () => {
		const result = await scanSource(
			"case-009.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-010 CMP-01 == token compare", async () => {
		const result = await scanSource(
			"case-010.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-011 CMP-01 !== token compare", async () => {
		const result = await scanSource(
			"case-011.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-012 CMP-01 != token compare", async () => {
		const result = await scanSource(
			"case-012.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-013 CMP-01 no crypto import clean", async () => {
		const result = await scanSource(
			"case-013.ts",
			"export function check(token: string, expected: string) {\n  return token === expected;\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-014 CMP-01 timingSafeEqual clean", async () => {
		const result = await scanSource(
			"case-014.ts",
			'import { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-015 RNG-01 Math.random in generateToken", async () => {
		const result = await scanSource(
			"case-015.ts",
			"export function generateToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-016 RNG-01 Math.random in createSessionToken", async () => {
		const result = await scanSource(
			"case-016.ts",
			"export function createSessionToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-017 RNG-01 Math.random in issueAccessToken", async () => {
		const result = await scanSource(
			"case-017.ts",
			"export function issueAccessToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-018 RNG-01 Math.random in verifyPassword", async () => {
		const result = await scanSource(
			"case-018.ts",
			"export function verifyPassword() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-019 RNG-01 UI pickUiColor clean", async () => {
		const result = await scanSource(
			"case-019.ts",
			"export function pickUiColor() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-020 RNG-01 UI shuffleDeck clean", async () => {
		const result = await scanSource(
			"case-020.ts",
			"export function shuffleDeck() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-021 RNG-01 UI animateJitter clean", async () => {
		const result = await scanSource(
			"case-021.ts",
			"export function animateJitter() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-022 RNG-01 UI randomHue clean", async () => {
		const result = await scanSource(
			"case-022.ts",
			"export function randomHue() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-023 RNG-02 randomBytes(4) auth fn", async () => {
		const result = await scanSource(
			"case-023.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-024 RNG-02 randomBytes(8) auth fn", async () => {
		const result = await scanSource(
			"case-024.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-025 RNG-02 randomBytes(15) auth fn", async () => {
		const result = await scanSource(
			"case-025.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-026 RNG-02 randomBytes(16) auth fn", async () => {
		const result = await scanSource(
			"case-026.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-027 RNG-02 randomBytes(32) auth fn", async () => {
		const result = await scanSource(
			"case-027.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-028 RNG-02 randomBytes(64) auth fn", async () => {
		const result = await scanSource(
			"case-028.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-029 RNG-01+02 overlap auth fn", async () => {
		const result = await scanSource(
			"case-029.ts",
			"import { randomBytes } from \"crypto\";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString('hex') + n;\n}",
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-030 CMP-01 user.password compare", async () => {
		const result = await scanSource(
			"case-030.ts",
			'import crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-031 CMP-01 === token compare wrap0", async () => {
		const result = await scanSource(
			"case-031.ts",
			'"use strict";\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-032 CMP-01 === token compare wrap1", async () => {
		const result = await scanSource(
			"case-032.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-033 CMP-01 === token compare wrap2", async () => {
		const result = await scanSource(
			"case-033.ts",
			'// password hashing module\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-034 CMP-01 === token compare wrap3", async () => {
		const result = await scanSource(
			"case-034.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-035 CMP-01 === token compare wrap4", async () => {
		const result = await scanSource(
			"case-035.ts",
			'/* crypto */\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-036 CMP-01 == token compare wrap0", async () => {
		const result = await scanSource(
			"case-036.ts",
			'"use strict";\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-037 CMP-01 == token compare wrap1", async () => {
		const result = await scanSource(
			"case-037.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-038 CMP-01 == token compare wrap2", async () => {
		const result = await scanSource(
			"case-038.ts",
			'// password hashing module\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-039 CMP-01 == token compare wrap3", async () => {
		const result = await scanSource(
			"case-039.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-040 CMP-01 == token compare wrap4", async () => {
		const result = await scanSource(
			"case-040.ts",
			'/* crypto */\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-041 CMP-01 !== token compare wrap0", async () => {
		const result = await scanSource(
			"case-041.ts",
			'"use strict";\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-042 CMP-01 !== token compare wrap1", async () => {
		const result = await scanSource(
			"case-042.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-043 CMP-01 !== token compare wrap2", async () => {
		const result = await scanSource(
			"case-043.ts",
			'// password hashing module\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-044 CMP-01 !== token compare wrap3", async () => {
		const result = await scanSource(
			"case-044.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-045 CMP-01 !== token compare wrap4", async () => {
		const result = await scanSource(
			"case-045.ts",
			'/* crypto */\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-046 CMP-01 != token compare wrap0", async () => {
		const result = await scanSource(
			"case-046.ts",
			'"use strict";\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-047 CMP-01 != token compare wrap1", async () => {
		const result = await scanSource(
			"case-047.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-048 CMP-01 != token compare wrap2", async () => {
		const result = await scanSource(
			"case-048.ts",
			'// password hashing module\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-049 CMP-01 != token compare wrap3", async () => {
		const result = await scanSource(
			"case-049.ts",
			'import crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-050 CMP-01 != token compare wrap4", async () => {
		const result = await scanSource(
			"case-050.ts",
			'/* crypto */\nimport crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-051 CMP-01 === token compare wrap0", async () => {
		const result = await scanSource(
			"case-051.ts",
			'"use strict";\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-052 CMP-01 === token compare wrap1", async () => {
		const result = await scanSource(
			"case-052.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-053 CMP-01 === token compare wrap2", async () => {
		const result = await scanSource(
			"case-053.ts",
			'// password hashing module\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-054 CMP-01 === token compare wrap3", async () => {
		const result = await scanSource(
			"case-054.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-055 CMP-01 === token compare wrap4", async () => {
		const result = await scanSource(
			"case-055.ts",
			'/* crypto */\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-056 CMP-01 == token compare wrap0", async () => {
		const result = await scanSource(
			"case-056.ts",
			'"use strict";\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-057 CMP-01 == token compare wrap1", async () => {
		const result = await scanSource(
			"case-057.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-058 CMP-01 == token compare wrap2", async () => {
		const result = await scanSource(
			"case-058.ts",
			'// password hashing module\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-059 CMP-01 == token compare wrap3", async () => {
		const result = await scanSource(
			"case-059.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-060 CMP-01 == token compare wrap4", async () => {
		const result = await scanSource(
			"case-060.ts",
			'/* crypto */\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-061 CMP-01 !== token compare wrap0", async () => {
		const result = await scanSource(
			"case-061.ts",
			'"use strict";\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-062 CMP-01 !== token compare wrap1", async () => {
		const result = await scanSource(
			"case-062.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-063 CMP-01 !== token compare wrap2", async () => {
		const result = await scanSource(
			"case-063.ts",
			'// password hashing module\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-064 CMP-01 !== token compare wrap3", async () => {
		const result = await scanSource(
			"case-064.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-065 CMP-01 !== token compare wrap4", async () => {
		const result = await scanSource(
			"case-065.ts",
			'/* crypto */\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-066 CMP-01 != token compare wrap0", async () => {
		const result = await scanSource(
			"case-066.ts",
			'"use strict";\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-067 CMP-01 != token compare wrap1", async () => {
		const result = await scanSource(
			"case-067.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-068 CMP-01 != token compare wrap2", async () => {
		const result = await scanSource(
			"case-068.ts",
			'// password hashing module\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-069 CMP-01 != token compare wrap3", async () => {
		const result = await scanSource(
			"case-069.ts",
			'import * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-070 CMP-01 != token compare wrap4", async () => {
		const result = await scanSource(
			"case-070.ts",
			'/* crypto */\nimport * as crypto from "crypto";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-071 CMP-01 === token compare wrap0", async () => {
		const result = await scanSource(
			"case-071.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-072 CMP-01 === token compare wrap1", async () => {
		const result = await scanSource(
			"case-072.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-073 CMP-01 === token compare wrap2", async () => {
		const result = await scanSource(
			"case-073.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-074 CMP-01 === token compare wrap3", async () => {
		const result = await scanSource(
			"case-074.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-075 CMP-01 === token compare wrap4", async () => {
		const result = await scanSource(
			"case-075.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-076 CMP-01 == token compare wrap0", async () => {
		const result = await scanSource(
			"case-076.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-077 CMP-01 == token compare wrap1", async () => {
		const result = await scanSource(
			"case-077.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-078 CMP-01 == token compare wrap2", async () => {
		const result = await scanSource(
			"case-078.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-079 CMP-01 == token compare wrap3", async () => {
		const result = await scanSource(
			"case-079.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-080 CMP-01 == token compare wrap4", async () => {
		const result = await scanSource(
			"case-080.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token == expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-081 CMP-01 !== token compare wrap0", async () => {
		const result = await scanSource(
			"case-081.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-082 CMP-01 !== token compare wrap1", async () => {
		const result = await scanSource(
			"case-082.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-083 CMP-01 !== token compare wrap2", async () => {
		const result = await scanSource(
			"case-083.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-084 CMP-01 !== token compare wrap3", async () => {
		const result = await scanSource(
			"case-084.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-085 CMP-01 !== token compare wrap4", async () => {
		const result = await scanSource(
			"case-085.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token !== expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-086 CMP-01 != token compare wrap0", async () => {
		const result = await scanSource(
			"case-086.ts",
			'"use strict";\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-087 CMP-01 != token compare wrap1", async () => {
		const result = await scanSource(
			"case-087.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-088 CMP-01 != token compare wrap2", async () => {
		const result = await scanSource(
			"case-088.ts",
			'// password hashing module\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-089 CMP-01 != token compare wrap3", async () => {
		const result = await scanSource(
			"case-089.ts",
			'import bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-090 CMP-01 != token compare wrap4", async () => {
		const result = await scanSource(
			"case-090.ts",
			'/* crypto */\nimport bcrypt from "bcrypt";\nexport function check(token: string, expected: string) {\n  return token != expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-091 CMP-01 no crypto import clean wrap0", async () => {
		const result = await scanSource(
			"case-091.ts",
			'"use strict";\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-092 CMP-01 no crypto import clean wrap1", async () => {
		const result = await scanSource(
			"case-092.ts",
			"export function check(token: string, expected: string) {\n  return token === expected;\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-093 CMP-01 no crypto import clean wrap2", async () => {
		const result = await scanSource(
			"case-093.ts",
			"// password hashing module\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-094 CMP-01 no crypto import clean wrap3", async () => {
		const result = await scanSource(
			"case-094.ts",
			"export function check(token: string, expected: string) {\n  return token === expected;\n}\n\nexport const __module = true;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-095 CMP-01 no crypto import clean wrap4", async () => {
		const result = await scanSource(
			"case-095.ts",
			"/* crypto */\nexport function check(token: string, expected: string) {\n  return token === expected;\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-096 CMP-01 timingSafeEqual clean wrap0", async () => {
		const result = await scanSource(
			"case-096.ts",
			'"use strict";\nimport { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-097 CMP-01 timingSafeEqual clean wrap1", async () => {
		const result = await scanSource(
			"case-097.ts",
			'import { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-098 CMP-01 timingSafeEqual clean wrap2", async () => {
		const result = await scanSource(
			"case-098.ts",
			'// password hashing module\nimport { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-099 CMP-01 timingSafeEqual clean wrap3", async () => {
		const result = await scanSource(
			"case-099.ts",
			'import { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-100 CMP-01 timingSafeEqual clean wrap4", async () => {
		const result = await scanSource(
			"case-100.ts",
			'/* crypto */\nimport { timingSafeEqual } from "crypto";\nexport function check(a: Buffer, b: Buffer) {\n  return timingSafeEqual(a, b);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-101 RNG-01 Math.random in generateToken wrap0", async () => {
		const result = await scanSource(
			"case-101.ts",
			'"use strict";\nexport function generateToken() {\n  return Math.random().toString(36);\n}\n',
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-102 RNG-01 Math.random in generateToken wrap1", async () => {
		const result = await scanSource(
			"case-102.ts",
			"export function generateToken() {\n  return Math.random().toString(36);\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-103 RNG-01 Math.random in generateToken wrap2", async () => {
		const result = await scanSource(
			"case-103.ts",
			"// password hashing module\nexport function generateToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-104 RNG-01 Math.random in generateToken wrap3", async () => {
		const result = await scanSource(
			"case-104.ts",
			"export function generateToken() {\n  return Math.random().toString(36);\n}\n\nexport const __module = true;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-105 RNG-01 Math.random in generateToken wrap4", async () => {
		const result = await scanSource(
			"case-105.ts",
			"/* crypto */\nexport function generateToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-106 RNG-01 Math.random in createSessionToken wrap0", async () => {
		const result = await scanSource(
			"case-106.ts",
			'"use strict";\nexport function createSessionToken() {\n  return Math.random().toString(36);\n}\n',
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-107 RNG-01 Math.random in createSessionToken wrap1", async () => {
		const result = await scanSource(
			"case-107.ts",
			"export function createSessionToken() {\n  return Math.random().toString(36);\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-108 RNG-01 Math.random in createSessionToken wrap2", async () => {
		const result = await scanSource(
			"case-108.ts",
			"// password hashing module\nexport function createSessionToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-109 RNG-01 Math.random in createSessionToken wrap3", async () => {
		const result = await scanSource(
			"case-109.ts",
			"export function createSessionToken() {\n  return Math.random().toString(36);\n}\n\nexport const __module = true;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-110 RNG-01 Math.random in createSessionToken wrap4", async () => {
		const result = await scanSource(
			"case-110.ts",
			"/* crypto */\nexport function createSessionToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-111 RNG-01 Math.random in issueAccessToken wrap0", async () => {
		const result = await scanSource(
			"case-111.ts",
			'"use strict";\nexport function issueAccessToken() {\n  return Math.random().toString(36);\n}\n',
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-112 RNG-01 Math.random in issueAccessToken wrap1", async () => {
		const result = await scanSource(
			"case-112.ts",
			"export function issueAccessToken() {\n  return Math.random().toString(36);\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-113 RNG-01 Math.random in issueAccessToken wrap2", async () => {
		const result = await scanSource(
			"case-113.ts",
			"// password hashing module\nexport function issueAccessToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-114 RNG-01 Math.random in issueAccessToken wrap3", async () => {
		const result = await scanSource(
			"case-114.ts",
			"export function issueAccessToken() {\n  return Math.random().toString(36);\n}\n\nexport const __module = true;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-115 RNG-01 Math.random in issueAccessToken wrap4", async () => {
		const result = await scanSource(
			"case-115.ts",
			"/* crypto */\nexport function issueAccessToken() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-116 RNG-01 Math.random in verifyPassword wrap0", async () => {
		const result = await scanSource(
			"case-116.ts",
			'"use strict";\nexport function verifyPassword() {\n  return Math.random().toString(36);\n}\n',
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-117 RNG-01 Math.random in verifyPassword wrap1", async () => {
		const result = await scanSource(
			"case-117.ts",
			"export function verifyPassword() {\n  return Math.random().toString(36);\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-118 RNG-01 Math.random in verifyPassword wrap2", async () => {
		const result = await scanSource(
			"case-118.ts",
			"// password hashing module\nexport function verifyPassword() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-119 RNG-01 Math.random in verifyPassword wrap3", async () => {
		const result = await scanSource(
			"case-119.ts",
			"export function verifyPassword() {\n  return Math.random().toString(36);\n}\n\nexport const __module = true;\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-120 RNG-01 Math.random in verifyPassword wrap4", async () => {
		const result = await scanSource(
			"case-120.ts",
			"/* crypto */\nexport function verifyPassword() {\n  return Math.random().toString(36);\n}\n",
		);
		const expected = { "CS-RNG-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-121 RNG-01 UI pickUiColor clean wrap0", async () => {
		const result = await scanSource(
			"case-121.ts",
			'"use strict";\nexport function pickUiColor() {\n  return Math.random();\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-122 RNG-01 UI pickUiColor clean wrap1", async () => {
		const result = await scanSource(
			"case-122.ts",
			"export function pickUiColor() {\n  return Math.random();\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-123 RNG-01 UI pickUiColor clean wrap2", async () => {
		const result = await scanSource(
			"case-123.ts",
			"// password hashing module\nexport function pickUiColor() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-124 RNG-01 UI pickUiColor clean wrap3", async () => {
		const result = await scanSource(
			"case-124.ts",
			"export function pickUiColor() {\n  return Math.random();\n}\n\nexport const __module = true;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-125 RNG-01 UI pickUiColor clean wrap4", async () => {
		const result = await scanSource(
			"case-125.ts",
			"/* crypto */\nexport function pickUiColor() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-126 RNG-01 UI shuffleDeck clean wrap0", async () => {
		const result = await scanSource(
			"case-126.ts",
			'"use strict";\nexport function shuffleDeck() {\n  return Math.random();\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-127 RNG-01 UI shuffleDeck clean wrap1", async () => {
		const result = await scanSource(
			"case-127.ts",
			"export function shuffleDeck() {\n  return Math.random();\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-128 RNG-01 UI shuffleDeck clean wrap2", async () => {
		const result = await scanSource(
			"case-128.ts",
			"// password hashing module\nexport function shuffleDeck() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-129 RNG-01 UI shuffleDeck clean wrap3", async () => {
		const result = await scanSource(
			"case-129.ts",
			"export function shuffleDeck() {\n  return Math.random();\n}\n\nexport const __module = true;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-130 RNG-01 UI shuffleDeck clean wrap4", async () => {
		const result = await scanSource(
			"case-130.ts",
			"/* crypto */\nexport function shuffleDeck() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-131 RNG-01 UI animateJitter clean wrap0", async () => {
		const result = await scanSource(
			"case-131.ts",
			'"use strict";\nexport function animateJitter() {\n  return Math.random();\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-132 RNG-01 UI animateJitter clean wrap1", async () => {
		const result = await scanSource(
			"case-132.ts",
			"export function animateJitter() {\n  return Math.random();\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-133 RNG-01 UI animateJitter clean wrap2", async () => {
		const result = await scanSource(
			"case-133.ts",
			"// password hashing module\nexport function animateJitter() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-134 RNG-01 UI animateJitter clean wrap3", async () => {
		const result = await scanSource(
			"case-134.ts",
			"export function animateJitter() {\n  return Math.random();\n}\n\nexport const __module = true;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-135 RNG-01 UI animateJitter clean wrap4", async () => {
		const result = await scanSource(
			"case-135.ts",
			"/* crypto */\nexport function animateJitter() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-136 RNG-01 UI randomHue clean wrap0", async () => {
		const result = await scanSource(
			"case-136.ts",
			'"use strict";\nexport function randomHue() {\n  return Math.random();\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-137 RNG-01 UI randomHue clean wrap1", async () => {
		const result = await scanSource(
			"case-137.ts",
			"export function randomHue() {\n  return Math.random();\n}\n\nexport const VERSION = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-138 RNG-01 UI randomHue clean wrap2", async () => {
		const result = await scanSource(
			"case-138.ts",
			"// password hashing module\nexport function randomHue() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-139 RNG-01 UI randomHue clean wrap3", async () => {
		const result = await scanSource(
			"case-139.ts",
			"export function randomHue() {\n  return Math.random();\n}\n\nexport const __module = true;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-140 RNG-01 UI randomHue clean wrap4", async () => {
		const result = await scanSource(
			"case-140.ts",
			"/* crypto */\nexport function randomHue() {\n  return Math.random();\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-141 RNG-02 randomBytes(4) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-141.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-142 RNG-02 randomBytes(4) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-142.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-143 RNG-02 randomBytes(4) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-143.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-144 RNG-02 randomBytes(4) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-144.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-145 RNG-02 randomBytes(4) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-145.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(4).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-146 RNG-02 randomBytes(8) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-146.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-147 RNG-02 randomBytes(8) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-147.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-148 RNG-02 randomBytes(8) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-148.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-149 RNG-02 randomBytes(8) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-149.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-150 RNG-02 randomBytes(8) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-150.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(8).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-151 RNG-02 randomBytes(15) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-151.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-152 RNG-02 randomBytes(15) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-152.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-153 RNG-02 randomBytes(15) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-153.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-154 RNG-02 randomBytes(15) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-154.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-155 RNG-02 randomBytes(15) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-155.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(15).toString("hex");\n}',
		);
		const expected = { "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-156 RNG-02 randomBytes(16) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-156.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-157 RNG-02 randomBytes(16) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-157.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-158 RNG-02 randomBytes(16) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-158.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-159 RNG-02 randomBytes(16) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-159.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-160 RNG-02 randomBytes(16) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-160.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(16).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-161 RNG-02 randomBytes(32) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-161.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-162 RNG-02 randomBytes(32) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-162.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-163 RNG-02 randomBytes(32) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-163.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-164 RNG-02 randomBytes(32) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-164.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-165 RNG-02 randomBytes(32) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-165.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(32).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-166 RNG-02 randomBytes(64) auth fn wrap0", async () => {
		const result = await scanSource(
			"case-166.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-167 RNG-02 randomBytes(64) auth fn wrap1", async () => {
		const result = await scanSource(
			"case-167.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-168 RNG-02 randomBytes(64) auth fn wrap2", async () => {
		const result = await scanSource(
			"case-168.ts",
			'// password hashing module\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-169 RNG-02 randomBytes(64) auth fn wrap3", async () => {
		const result = await scanSource(
			"case-169.ts",
			'import { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-170 RNG-02 randomBytes(64) auth fn wrap4", async () => {
		const result = await scanSource(
			"case-170.ts",
			'/* crypto */\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  return randomBytes(64).toString("hex");\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-171 RNG-01+02 overlap auth fn wrap0", async () => {
		const result = await scanSource(
			"case-171.ts",
			'"use strict";\nimport { randomBytes } from "crypto";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString(\'hex\') + n;\n}',
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-172 RNG-01+02 overlap auth fn wrap1", async () => {
		const result = await scanSource(
			"case-172.ts",
			"import { randomBytes } from \"crypto\";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString('hex') + n;\n}\nexport const VERSION = 1;\n",
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-173 RNG-01+02 overlap auth fn wrap2", async () => {
		const result = await scanSource(
			"case-173.ts",
			"// password hashing module\nimport { randomBytes } from \"crypto\";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString('hex') + n;\n}",
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-174 RNG-01+02 overlap auth fn wrap3", async () => {
		const result = await scanSource(
			"case-174.ts",
			"import { randomBytes } from \"crypto\";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString('hex') + n;\n}\nexport const __module = true;\n",
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-175 RNG-01+02 overlap auth fn wrap4", async () => {
		const result = await scanSource(
			"case-175.ts",
			"/* crypto */\nimport { randomBytes } from \"crypto\";\nexport function generateSessionToken() {\n  const n = Math.random();\n  return randomBytes(8).toString('hex') + n;\n}",
		);
		const expected = { "CS-RNG-01": 1, "CS-RNG-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-176 CMP-01 user.password compare wrap0", async () => {
		const result = await scanSource(
			"case-176.ts",
			'"use strict";\nimport crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-177 CMP-01 user.password compare wrap1", async () => {
		const result = await scanSource(
			"case-177.ts",
			'import crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-178 CMP-01 user.password compare wrap2", async () => {
		const result = await scanSource(
			"case-178.ts",
			'// password hashing module\nimport crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-179 CMP-01 user.password compare wrap3", async () => {
		const result = await scanSource(
			"case-179.ts",
			'import crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-CMP-RNG-180 CMP-01 user.password compare wrap4", async () => {
		const result = await scanSource(
			"case-180.ts",
			'/* crypto */\nimport crypto from "crypto";\nexport function check(user: { password: string }, expected: string) {\n  return user.password === expected;\n}',
		);
		const expected = { "CS-CMP-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
