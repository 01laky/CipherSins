// Auto-generated by scripts/generate-v131-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/v131-scan-source.js";

describe("CS-V131 JWT exhaustive edge cases", () => {
	it("CS-V131-JWT-001 JWT-01 decode-only import jwt from 'jso", async () => {
		const result = await scanSource(
			"case-001.ts",
			'import jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-002 JWT-01 decode-only import * as jwt from", async () => {
		const result = await scanSource(
			"case-002.ts",
			'import * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-003 JWT-01 decode-only import { decode, ver", async () => {
		const result = await scanSource(
			"case-003.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-004 JWT-01 decode-only const jwt = require(", async () => {
		const result = await scanSource(
			"case-004.ts",
			'const jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-005 JWT-01 decode-only const jwt = require(", async () => {
		const result = await scanSource(
			"case-005.ts",
			'const jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-006 JWT-01 verify in inner fn suppresses decode", async () => {
		const result = await scanSource(
			"case-006.ts",
			"import jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-007 JWT-01 verify in outer only flags decode", async () => {
		const result = await scanSource(
			"case-007.ts",
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}",
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-008 JWT-01 direct helper verify suppresses", async () => {
		const result = await scanSource(
			"case-008.ts",
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-009 JWT-01 re-export verify suppresses decode", async () => {
		const result = await scanSource(
			"case-009.ts",
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-010 JWT-02 jwt.verify(t, s)", async () => {
		const result = await scanSource(
			"case-010.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-011 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-011.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-012 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-012.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-013 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-013.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-014 JWT-02 jwt.verify(t, s, { clockTolera", async () => {
		const result = await scanSource(
			"case-014.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-015 JWT-02 jwt.verify(t, s, cb)", async () => {
		const result = await scanSource(
			"case-015.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-016 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-016.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-017 JWT-02 jwt.verify(t, s)", async () => {
		const result = await scanSource(
			"case-017.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-018 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-018.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-019 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-019.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-020 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-020.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-021 JWT-02 jwt.verify(t, s, { clockTolera", async () => {
		const result = await scanSource(
			"case-021.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-022 JWT-02 jwt.verify(t, s, cb)", async () => {
		const result = await scanSource(
			"case-022.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-023 JWT-02 jwt.verify(t, s, { algorithms:", async () => {
		const result = await scanSource(
			"case-023.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-024 JWT-02 verify(t, s)", async () => {
		const result = await scanSource(
			"case-024.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-025 JWT-02 verify(t, s, { algorithms: ['H", async () => {
		const result = await scanSource(
			"case-025.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-026 JWT-02 verify(t, s, { algorithms: ['R", async () => {
		const result = await scanSource(
			"case-026.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-027 JWT-02 verify(t, s, { algorithms: ['H", async () => {
		const result = await scanSource(
			"case-027.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-028 JWT-02 verify(t, s, { clockTolerance:", async () => {
		const result = await scanSource(
			"case-028.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-029 JWT-02 verify(t, s, cb)", async () => {
		const result = await scanSource(
			"case-029.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-030 JWT-02 verify(t, s, { algorithms: ['H", async () => {
		const result = await scanSource(
			"case-030.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-031 JWT-03 jwt.verify(t, s, { algorithms: ['none'] ", async () => {
		const result = await scanSource(
			"case-031.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-032 JWT-03 jwt.verify(t, s, { algorithms: ['HS256',", async () => {
		const result = await scanSource(
			"case-032.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-033 JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: '", async () => {
		const result = await scanSource(
			"case-033.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		);
		const expected = { "CS-JWT-03": 1, "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-034 JWT-03 jwt.verify(t, s, { algorithms: ['HS256']", async () => {
		const result = await scanSource(
			"case-034.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-035 JWT-04 jwt.verify(t, s, { ignoreExpiration: tru", async () => {
		const result = await scanSource(
			"case-035.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		);
		const expected = { "CS-JWT-04": 1, "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-036 JWT-04 jwt.verify(t, s, { ignoreExpiration: fal", async () => {
		const result = await scanSource(
			"case-036.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-037 JWT-04 jwt.verify(t, s, { algorithms: ['HS256']", async () => {
		const result = await scanSource(
			"case-037.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n",
		);
		const expected = { "CS-JWT-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-038 JWT-05 jwt.sign({ sub: 'u' }, s)", async () => {
		const result = await scanSource(
			"case-038.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-039 JWT-05 jwt.sign({ sub: 'u' }, s, { expires", async () => {
		const result = await scanSource(
			"case-039.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-040 JWT-05 jwt.sign({ sub: 'u', exp: 999999999", async () => {
		const result = await scanSource(
			"case-040.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-041 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit", async () => {
		const result = await scanSource(
			"case-041.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-042 JWT-05 jwt.sign({ sub: 'u' }, s, cb)", async () => {
		const result = await scanSource(
			"case-042.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-043 JWT-05 jwt.sign({ sub: 'u' }, s)", async () => {
		const result = await scanSource(
			"case-043.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-044 JWT-05 jwt.sign({ sub: 'u' }, s, { expires", async () => {
		const result = await scanSource(
			"case-044.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-045 JWT-05 jwt.sign({ sub: 'u', exp: 999999999", async () => {
		const result = await scanSource(
			"case-045.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-046 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit", async () => {
		const result = await scanSource(
			"case-046.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-047 JWT-05 jwt.sign({ sub: 'u' }, s, cb)", async () => {
		const result = await scanSource(
			"case-047.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-048 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp:", async () => {
		const result = await scanSource(
			"case-048.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		);
		const expected = { "CS-JWT-05": 1, "CS-JWT-06": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-049 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp:", async () => {
		const result = await scanSource(
			"case-049.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-050 JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 }, ", async () => {
		const result = await scanSource(
			"case-050.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-051 JWT-01 destructured decode not tracked", async () => {
		const result = await scanSource(
			"case-051.ts",
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-052 JWT-01 verify-only clean", async () => {
		const result = await scanSource(
			"case-052.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-053 JWT-01 arrow decode", async () => {
		const result = await scanSource(
			"case-053.ts",
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-054 JWT-01 async decode", async () => {
		const result = await scanSource(
			"case-054.ts",
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-055 JWT-01 class method decode", async () => {
		const result = await scanSource(
			"case-055.ts",
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-056 JWT-01 IIFE decode", async () => {
		const result = await scanSource(
			"case-056.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-057 JWT-02 spread algorithms not explicit", async () => {
		const result = await scanSource(
			"case-057.ts",
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}",
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-058 JWT-02 empty algorithms array", async () => {
		const result = await scanSource(
			"case-058.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-059 JWT-05 two-arg sign secret variant", async () => {
		const result = await scanSource(
			"case-059.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-060 JWT-05 expiresIn clean variant", async () => {
		const result = await scanSource(
			"case-060.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-061 JWT-05 two-arg sign secret variant", async () => {
		const result = await scanSource(
			"case-061.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-062 JWT-05 expiresIn clean variant", async () => {
		const result = await scanSource(
			"case-062.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-063 grid decode authenticate", async () => {
		const result = await scanSource(
			"case-063.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-064 grid verify-no-alg authenticate", async () => {
		const result = await scanSource(
			"case-064.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-065 grid verify-alg authenticate", async () => {
		const result = await scanSource(
			"case-065.ts",
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-066 grid sign-no-exp authenticate", async () => {
		const result = await scanSource(
			"case-066.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-067 grid decode handleAuth", async () => {
		const result = await scanSource(
			"case-067.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-068 grid verify-no-alg handleAuth", async () => {
		const result = await scanSource(
			"case-068.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-069 grid verify-alg handleAuth", async () => {
		const result = await scanSource(
			"case-069.ts",
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-070 grid sign-no-exp handleAuth", async () => {
		const result = await scanSource(
			"case-070.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-071 grid decode verifyRequest", async () => {
		const result = await scanSource(
			"case-071.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-072 grid verify-no-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-072.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-073 grid verify-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-073.ts",
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-074 grid sign-no-exp verifyRequest", async () => {
		const result = await scanSource(
			"case-074.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-075 grid decode issueCredentials", async () => {
		const result = await scanSource(
			"case-075.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-076 grid verify-no-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-076.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-077 grid verify-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-077.ts",
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-078 grid sign-no-exp issueCredentials", async () => {
		const result = await scanSource(
			"case-078.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-079 grid decode parseBearer", async () => {
		const result = await scanSource(
			"case-079.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-080 grid verify-no-alg parseBearer", async () => {
		const result = await scanSource(
			"case-080.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-081 grid verify-alg parseBearer", async () => {
		const result = await scanSource(
			"case-081.ts",
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-082 grid sign-no-exp parseBearer", async () => {
		const result = await scanSource(
			"case-082.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-083 grid decode validateSession", async () => {
		const result = await scanSource(
			"case-083.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-084 grid verify-no-alg validateSession", async () => {
		const result = await scanSource(
			"case-084.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-085 grid verify-alg validateSession", async () => {
		const result = await scanSource(
			"case-085.ts",
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-086 grid sign-no-exp validateSession", async () => {
		const result = await scanSource(
			"case-086.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-087 grid decode loginUser", async () => {
		const result = await scanSource(
			"case-087.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-088 grid verify-no-alg loginUser", async () => {
		const result = await scanSource(
			"case-088.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-089 grid verify-alg loginUser", async () => {
		const result = await scanSource(
			"case-089.ts",
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-090 grid sign-no-exp loginUser", async () => {
		const result = await scanSource(
			"case-090.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-091 grid decode middleware", async () => {
		const result = await scanSource(
			"case-091.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-092 grid verify-no-alg middleware", async () => {
		const result = await scanSource(
			"case-092.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-093 grid verify-alg middleware", async () => {
		const result = await scanSource(
			"case-093.ts",
			"import jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-094 grid sign-no-exp middleware", async () => {
		const result = await scanSource(
			"case-094.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-095 grid decode authenticate", async () => {
		const result = await scanSource(
			"case-095.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-096 grid verify-no-alg authenticate", async () => {
		const result = await scanSource(
			"case-096.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-097 grid verify-alg authenticate", async () => {
		const result = await scanSource(
			"case-097.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-098 grid sign-no-exp authenticate", async () => {
		const result = await scanSource(
			"case-098.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-099 grid decode handleAuth", async () => {
		const result = await scanSource(
			"case-099.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-100 grid verify-no-alg handleAuth", async () => {
		const result = await scanSource(
			"case-100.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-101 grid verify-alg handleAuth", async () => {
		const result = await scanSource(
			"case-101.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-102 grid sign-no-exp handleAuth", async () => {
		const result = await scanSource(
			"case-102.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-103 grid decode verifyRequest", async () => {
		const result = await scanSource(
			"case-103.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-104 grid verify-no-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-104.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-105 grid verify-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-105.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-106 grid sign-no-exp verifyRequest", async () => {
		const result = await scanSource(
			"case-106.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-107 grid decode issueCredentials", async () => {
		const result = await scanSource(
			"case-107.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-108 grid verify-no-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-108.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-109 grid verify-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-109.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-110 grid sign-no-exp issueCredentials", async () => {
		const result = await scanSource(
			"case-110.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-111 grid decode parseBearer", async () => {
		const result = await scanSource(
			"case-111.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-112 grid verify-no-alg parseBearer", async () => {
		const result = await scanSource(
			"case-112.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-113 grid verify-alg parseBearer", async () => {
		const result = await scanSource(
			"case-113.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-114 grid sign-no-exp parseBearer", async () => {
		const result = await scanSource(
			"case-114.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-115 grid decode validateSession", async () => {
		const result = await scanSource(
			"case-115.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-116 grid verify-no-alg validateSession", async () => {
		const result = await scanSource(
			"case-116.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-117 grid verify-alg validateSession", async () => {
		const result = await scanSource(
			"case-117.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-118 grid sign-no-exp validateSession", async () => {
		const result = await scanSource(
			"case-118.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-119 grid decode loginUser", async () => {
		const result = await scanSource(
			"case-119.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-120 grid verify-no-alg loginUser", async () => {
		const result = await scanSource(
			"case-120.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-121 grid verify-alg loginUser", async () => {
		const result = await scanSource(
			"case-121.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-122 grid sign-no-exp loginUser", async () => {
		const result = await scanSource(
			"case-122.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-123 grid decode middleware", async () => {
		const result = await scanSource(
			"case-123.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-124 grid verify-no-alg middleware", async () => {
		const result = await scanSource(
			"case-124.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-125 grid verify-alg middleware", async () => {
		const result = await scanSource(
			"case-125.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-126 grid sign-no-exp middleware", async () => {
		const result = await scanSource(
			"case-126.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-127 grid decode authenticate", async () => {
		const result = await scanSource(
			"case-127.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-128 grid verify-no-alg authenticate", async () => {
		const result = await scanSource(
			"case-128.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-129 grid verify-alg authenticate", async () => {
		const result = await scanSource(
			"case-129.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-130 grid sign-no-exp authenticate", async () => {
		const result = await scanSource(
			"case-130.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-131 grid decode handleAuth", async () => {
		const result = await scanSource(
			"case-131.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-132 grid verify-no-alg handleAuth", async () => {
		const result = await scanSource(
			"case-132.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-133 grid verify-alg handleAuth", async () => {
		const result = await scanSource(
			"case-133.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-134 grid sign-no-exp handleAuth", async () => {
		const result = await scanSource(
			"case-134.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-135 grid decode verifyRequest", async () => {
		const result = await scanSource(
			"case-135.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-136 grid verify-no-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-136.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-137 grid verify-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-137.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-138 grid sign-no-exp verifyRequest", async () => {
		const result = await scanSource(
			"case-138.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-139 grid decode issueCredentials", async () => {
		const result = await scanSource(
			"case-139.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-140 grid verify-no-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-140.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-141 grid verify-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-141.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-142 grid sign-no-exp issueCredentials", async () => {
		const result = await scanSource(
			"case-142.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-143 grid decode parseBearer", async () => {
		const result = await scanSource(
			"case-143.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-144 grid verify-no-alg parseBearer", async () => {
		const result = await scanSource(
			"case-144.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-145 grid verify-alg parseBearer", async () => {
		const result = await scanSource(
			"case-145.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-146 grid sign-no-exp parseBearer", async () => {
		const result = await scanSource(
			"case-146.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-147 grid decode validateSession", async () => {
		const result = await scanSource(
			"case-147.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-148 grid verify-no-alg validateSession", async () => {
		const result = await scanSource(
			"case-148.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-149 grid verify-alg validateSession", async () => {
		const result = await scanSource(
			"case-149.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-150 grid sign-no-exp validateSession", async () => {
		const result = await scanSource(
			"case-150.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-151 grid decode loginUser", async () => {
		const result = await scanSource(
			"case-151.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-152 grid verify-no-alg loginUser", async () => {
		const result = await scanSource(
			"case-152.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-153 grid verify-alg loginUser", async () => {
		const result = await scanSource(
			"case-153.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-154 grid sign-no-exp loginUser", async () => {
		const result = await scanSource(
			"case-154.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-155 grid decode middleware", async () => {
		const result = await scanSource(
			"case-155.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-156 grid verify-no-alg middleware", async () => {
		const result = await scanSource(
			"case-156.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-157 grid verify-alg middleware", async () => {
		const result = await scanSource(
			"case-157.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-158 grid sign-no-exp middleware", async () => {
		const result = await scanSource(
			"case-158.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-159 grid decode authenticate", async () => {
		const result = await scanSource(
			"case-159.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-160 grid verify-no-alg authenticate", async () => {
		const result = await scanSource(
			"case-160.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-161 grid verify-alg authenticate", async () => {
		const result = await scanSource(
			"case-161.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-162 grid sign-no-exp authenticate", async () => {
		const result = await scanSource(
			"case-162.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-163 grid decode handleAuth", async () => {
		const result = await scanSource(
			"case-163.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-164 grid verify-no-alg handleAuth", async () => {
		const result = await scanSource(
			"case-164.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-165 grid verify-alg handleAuth", async () => {
		const result = await scanSource(
			"case-165.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-166 grid sign-no-exp handleAuth", async () => {
		const result = await scanSource(
			"case-166.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-167 grid decode verifyRequest", async () => {
		const result = await scanSource(
			"case-167.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-168 grid verify-no-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-168.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-169 grid verify-alg verifyRequest", async () => {
		const result = await scanSource(
			"case-169.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-170 grid sign-no-exp verifyRequest", async () => {
		const result = await scanSource(
			"case-170.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-171 grid decode issueCredentials", async () => {
		const result = await scanSource(
			"case-171.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-172 grid verify-no-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-172.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-173 grid verify-alg issueCredentials", async () => {
		const result = await scanSource(
			"case-173.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-174 grid sign-no-exp issueCredentials", async () => {
		const result = await scanSource(
			"case-174.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-175 grid decode parseBearer", async () => {
		const result = await scanSource(
			"case-175.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-176 grid verify-no-alg parseBearer", async () => {
		const result = await scanSource(
			"case-176.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-177 grid verify-alg parseBearer", async () => {
		const result = await scanSource(
			"case-177.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-178 grid sign-no-exp parseBearer", async () => {
		const result = await scanSource(
			"case-178.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-179 grid decode validateSession", async () => {
		const result = await scanSource(
			"case-179.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-180 grid verify-no-alg validateSession", async () => {
		const result = await scanSource(
			"case-180.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-181 grid verify-alg validateSession", async () => {
		const result = await scanSource(
			"case-181.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-182 grid sign-no-exp validateSession", async () => {
		const result = await scanSource(
			"case-182.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-183 grid decode loginUser", async () => {
		const result = await scanSource(
			"case-183.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-184 grid verify-no-alg loginUser", async () => {
		const result = await scanSource(
			"case-184.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-185 grid verify-alg loginUser", async () => {
		const result = await scanSource(
			"case-185.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-186 grid sign-no-exp loginUser", async () => {
		const result = await scanSource(
			"case-186.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-187 grid decode middleware", async () => {
		const result = await scanSource(
			"case-187.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-188 grid verify-no-alg middleware", async () => {
		const result = await scanSource(
			"case-188.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-189 grid verify-alg middleware", async () => {
		const result = await scanSource(
			"case-189.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-190 grid sign-no-exp middleware", async () => {
		const result = await scanSource(
			"case-190.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-191 JWT-01 decode-only import jwt from 'jso use strict", async () => {
		const result = await scanSource(
			"case-191.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-192 JWT-01 decode-only import jwt from 'jso module banner", async () => {
		const result = await scanSource(
			"case-192.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-193 JWT-01 decode-only import jwt from 'jso export const", async () => {
		const result = await scanSource(
			"case-193.ts",
			'import jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-194 JWT-01 decode-only import * as jwt from use strict", async () => {
		const result = await scanSource(
			"case-194.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-195 JWT-01 decode-only import * as jwt from module banner", async () => {
		const result = await scanSource(
			"case-195.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-196 JWT-01 decode-only import * as jwt from export const", async () => {
		const result = await scanSource(
			"case-196.ts",
			'import * as jwt from "jsonwebtoken";\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-197 JWT-01 decode-only import { decode, ver use strict", async () => {
		const result = await scanSource(
			"case-197.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-198 JWT-01 decode-only import { decode, ver module banner", async () => {
		const result = await scanSource(
			"case-198.ts",
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-199 JWT-01 decode-only import { decode, ver export const", async () => {
		const result = await scanSource(
			"case-199.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function auth(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-200 JWT-01 decode-only const jwt = require( use strict", async () => {
		const result = await scanSource(
			"case-200.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-201 JWT-01 decode-only const jwt = require( module banner", async () => {
		const result = await scanSource(
			"case-201.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-202 JWT-01 decode-only const jwt = require( export const", async () => {
		const result = await scanSource(
			"case-202.ts",
			'const jwt = require("jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-203 JWT-01 decode-only const jwt = require( use strict", async () => {
		const result = await scanSource(
			"case-203.ts",
			'"use strict";\nconst jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-204 JWT-01 decode-only const jwt = require( module banner", async () => {
		const result = await scanSource(
			"case-204.ts",
			'/** module */\nconst jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-205 JWT-01 decode-only const jwt = require( export const", async () => {
		const result = await scanSource(
			"case-205.ts",
			'const jwt = require("node:jsonwebtoken");\nexport function auth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-206 JWT-01 verify in inner fn suppresses decode use strict", async () => {
		const result = await scanSource(
			"case-206.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: [\'HS256\'] }); }\n  return jwt.decode(t);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-207 JWT-01 verify in inner fn suppresses decode module banner", async () => {
		const result = await scanSource(
			"case-207.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-208 JWT-01 verify in inner fn suppresses decode export const", async () => {
		const result = await scanSource(
			"case-208.ts",
			"import jwt from \"jsonwebtoken\";\nexport function auth(t: string, s: string) {\n  function inner() { jwt.verify(t, s, { algorithms: ['HS256'] }); }\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-209 JWT-01 verify in outer only flags decode use strict", async () => {
		const result = await scanSource(
			"case-209.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return inner(t);\n}',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-210 JWT-01 verify in outer only flags decode module banner", async () => {
		const result = await scanSource(
			"case-210.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}",
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-211 JWT-01 verify in outer only flags decode export const", async () => {
		const result = await scanSource(
			"case-211.ts",
			"import jwt from \"jsonwebtoken\";\nfunction inner(t: string) { return jwt.decode(t); }\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n  return inner(t);\n}\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-212 JWT-01 direct helper verify suppresses use strict", async () => {
		const result = await scanSource(
			"case-212.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-213 JWT-01 direct helper verify suppresses module banner", async () => {
		const result = await scanSource(
			"case-213.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-214 JWT-01 direct helper verify suppresses export const", async () => {
		const result = await scanSource(
			"case-214.ts",
			"import jwt from \"jsonwebtoken\";\nfunction verifyToken(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: ['HS256'] });\n}\nexport function auth(t: string, s: string) {\n  verifyToken(t, s);\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-215 JWT-01 re-export verify suppresses decode use strict", async () => {
		const result = await scanSource(
			"case-215.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-216 JWT-01 re-export verify suppresses decode module banner", async () => {
		const result = await scanSource(
			"case-216.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-217 JWT-01 re-export verify suppresses decode export const", async () => {
		const result = await scanSource(
			"case-217.ts",
			'import jwt from "jsonwebtoken";\nexport { verify } from "jsonwebtoken";\nexport function auth(t: string, s: string) {\n  jwt.verify(t, s, { algorithms: [\'HS256\'] });\n  return jwt.decode(t);\n}\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-218 JWT-02 jwt.verify(t, s) use strict", async () => {
		const result = await scanSource(
			"case-218.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-219 JWT-02 jwt.verify(t, s) module banner", async () => {
		const result = await scanSource(
			"case-219.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-220 JWT-02 jwt.verify(t, s) export const", async () => {
		const result = await scanSource(
			"case-220.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-221 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-221.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-222 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-222.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-223 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-223.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-224 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-224.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-225 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-225.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-226 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-226.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-227 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-227.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-228 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-228.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-229 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-229.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-230 JWT-02 jwt.verify(t, s, { clockTolera use strict", async () => {
		const result = await scanSource(
			"case-230.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-231 JWT-02 jwt.verify(t, s, { clockTolera module banner", async () => {
		const result = await scanSource(
			"case-231.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-232 JWT-02 jwt.verify(t, s, { clockTolera export const", async () => {
		const result = await scanSource(
			"case-232.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-233 JWT-02 jwt.verify(t, s, cb) use strict", async () => {
		const result = await scanSource(
			"case-233.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-234 JWT-02 jwt.verify(t, s, cb) module banner", async () => {
		const result = await scanSource(
			"case-234.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-235 JWT-02 jwt.verify(t, s, cb) export const", async () => {
		const result = await scanSource(
			"case-235.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-236 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-236.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-237 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-237.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-238 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-238.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-239 JWT-02 jwt.verify(t, s) use strict", async () => {
		const result = await scanSource(
			"case-239.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-240 JWT-02 jwt.verify(t, s) module banner", async () => {
		const result = await scanSource(
			"case-240.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-241 JWT-02 jwt.verify(t, s) export const", async () => {
		const result = await scanSource(
			"case-241.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-242 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-242.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-243 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-243.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-244 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-244.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-245 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-245.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-246 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-246.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-247 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-247.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-248 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-248.ts",
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-249 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-249.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-250 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-250.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-251 JWT-02 jwt.verify(t, s, { clockTolera use strict", async () => {
		const result = await scanSource(
			"case-251.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-252 JWT-02 jwt.verify(t, s, { clockTolera module banner", async () => {
		const result = await scanSource(
			"case-252.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-253 JWT-02 jwt.verify(t, s, { clockTolera export const", async () => {
		const result = await scanSource(
			"case-253.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-254 JWT-02 jwt.verify(t, s, cb) use strict", async () => {
		const result = await scanSource(
			"case-254.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-255 JWT-02 jwt.verify(t, s, cb) module banner", async () => {
		const result = await scanSource(
			"case-255.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-256 JWT-02 jwt.verify(t, s, cb) export const", async () => {
		const result = await scanSource(
			"case-256.ts",
			'import * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-257 JWT-02 jwt.verify(t, s, { algorithms: use strict", async () => {
		const result = await scanSource(
			"case-257.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-258 JWT-02 jwt.verify(t, s, { algorithms: module banner", async () => {
		const result = await scanSource(
			"case-258.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-259 JWT-02 jwt.verify(t, s, { algorithms: export const", async () => {
		const result = await scanSource(
			"case-259.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-260 JWT-02 verify(t, s) use strict", async () => {
		const result = await scanSource(
			"case-260.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-261 JWT-02 verify(t, s) module banner", async () => {
		const result = await scanSource(
			"case-261.ts",
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-262 JWT-02 verify(t, s) export const", async () => {
		const result = await scanSource(
			"case-262.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-263 JWT-02 verify(t, s, { algorithms: ['H use strict", async () => {
		const result = await scanSource(
			"case-263.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-264 JWT-02 verify(t, s, { algorithms: ['H module banner", async () => {
		const result = await scanSource(
			"case-264.ts",
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-265 JWT-02 verify(t, s, { algorithms: ['H export const", async () => {
		const result = await scanSource(
			"case-265.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-266 JWT-02 verify(t, s, { algorithms: ['R use strict", async () => {
		const result = await scanSource(
			"case-266.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'RS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-267 JWT-02 verify(t, s, { algorithms: ['R module banner", async () => {
		const result = await scanSource(
			"case-267.ts",
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-268 JWT-02 verify(t, s, { algorithms: ['R export const", async () => {
		const result = await scanSource(
			"case-268.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-269 JWT-02 verify(t, s, { algorithms: ['H use strict", async () => {
		const result = await scanSource(
			"case-269.ts",
			"\"use strict\";\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-270 JWT-02 verify(t, s, { algorithms: ['H module banner", async () => {
		const result = await scanSource(
			"case-270.ts",
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-271 JWT-02 verify(t, s, { algorithms: ['H export const", async () => {
		const result = await scanSource(
			"case-271.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256', 'RS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-272 JWT-02 verify(t, s, { clockTolerance: use strict", async () => {
		const result = await scanSource(
			"case-272.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-273 JWT-02 verify(t, s, { clockTolerance: module banner", async () => {
		const result = await scanSource(
			"case-273.ts",
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-274 JWT-02 verify(t, s, { clockTolerance: export const", async () => {
		const result = await scanSource(
			"case-274.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { clockTolerance: 5 });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-275 JWT-02 verify(t, s, cb) use strict", async () => {
		const result = await scanSource(
			"case-275.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-276 JWT-02 verify(t, s, cb) module banner", async () => {
		const result = await scanSource(
			"case-276.ts",
			'/** module */\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-277 JWT-02 verify(t, s, cb) export const", async () => {
		const result = await scanSource(
			"case-277.ts",
			'import { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, cb);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-278 JWT-02 verify(t, s, { algorithms: ['H use strict", async () => {
		const result = await scanSource(
			"case-278.ts",
			'"use strict";\nimport { decode, verify } from "jsonwebtoken";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: [\'HS256\'] }, cb);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-279 JWT-02 verify(t, s, { algorithms: ['H module banner", async () => {
		const result = await scanSource(
			"case-279.ts",
			"/** module */\nimport { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-280 JWT-02 verify(t, s, { algorithms: ['H export const", async () => {
		const result = await scanSource(
			"case-280.ts",
			"import { decode, verify } from \"jsonwebtoken\";\nexport function f(t: string, s: string, cb?: () => void) {\n  return verify(t, s, { algorithms: ['HS256'] }, cb);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-281 JWT-03 jwt.verify(t, s, { algorithms: ['none']  use strict", async () => {
		const result = await scanSource(
			"case-281.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'none\'] });\n}\n',
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-282 JWT-03 jwt.verify(t, s, { algorithms: ['none']  module banner", async () => {
		const result = await scanSource(
			"case-282.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-283 JWT-03 jwt.verify(t, s, { algorithms: ['none']  export const", async () => {
		const result = await scanSource(
			"case-283.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['none'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-284 JWT-03 jwt.verify(t, s, { algorithms: ['HS256', use strict", async () => {
		const result = await scanSource(
			"case-284.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-285 JWT-03 jwt.verify(t, s, { algorithms: ['HS256', module banner", async () => {
		const result = await scanSource(
			"case-285.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-286 JWT-03 jwt.verify(t, s, { algorithms: ['HS256', export const", async () => {
		const result = await scanSource(
			"case-286.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256', 'none'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-287 JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' use strict", async () => {
		const result = await scanSource(
			"case-287.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		);
		const expected = { "CS-JWT-03": 1, "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-288 JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' module banner", async () => {
		const result = await scanSource(
			"case-288.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n",
		);
		const expected = { "CS-JWT-03": 1, "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-289 JWT-03 jwt.sign({ sub: 'u' }, s, { algorithm: ' export const", async () => {
		const result = await scanSource(
			"case-289.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'none' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-03": 1, "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-290 JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] use strict", async () => {
		const result = await scanSource(
			"case-290.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-291 JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] module banner", async () => {
		const result = await scanSource(
			"case-291.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-292 JWT-03 jwt.verify(t, s, { algorithms: ['HS256'] export const", async () => {
		const result = await scanSource(
			"case-292.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-293 JWT-04 jwt.verify(t, s, { ignoreExpiration: tru use strict", async () => {
		const result = await scanSource(
			"case-293.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		);
		const expected = { "CS-JWT-04": 1, "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-294 JWT-04 jwt.verify(t, s, { ignoreExpiration: tru module banner", async () => {
		const result = await scanSource(
			"case-294.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n',
		);
		const expected = { "CS-JWT-04": 1, "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-295 JWT-04 jwt.verify(t, s, { ignoreExpiration: tru export const", async () => {
		const result = await scanSource(
			"case-295.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-04": 1, "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-296 JWT-04 jwt.verify(t, s, { ignoreExpiration: fal use strict", async () => {
		const result = await scanSource(
			"case-296.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-297 JWT-04 jwt.verify(t, s, { ignoreExpiration: fal module banner", async () => {
		const result = await scanSource(
			"case-297.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-298 JWT-04 jwt.verify(t, s, { ignoreExpiration: fal export const", async () => {
		const result = await scanSource(
			"case-298.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ignoreExpiration: false, algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-299 JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] use strict", async () => {
		const result = await scanSource(
			"case-299.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'], ignoreExpiration: true });\n}\n',
		);
		const expected = { "CS-JWT-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-300 JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] module banner", async () => {
		const result = await scanSource(
			"case-300.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n",
		);
		const expected = { "CS-JWT-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-301 JWT-04 jwt.verify(t, s, { algorithms: ['HS256'] export const", async () => {
		const result = await scanSource(
			"case-301.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'], ignoreExpiration: true });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-302 JWT-05 jwt.sign({ sub: 'u' }, s) use strict", async () => {
		const result = await scanSource(
			"case-302.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-303 JWT-05 jwt.sign({ sub: 'u' }, s) module banner", async () => {
		const result = await scanSource(
			"case-303.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-304 JWT-05 jwt.sign({ sub: 'u' }, s) export const", async () => {
		const result = await scanSource(
			"case-304.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-305 JWT-05 jwt.sign({ sub: 'u' }, s, { expires use strict", async () => {
		const result = await scanSource(
			"case-305.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-306 JWT-05 jwt.sign({ sub: 'u' }, s, { expires module banner", async () => {
		const result = await scanSource(
			"case-306.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-307 JWT-05 jwt.sign({ sub: 'u' }, s, { expires export const", async () => {
		const result = await scanSource(
			"case-307.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-308 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 use strict", async () => {
		const result = await scanSource(
			"case-308.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-309 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 module banner", async () => {
		const result = await scanSource(
			"case-309.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-310 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 export const", async () => {
		const result = await scanSource(
			"case-310.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-311 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit use strict", async () => {
		const result = await scanSource(
			"case-311.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-312 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit module banner", async () => {
		const result = await scanSource(
			"case-312.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-313 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit export const", async () => {
		const result = await scanSource(
			"case-313.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-314 JWT-05 jwt.sign({ sub: 'u' }, s, cb) use strict", async () => {
		const result = await scanSource(
			"case-314.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s, cb);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-315 JWT-05 jwt.sign({ sub: 'u' }, s, cb) module banner", async () => {
		const result = await scanSource(
			"case-315.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-316 JWT-05 jwt.sign({ sub: 'u' }, s, cb) export const", async () => {
		const result = await scanSource(
			"case-316.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-317 JWT-05 jwt.sign({ sub: 'u' }, s) use strict", async () => {
		const result = await scanSource(
			"case-317.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-318 JWT-05 jwt.sign({ sub: 'u' }, s) module banner", async () => {
		const result = await scanSource(
			"case-318.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-319 JWT-05 jwt.sign({ sub: 'u' }, s) export const", async () => {
		const result = await scanSource(
			"case-319.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-320 JWT-05 jwt.sign({ sub: 'u' }, s, { expires use strict", async () => {
		const result = await scanSource(
			"case-320.ts",
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-321 JWT-05 jwt.sign({ sub: 'u' }, s, { expires module banner", async () => {
		const result = await scanSource(
			"case-321.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-322 JWT-05 jwt.sign({ sub: 'u' }, s, { expires export const", async () => {
		const result = await scanSource(
			"case-322.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-323 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 use strict", async () => {
		const result = await scanSource(
			"case-323.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s);\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-324 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 module banner", async () => {
		const result = await scanSource(
			"case-324.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-325 JWT-05 jwt.sign({ sub: 'u', exp: 999999999 export const", async () => {
		const result = await scanSource(
			"case-325.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-326 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit use strict", async () => {
		const result = await scanSource(
			"case-326.ts",
			"\"use strict\";\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-327 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit module banner", async () => {
		const result = await scanSource(
			"case-327.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-328 JWT-05 jwt.sign({ sub: 'u' }, s, { algorit export const", async () => {
		const result = await scanSource(
			"case-328.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, { algorithm: 'HS256' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-329 JWT-05 jwt.sign({ sub: 'u' }, s, cb) use strict", async () => {
		const result = await scanSource(
			"case-329.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: \'u\' }, s, cb);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-330 JWT-05 jwt.sign({ sub: 'u' }, s, cb) module banner", async () => {
		const result = await scanSource(
			"case-330.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-331 JWT-05 jwt.sign({ sub: 'u' }, s, cb) export const", async () => {
		const result = await scanSource(
			"case-331.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function f(s: string, cb?: () => void) {\n  return jwt.sign({ sub: 'u' }, s, cb);\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-332 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: use strict", async () => {
		const result = await scanSource(
			"case-332.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string) {\n  return jwt.sign({ sub: \'u\' }, s, { noTimestamp: true });\n}\n',
		);
		const expected = { "CS-JWT-05": 1, "CS-JWT-06": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-333 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: module banner", async () => {
		const result = await scanSource(
			"case-333.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n",
		);
		const expected = { "CS-JWT-05": 1, "CS-JWT-06": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-334 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: export const", async () => {
		const result = await scanSource(
			"case-334.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-05": 1, "CS-JWT-06": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-335 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: use strict", async () => {
		const result = await scanSource(
			"case-335.ts",
			"\"use strict\";\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-336 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: module banner", async () => {
		const result = await scanSource(
			"case-336.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-337 JWT-06 jwt.sign({ sub: 'u' }, s, { noTimestamp: export const", async () => {
		const result = await scanSource(
			"case-337.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u' }, s, { noTimestamp: true, expiresIn: '1h' });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-338 JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  use strict", async () => {
		const result = await scanSource(
			"case-338.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(s: string) {\n  return jwt.sign({ sub: \'u\', exp: 9999999999 }, s, { noTimestamp: true });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-339 JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  module banner", async () => {
		const result = await scanSource(
			"case-339.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-340 JWT-06 jwt.sign({ sub: 'u', exp: 9999999999 },  export const", async () => {
		const result = await scanSource(
			"case-340.ts",
			"import jwt from \"jsonwebtoken\";\nexport function f(s: string) {\n  return jwt.sign({ sub: 'u', exp: 9999999999 }, s, { noTimestamp: true });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-341 JWT-01 destructured decode not tracked use strict", async () => {
		const result = await scanSource(
			"case-341.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-342 JWT-01 destructured decode not tracked module banner", async () => {
		const result = await scanSource(
			"case-342.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-343 JWT-01 destructured decode not tracked export const", async () => {
		const result = await scanSource(
			"case-343.ts",
			'import jwt from "jsonwebtoken";\nconst { decode } = jwt;\nexport function f(t: string) { return decode(t); }\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-344 JWT-01 verify-only clean use strict", async () => {
		const result = await scanSource(
			"case-344.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-345 JWT-01 verify-only clean module banner", async () => {
		const result = await scanSource(
			"case-345.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-346 JWT-01 verify-only clean export const", async () => {
		const result = await scanSource(
			"case-346.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ["HS256"] });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-347 JWT-01 arrow decode use strict", async () => {
		const result = await scanSource(
			"case-347.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-348 JWT-01 arrow decode module banner", async () => {
		const result = await scanSource(
			"case-348.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-349 JWT-01 arrow decode export const", async () => {
		const result = await scanSource(
			"case-349.ts",
			'import jwt from "jsonwebtoken";\nexport const f = (t: string) => jwt.decode(t);\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-350 JWT-01 async decode use strict", async () => {
		const result = await scanSource(
			"case-350.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-351 JWT-01 async decode module banner", async () => {
		const result = await scanSource(
			"case-351.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-352 JWT-01 async decode export const", async () => {
		const result = await scanSource(
			"case-352.ts",
			'import jwt from "jsonwebtoken";\nexport async function f(t: string) { return jwt.decode(t); }\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-353 JWT-01 class method decode use strict", async () => {
		const result = await scanSource(
			"case-353.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-354 JWT-01 class method decode module banner", async () => {
		const result = await scanSource(
			"case-354.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-355 JWT-01 class method decode export const", async () => {
		const result = await scanSource(
			"case-355.ts",
			'import jwt from "jsonwebtoken";\nexport class S { m(t: string) { return jwt.decode(t); } }\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-356 JWT-01 IIFE decode use strict", async () => {
		const result = await scanSource(
			"case-356.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-357 JWT-01 IIFE decode module banner", async () => {
		const result = await scanSource(
			"case-357.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-358 JWT-01 IIFE decode export const", async () => {
		const result = await scanSource(
			"case-358.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string) { return (() => jwt.decode(t))(); }\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-359 JWT-02 spread algorithms not explicit use strict", async () => {
		const result = await scanSource(
			"case-359.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nconst opts = { algorithms: [\'HS256\'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-360 JWT-02 spread algorithms not explicit module banner", async () => {
		const result = await scanSource(
			"case-360.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}",
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-361 JWT-02 spread algorithms not explicit export const", async () => {
		const result = await scanSource(
			"case-361.ts",
			"import jwt from \"jsonwebtoken\";\nconst opts = { algorithms: ['HS256'] as const };\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { ...opts });\n}\nexport const MODULE = 1;\n",
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-362 JWT-02 empty algorithms array use strict", async () => {
		const result = await scanSource(
			"case-362.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-363 JWT-02 empty algorithms array module banner", async () => {
		const result = await scanSource(
			"case-363.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-364 JWT-02 empty algorithms array export const", async () => {
		const result = await scanSource(
			"case-364.ts",
			'import jwt from "jsonwebtoken";\nexport function f(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [] });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-365 JWT-05 two-arg sign secret variant use strict", async () => {
		const result = await scanSource(
			"case-365.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-366 JWT-05 two-arg sign secret variant module banner", async () => {
		const result = await scanSource(
			"case-366.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-367 JWT-05 two-arg sign secret variant export const", async () => {
		const result = await scanSource(
			"case-367.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-368 JWT-05 expiresIn clean variant use strict", async () => {
		const result = await scanSource(
			"case-368.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-369 JWT-05 expiresIn clean variant module banner", async () => {
		const result = await scanSource(
			"case-369.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-370 JWT-05 expiresIn clean variant export const", async () => {
		const result = await scanSource(
			"case-370.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = s;\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-371 JWT-05 two-arg sign secret variant use strict", async () => {
		const result = await scanSource(
			"case-371.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-372 JWT-05 two-arg sign secret variant module banner", async () => {
		const result = await scanSource(
			"case-372.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-373 JWT-05 two-arg sign secret variant export const", async () => {
		const result = await scanSource(
			"case-373.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-374 JWT-05 expiresIn clean variant use strict", async () => {
		const result = await scanSource(
			"case-374.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-375 JWT-05 expiresIn clean variant module banner", async () => {
		const result = await scanSource(
			"case-375.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-376 JWT-05 expiresIn clean variant export const", async () => {
		const result = await scanSource(
			"case-376.ts",
			'import jwt from "jsonwebtoken";\nexport function issue(p: object) {\n  const secret = process.env.JWT_SECRET ?? "dev";\n  return jwt.sign(p, secret, { expiresIn: "7d" });\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-377 grid decode authenticate use strict", async () => {
		const result = await scanSource(
			"case-377.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-378 grid decode authenticate module banner", async () => {
		const result = await scanSource(
			"case-378.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-379 grid decode authenticate export const", async () => {
		const result = await scanSource(
			"case-379.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-380 grid verify-no-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-380.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-381 grid verify-no-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-381.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-382 grid verify-no-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-382.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-383 grid verify-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-383.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-384 grid verify-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-384.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-385 grid verify-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-385.ts",
			"import jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-386 grid sign-no-exp authenticate use strict", async () => {
		const result = await scanSource(
			"case-386.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-387 grid sign-no-exp authenticate module banner", async () => {
		const result = await scanSource(
			"case-387.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-388 grid sign-no-exp authenticate export const", async () => {
		const result = await scanSource(
			"case-388.ts",
			'import jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-389 grid decode handleAuth use strict", async () => {
		const result = await scanSource(
			"case-389.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-390 grid decode handleAuth module banner", async () => {
		const result = await scanSource(
			"case-390.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-391 grid decode handleAuth export const", async () => {
		const result = await scanSource(
			"case-391.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-392 grid verify-no-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-392.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-393 grid verify-no-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-393.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-394 grid verify-no-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-394.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-395 grid verify-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-395.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-396 grid verify-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-396.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-397 grid verify-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-397.ts",
			"import jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-398 grid sign-no-exp handleAuth use strict", async () => {
		const result = await scanSource(
			"case-398.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-399 grid sign-no-exp handleAuth module banner", async () => {
		const result = await scanSource(
			"case-399.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-400 grid sign-no-exp handleAuth export const", async () => {
		const result = await scanSource(
			"case-400.ts",
			'import jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-401 grid decode verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-401.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-402 grid decode verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-402.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-403 grid decode verifyRequest export const", async () => {
		const result = await scanSource(
			"case-403.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-404 grid verify-no-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-404.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-405 grid verify-no-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-405.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-406 grid verify-no-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-406.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-407 grid verify-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-407.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-408 grid verify-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-408.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-409 grid verify-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-409.ts",
			"import jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-410 grid sign-no-exp verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-410.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-411 grid sign-no-exp verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-411.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-412 grid sign-no-exp verifyRequest export const", async () => {
		const result = await scanSource(
			"case-412.ts",
			'import jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-413 grid decode issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-413.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-414 grid decode issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-414.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-415 grid decode issueCredentials export const", async () => {
		const result = await scanSource(
			"case-415.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-416 grid verify-no-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-416.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-417 grid verify-no-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-417.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-418 grid verify-no-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-418.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-419 grid verify-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-419.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-420 grid verify-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-420.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-421 grid verify-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-421.ts",
			"import jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-422 grid sign-no-exp issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-422.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-423 grid sign-no-exp issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-423.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-424 grid sign-no-exp issueCredentials export const", async () => {
		const result = await scanSource(
			"case-424.ts",
			'import jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-425 grid decode parseBearer use strict", async () => {
		const result = await scanSource(
			"case-425.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-426 grid decode parseBearer module banner", async () => {
		const result = await scanSource(
			"case-426.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-427 grid decode parseBearer export const", async () => {
		const result = await scanSource(
			"case-427.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-428 grid verify-no-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-428.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-429 grid verify-no-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-429.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-430 grid verify-no-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-430.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-431 grid verify-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-431.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-432 grid verify-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-432.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-433 grid verify-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-433.ts",
			"import jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-434 grid sign-no-exp parseBearer use strict", async () => {
		const result = await scanSource(
			"case-434.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-435 grid sign-no-exp parseBearer module banner", async () => {
		const result = await scanSource(
			"case-435.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-436 grid sign-no-exp parseBearer export const", async () => {
		const result = await scanSource(
			"case-436.ts",
			'import jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-437 grid decode validateSession use strict", async () => {
		const result = await scanSource(
			"case-437.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-438 grid decode validateSession module banner", async () => {
		const result = await scanSource(
			"case-438.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-439 grid decode validateSession export const", async () => {
		const result = await scanSource(
			"case-439.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-440 grid verify-no-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-440.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-441 grid verify-no-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-441.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-442 grid verify-no-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-442.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-443 grid verify-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-443.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-444 grid verify-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-444.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-445 grid verify-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-445.ts",
			"import jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-446 grid sign-no-exp validateSession use strict", async () => {
		const result = await scanSource(
			"case-446.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-447 grid sign-no-exp validateSession module banner", async () => {
		const result = await scanSource(
			"case-447.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-448 grid sign-no-exp validateSession export const", async () => {
		const result = await scanSource(
			"case-448.ts",
			'import jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-449 grid decode loginUser use strict", async () => {
		const result = await scanSource(
			"case-449.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-450 grid decode loginUser module banner", async () => {
		const result = await scanSource(
			"case-450.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-451 grid decode loginUser export const", async () => {
		const result = await scanSource(
			"case-451.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-452 grid verify-no-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-452.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-453 grid verify-no-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-453.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-454 grid verify-no-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-454.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-455 grid verify-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-455.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-456 grid verify-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-456.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-457 grid verify-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-457.ts",
			"import jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-458 grid sign-no-exp loginUser use strict", async () => {
		const result = await scanSource(
			"case-458.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-459 grid sign-no-exp loginUser module banner", async () => {
		const result = await scanSource(
			"case-459.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-460 grid sign-no-exp loginUser export const", async () => {
		const result = await scanSource(
			"case-460.ts",
			'import jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-461 grid decode middleware use strict", async () => {
		const result = await scanSource(
			"case-461.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-462 grid decode middleware module banner", async () => {
		const result = await scanSource(
			"case-462.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-463 grid decode middleware export const", async () => {
		const result = await scanSource(
			"case-463.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-464 grid verify-no-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-464.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-465 grid verify-no-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-465.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-466 grid verify-no-alg middleware export const", async () => {
		const result = await scanSource(
			"case-466.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-467 grid verify-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-467.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-468 grid verify-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-468.ts",
			"/** module */\nimport jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-469 grid verify-alg middleware export const", async () => {
		const result = await scanSource(
			"case-469.ts",
			"import jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-470 grid sign-no-exp middleware use strict", async () => {
		const result = await scanSource(
			"case-470.ts",
			'"use strict";\nimport jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-471 grid sign-no-exp middleware module banner", async () => {
		const result = await scanSource(
			"case-471.ts",
			'/** module */\nimport jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-472 grid sign-no-exp middleware export const", async () => {
		const result = await scanSource(
			"case-472.ts",
			'import jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-473 grid decode authenticate use strict", async () => {
		const result = await scanSource(
			"case-473.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-474 grid decode authenticate module banner", async () => {
		const result = await scanSource(
			"case-474.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-475 grid decode authenticate export const", async () => {
		const result = await scanSource(
			"case-475.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-476 grid verify-no-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-476.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-477 grid verify-no-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-477.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-478 grid verify-no-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-478.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-479 grid verify-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-479.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-480 grid verify-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-480.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-481 grid verify-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-481.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-482 grid sign-no-exp authenticate use strict", async () => {
		const result = await scanSource(
			"case-482.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-483 grid sign-no-exp authenticate module banner", async () => {
		const result = await scanSource(
			"case-483.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-484 grid sign-no-exp authenticate export const", async () => {
		const result = await scanSource(
			"case-484.ts",
			'import * as jwt from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-485 grid decode handleAuth use strict", async () => {
		const result = await scanSource(
			"case-485.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-486 grid decode handleAuth module banner", async () => {
		const result = await scanSource(
			"case-486.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-487 grid decode handleAuth export const", async () => {
		const result = await scanSource(
			"case-487.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-488 grid verify-no-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-488.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-489 grid verify-no-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-489.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-490 grid verify-no-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-490.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-491 grid verify-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-491.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-492 grid verify-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-492.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-493 grid verify-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-493.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-494 grid sign-no-exp handleAuth use strict", async () => {
		const result = await scanSource(
			"case-494.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-495 grid sign-no-exp handleAuth module banner", async () => {
		const result = await scanSource(
			"case-495.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-496 grid sign-no-exp handleAuth export const", async () => {
		const result = await scanSource(
			"case-496.ts",
			'import * as jwt from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-497 grid decode verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-497.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-498 grid decode verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-498.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-499 grid decode verifyRequest export const", async () => {
		const result = await scanSource(
			"case-499.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-500 grid verify-no-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-500.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-501 grid verify-no-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-501.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-502 grid verify-no-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-502.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-503 grid verify-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-503.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-504 grid verify-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-504.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-505 grid verify-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-505.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-506 grid sign-no-exp verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-506.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-507 grid sign-no-exp verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-507.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-508 grid sign-no-exp verifyRequest export const", async () => {
		const result = await scanSource(
			"case-508.ts",
			'import * as jwt from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-509 grid decode issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-509.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-510 grid decode issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-510.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-511 grid decode issueCredentials export const", async () => {
		const result = await scanSource(
			"case-511.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-512 grid verify-no-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-512.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-513 grid verify-no-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-513.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-514 grid verify-no-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-514.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-515 grid verify-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-515.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-516 grid verify-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-516.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-517 grid verify-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-517.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-518 grid sign-no-exp issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-518.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-519 grid sign-no-exp issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-519.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-520 grid sign-no-exp issueCredentials export const", async () => {
		const result = await scanSource(
			"case-520.ts",
			'import * as jwt from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-521 grid decode parseBearer use strict", async () => {
		const result = await scanSource(
			"case-521.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-522 grid decode parseBearer module banner", async () => {
		const result = await scanSource(
			"case-522.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-523 grid decode parseBearer export const", async () => {
		const result = await scanSource(
			"case-523.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-524 grid verify-no-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-524.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-525 grid verify-no-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-525.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-526 grid verify-no-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-526.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-527 grid verify-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-527.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-528 grid verify-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-528.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-529 grid verify-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-529.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-530 grid sign-no-exp parseBearer use strict", async () => {
		const result = await scanSource(
			"case-530.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-531 grid sign-no-exp parseBearer module banner", async () => {
		const result = await scanSource(
			"case-531.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-532 grid sign-no-exp parseBearer export const", async () => {
		const result = await scanSource(
			"case-532.ts",
			'import * as jwt from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-533 grid decode validateSession use strict", async () => {
		const result = await scanSource(
			"case-533.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-534 grid decode validateSession module banner", async () => {
		const result = await scanSource(
			"case-534.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-535 grid decode validateSession export const", async () => {
		const result = await scanSource(
			"case-535.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-536 grid verify-no-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-536.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-537 grid verify-no-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-537.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-538 grid verify-no-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-538.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-539 grid verify-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-539.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-540 grid verify-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-540.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-541 grid verify-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-541.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-542 grid sign-no-exp validateSession use strict", async () => {
		const result = await scanSource(
			"case-542.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-543 grid sign-no-exp validateSession module banner", async () => {
		const result = await scanSource(
			"case-543.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-544 grid sign-no-exp validateSession export const", async () => {
		const result = await scanSource(
			"case-544.ts",
			'import * as jwt from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-545 grid decode loginUser use strict", async () => {
		const result = await scanSource(
			"case-545.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-546 grid decode loginUser module banner", async () => {
		const result = await scanSource(
			"case-546.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-547 grid decode loginUser export const", async () => {
		const result = await scanSource(
			"case-547.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-548 grid verify-no-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-548.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-549 grid verify-no-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-549.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-550 grid verify-no-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-550.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-551 grid verify-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-551.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-552 grid verify-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-552.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-553 grid verify-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-553.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-554 grid sign-no-exp loginUser use strict", async () => {
		const result = await scanSource(
			"case-554.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-555 grid sign-no-exp loginUser module banner", async () => {
		const result = await scanSource(
			"case-555.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-556 grid sign-no-exp loginUser export const", async () => {
		const result = await scanSource(
			"case-556.ts",
			'import * as jwt from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-557 grid decode middleware use strict", async () => {
		const result = await scanSource(
			"case-557.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-558 grid decode middleware module banner", async () => {
		const result = await scanSource(
			"case-558.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-559 grid decode middleware export const", async () => {
		const result = await scanSource(
			"case-559.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-560 grid verify-no-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-560.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-561 grid verify-no-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-561.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-562 grid verify-no-alg middleware export const", async () => {
		const result = await scanSource(
			"case-562.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-563 grid verify-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-563.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-564 grid verify-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-564.ts",
			"/** module */\nimport * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-565 grid verify-alg middleware export const", async () => {
		const result = await scanSource(
			"case-565.ts",
			"import * as jwt from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-566 grid sign-no-exp middleware use strict", async () => {
		const result = await scanSource(
			"case-566.ts",
			'"use strict";\nimport * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-567 grid sign-no-exp middleware module banner", async () => {
		const result = await scanSource(
			"case-567.ts",
			'/** module */\nimport * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-568 grid sign-no-exp middleware export const", async () => {
		const result = await scanSource(
			"case-568.ts",
			'import * as jwt from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-569 grid decode authenticate use strict", async () => {
		const result = await scanSource(
			"case-569.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-570 grid decode authenticate module banner", async () => {
		const result = await scanSource(
			"case-570.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-571 grid decode authenticate export const", async () => {
		const result = await scanSource(
			"case-571.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-572 grid verify-no-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-572.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-573 grid verify-no-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-573.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-574 grid verify-no-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-574.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-575 grid verify-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-575.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-576 grid verify-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-576.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-577 grid verify-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-577.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function authenticate(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-578 grid sign-no-exp authenticate use strict", async () => {
		const result = await scanSource(
			"case-578.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-579 grid sign-no-exp authenticate module banner", async () => {
		const result = await scanSource(
			"case-579.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-580 grid sign-no-exp authenticate export const", async () => {
		const result = await scanSource(
			"case-580.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function authenticate(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-581 grid decode handleAuth use strict", async () => {
		const result = await scanSource(
			"case-581.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-582 grid decode handleAuth module banner", async () => {
		const result = await scanSource(
			"case-582.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-583 grid decode handleAuth export const", async () => {
		const result = await scanSource(
			"case-583.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-584 grid verify-no-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-584.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-585 grid verify-no-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-585.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-586 grid verify-no-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-586.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-587 grid verify-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-587.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-588 grid verify-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-588.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-589 grid verify-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-589.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function handleAuth(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-590 grid sign-no-exp handleAuth use strict", async () => {
		const result = await scanSource(
			"case-590.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-591 grid sign-no-exp handleAuth module banner", async () => {
		const result = await scanSource(
			"case-591.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-592 grid sign-no-exp handleAuth export const", async () => {
		const result = await scanSource(
			"case-592.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function handleAuth(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-593 grid decode verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-593.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-594 grid decode verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-594.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-595 grid decode verifyRequest export const", async () => {
		const result = await scanSource(
			"case-595.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-596 grid verify-no-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-596.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-597 grid verify-no-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-597.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-598 grid verify-no-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-598.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-599 grid verify-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-599.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-600 grid verify-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-600.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-601 grid verify-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-601.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function verifyRequest(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-602 grid sign-no-exp verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-602.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-603 grid sign-no-exp verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-603.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-604 grid sign-no-exp verifyRequest export const", async () => {
		const result = await scanSource(
			"case-604.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function verifyRequest(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-605 grid decode issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-605.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-606 grid decode issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-606.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-607 grid decode issueCredentials export const", async () => {
		const result = await scanSource(
			"case-607.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-608 grid verify-no-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-608.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-609 grid verify-no-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-609.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-610 grid verify-no-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-610.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-611 grid verify-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-611.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-612 grid verify-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-612.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-613 grid verify-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-613.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function issueCredentials(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-614 grid sign-no-exp issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-614.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-615 grid sign-no-exp issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-615.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-616 grid sign-no-exp issueCredentials export const", async () => {
		const result = await scanSource(
			"case-616.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function issueCredentials(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-617 grid decode parseBearer use strict", async () => {
		const result = await scanSource(
			"case-617.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-618 grid decode parseBearer module banner", async () => {
		const result = await scanSource(
			"case-618.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-619 grid decode parseBearer export const", async () => {
		const result = await scanSource(
			"case-619.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-620 grid verify-no-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-620.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-621 grid verify-no-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-621.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-622 grid verify-no-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-622.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-623 grid verify-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-623.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-624 grid verify-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-624.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-625 grid verify-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-625.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function parseBearer(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-626 grid sign-no-exp parseBearer use strict", async () => {
		const result = await scanSource(
			"case-626.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-627 grid sign-no-exp parseBearer module banner", async () => {
		const result = await scanSource(
			"case-627.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-628 grid sign-no-exp parseBearer export const", async () => {
		const result = await scanSource(
			"case-628.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function parseBearer(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-629 grid decode validateSession use strict", async () => {
		const result = await scanSource(
			"case-629.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-630 grid decode validateSession module banner", async () => {
		const result = await scanSource(
			"case-630.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-631 grid decode validateSession export const", async () => {
		const result = await scanSource(
			"case-631.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-632 grid verify-no-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-632.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-633 grid verify-no-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-633.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-634 grid verify-no-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-634.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-635 grid verify-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-635.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-636 grid verify-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-636.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-637 grid verify-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-637.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function validateSession(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-638 grid sign-no-exp validateSession use strict", async () => {
		const result = await scanSource(
			"case-638.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-639 grid sign-no-exp validateSession module banner", async () => {
		const result = await scanSource(
			"case-639.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-640 grid sign-no-exp validateSession export const", async () => {
		const result = await scanSource(
			"case-640.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function validateSession(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-641 grid decode loginUser use strict", async () => {
		const result = await scanSource(
			"case-641.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-642 grid decode loginUser module banner", async () => {
		const result = await scanSource(
			"case-642.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-643 grid decode loginUser export const", async () => {
		const result = await scanSource(
			"case-643.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-644 grid verify-no-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-644.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-645 grid verify-no-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-645.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-646 grid verify-no-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-646.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-647 grid verify-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-647.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-648 grid verify-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-648.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-649 grid verify-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-649.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function loginUser(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-650 grid sign-no-exp loginUser use strict", async () => {
		const result = await scanSource(
			"case-650.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-651 grid sign-no-exp loginUser module banner", async () => {
		const result = await scanSource(
			"case-651.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-652 grid sign-no-exp loginUser export const", async () => {
		const result = await scanSource(
			"case-652.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function loginUser(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-653 grid decode middleware use strict", async () => {
		const result = await scanSource(
			"case-653.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-654 grid decode middleware module banner", async () => {
		const result = await scanSource(
			"case-654.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-655 grid decode middleware export const", async () => {
		const result = await scanSource(
			"case-655.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string) {\n  return decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-656 grid verify-no-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-656.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-657 grid verify-no-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-657.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-658 grid verify-no-alg middleware export const", async () => {
		const result = await scanSource(
			"case-658.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-659 grid verify-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-659.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-660 grid verify-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-660.ts",
			"/** module */\nimport { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-661 grid verify-alg middleware export const", async () => {
		const result = await scanSource(
			"case-661.ts",
			"import { decode, verify, sign } from \"jsonwebtoken\";\nexport function middleware(t: string, s: string) {\n  return verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-662 grid sign-no-exp middleware use strict", async () => {
		const result = await scanSource(
			"case-662.ts",
			'"use strict";\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-663 grid sign-no-exp middleware module banner", async () => {
		const result = await scanSource(
			"case-663.ts",
			'/** module */\nimport { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-664 grid sign-no-exp middleware export const", async () => {
		const result = await scanSource(
			"case-664.ts",
			'import { decode, verify, sign } from "jsonwebtoken";\nexport function middleware(p: object, s: string) {\n  return sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-665 grid decode authenticate use strict", async () => {
		const result = await scanSource(
			"case-665.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-666 grid decode authenticate module banner", async () => {
		const result = await scanSource(
			"case-666.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-667 grid decode authenticate export const", async () => {
		const result = await scanSource(
			"case-667.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-668 grid verify-no-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-668.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-669 grid verify-no-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-669.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-670 grid verify-no-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-670.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-671 grid verify-alg authenticate use strict", async () => {
		const result = await scanSource(
			"case-671.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-672 grid verify-alg authenticate module banner", async () => {
		const result = await scanSource(
			"case-672.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-673 grid verify-alg authenticate export const", async () => {
		const result = await scanSource(
			"case-673.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function authenticate(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-674 grid sign-no-exp authenticate use strict", async () => {
		const result = await scanSource(
			"case-674.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-675 grid sign-no-exp authenticate module banner", async () => {
		const result = await scanSource(
			"case-675.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-676 grid sign-no-exp authenticate export const", async () => {
		const result = await scanSource(
			"case-676.ts",
			'const jwt = require("jsonwebtoken");\nexport function authenticate(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-677 grid decode handleAuth use strict", async () => {
		const result = await scanSource(
			"case-677.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-678 grid decode handleAuth module banner", async () => {
		const result = await scanSource(
			"case-678.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-679 grid decode handleAuth export const", async () => {
		const result = await scanSource(
			"case-679.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-680 grid verify-no-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-680.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-681 grid verify-no-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-681.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-682 grid verify-no-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-682.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-683 grid verify-alg handleAuth use strict", async () => {
		const result = await scanSource(
			"case-683.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-684 grid verify-alg handleAuth module banner", async () => {
		const result = await scanSource(
			"case-684.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-685 grid verify-alg handleAuth export const", async () => {
		const result = await scanSource(
			"case-685.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function handleAuth(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-686 grid sign-no-exp handleAuth use strict", async () => {
		const result = await scanSource(
			"case-686.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-687 grid sign-no-exp handleAuth module banner", async () => {
		const result = await scanSource(
			"case-687.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-688 grid sign-no-exp handleAuth export const", async () => {
		const result = await scanSource(
			"case-688.ts",
			'const jwt = require("jsonwebtoken");\nexport function handleAuth(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-689 grid decode verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-689.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-690 grid decode verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-690.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-691 grid decode verifyRequest export const", async () => {
		const result = await scanSource(
			"case-691.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-692 grid verify-no-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-692.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-693 grid verify-no-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-693.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-694 grid verify-no-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-694.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-695 grid verify-alg verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-695.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-696 grid verify-alg verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-696.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-697 grid verify-alg verifyRequest export const", async () => {
		const result = await scanSource(
			"case-697.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function verifyRequest(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-698 grid sign-no-exp verifyRequest use strict", async () => {
		const result = await scanSource(
			"case-698.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-699 grid sign-no-exp verifyRequest module banner", async () => {
		const result = await scanSource(
			"case-699.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-700 grid sign-no-exp verifyRequest export const", async () => {
		const result = await scanSource(
			"case-700.ts",
			'const jwt = require("jsonwebtoken");\nexport function verifyRequest(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-701 grid decode issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-701.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-702 grid decode issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-702.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-703 grid decode issueCredentials export const", async () => {
		const result = await scanSource(
			"case-703.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-704 grid verify-no-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-704.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-705 grid verify-no-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-705.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-706 grid verify-no-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-706.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-707 grid verify-alg issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-707.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-708 grid verify-alg issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-708.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-709 grid verify-alg issueCredentials export const", async () => {
		const result = await scanSource(
			"case-709.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function issueCredentials(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-710 grid sign-no-exp issueCredentials use strict", async () => {
		const result = await scanSource(
			"case-710.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-711 grid sign-no-exp issueCredentials module banner", async () => {
		const result = await scanSource(
			"case-711.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-712 grid sign-no-exp issueCredentials export const", async () => {
		const result = await scanSource(
			"case-712.ts",
			'const jwt = require("jsonwebtoken");\nexport function issueCredentials(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-713 grid decode parseBearer use strict", async () => {
		const result = await scanSource(
			"case-713.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-714 grid decode parseBearer module banner", async () => {
		const result = await scanSource(
			"case-714.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-715 grid decode parseBearer export const", async () => {
		const result = await scanSource(
			"case-715.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-716 grid verify-no-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-716.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-717 grid verify-no-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-717.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-718 grid verify-no-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-718.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-719 grid verify-alg parseBearer use strict", async () => {
		const result = await scanSource(
			"case-719.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-720 grid verify-alg parseBearer module banner", async () => {
		const result = await scanSource(
			"case-720.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-721 grid verify-alg parseBearer export const", async () => {
		const result = await scanSource(
			"case-721.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function parseBearer(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-722 grid sign-no-exp parseBearer use strict", async () => {
		const result = await scanSource(
			"case-722.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-723 grid sign-no-exp parseBearer module banner", async () => {
		const result = await scanSource(
			"case-723.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-724 grid sign-no-exp parseBearer export const", async () => {
		const result = await scanSource(
			"case-724.ts",
			'const jwt = require("jsonwebtoken");\nexport function parseBearer(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-725 grid decode validateSession use strict", async () => {
		const result = await scanSource(
			"case-725.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-726 grid decode validateSession module banner", async () => {
		const result = await scanSource(
			"case-726.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-727 grid decode validateSession export const", async () => {
		const result = await scanSource(
			"case-727.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-728 grid verify-no-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-728.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-729 grid verify-no-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-729.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-730 grid verify-no-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-730.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-731 grid verify-alg validateSession use strict", async () => {
		const result = await scanSource(
			"case-731.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-732 grid verify-alg validateSession module banner", async () => {
		const result = await scanSource(
			"case-732.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-733 grid verify-alg validateSession export const", async () => {
		const result = await scanSource(
			"case-733.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function validateSession(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-734 grid sign-no-exp validateSession use strict", async () => {
		const result = await scanSource(
			"case-734.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-735 grid sign-no-exp validateSession module banner", async () => {
		const result = await scanSource(
			"case-735.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-736 grid sign-no-exp validateSession export const", async () => {
		const result = await scanSource(
			"case-736.ts",
			'const jwt = require("jsonwebtoken");\nexport function validateSession(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-737 grid decode loginUser use strict", async () => {
		const result = await scanSource(
			"case-737.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-738 grid decode loginUser module banner", async () => {
		const result = await scanSource(
			"case-738.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-739 grid decode loginUser export const", async () => {
		const result = await scanSource(
			"case-739.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-740 grid verify-no-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-740.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-741 grid verify-no-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-741.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-742 grid verify-no-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-742.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-743 grid verify-alg loginUser use strict", async () => {
		const result = await scanSource(
			"case-743.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-744 grid verify-alg loginUser module banner", async () => {
		const result = await scanSource(
			"case-744.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-745 grid verify-alg loginUser export const", async () => {
		const result = await scanSource(
			"case-745.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function loginUser(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-746 grid sign-no-exp loginUser use strict", async () => {
		const result = await scanSource(
			"case-746.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-747 grid sign-no-exp loginUser module banner", async () => {
		const result = await scanSource(
			"case-747.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-748 grid sign-no-exp loginUser export const", async () => {
		const result = await scanSource(
			"case-748.ts",
			'const jwt = require("jsonwebtoken");\nexport function loginUser(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-749 grid decode middleware use strict", async () => {
		const result = await scanSource(
			"case-749.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-750 grid decode middleware module banner", async () => {
		const result = await scanSource(
			"case-750.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-751 grid decode middleware export const", async () => {
		const result = await scanSource(
			"case-751.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string) {\n  return jwt.decode(t);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-752 grid verify-no-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-752.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-753 grid verify-no-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-753.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-754 grid verify-no-alg middleware export const", async () => {
		const result = await scanSource(
			"case-754.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-02": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-755 grid verify-alg middleware use strict", async () => {
		const result = await scanSource(
			"case-755.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: [\'HS256\'] });\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-756 grid verify-alg middleware module banner", async () => {
		const result = await scanSource(
			"case-756.ts",
			"/** module */\nconst jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-757 grid verify-alg middleware export const", async () => {
		const result = await scanSource(
			"case-757.ts",
			"const jwt = require(\"jsonwebtoken\");\nexport function middleware(t: string, s: string) {\n  return jwt.verify(t, s, { algorithms: ['HS256'] });\n}\n\nexport const MODULE = 1;\n",
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-758 grid sign-no-exp middleware use strict", async () => {
		const result = await scanSource(
			"case-758.ts",
			'"use strict";\nconst jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-759 grid sign-no-exp middleware module banner", async () => {
		const result = await scanSource(
			"case-759.ts",
			'/** module */\nconst jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-JWT-760 grid sign-no-exp middleware export const", async () => {
		const result = await scanSource(
			"case-760.ts",
			'const jwt = require("jsonwebtoken");\nexport function middleware(p: object, s: string) {\n  return jwt.sign(p, s);\n}\n\nexport const MODULE = 1;\n',
		);
		const expected = { "CS-JWT-05": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
