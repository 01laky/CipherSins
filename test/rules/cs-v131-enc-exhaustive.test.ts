// Auto-generated by scripts/generate-v131-tests.mjs — do not edit by hand
import { describe, expect, it } from "vitest";
import { countRule, scanSource } from "../helpers/v131-scan-source.js";

describe("CS-V131 ENC exhaustive edge cases", () => {
	it("CS-V131-ENC-001 ENC-03 weak cipher des-cbc", async () => {
		const result = await scanSource(
			"case-001.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-002 ENC-03 weak cipher des-ede3", async () => {
		const result = await scanSource(
			"case-002.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-003 ENC-03 weak cipher rc4", async () => {
		const result = await scanSource(
			"case-003.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-004 ENC-03 weak cipher rc2", async () => {
		const result = await scanSource(
			"case-004.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-005 ENC-03 weak cipher bf", async () => {
		const result = await scanSource(
			"case-005.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-006 ENC-03 weak cipher cast", async () => {
		const result = await scanSource(
			"case-006.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-007 ENC-03 weak cipher des", async () => {
		const result = await scanSource(
			"case-007.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-008 ENC-04 ecb aes-128-ecb", async () => {
		const result = await scanSource(
			"case-008.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-009 ENC-04 ecb aes-256-ecb", async () => {
		const result = await scanSource(
			"case-009.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-010 ENC-04 ecb aes-192-ecb", async () => {
		const result = await scanSource(
			"case-010.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-011 ENC-03 safe aes-256-gcm clean", async () => {
		const result = await scanSource(
			"case-011.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-012 ENC-03 safe aes-256-cbc clean", async () => {
		const result = await scanSource(
			"case-012.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-013 ENC-03 safe aes-128-gcm clean", async () => {
		const result = await scanSource(
			"case-013.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-014 ENC-01 hardcoded key string", async () => {
		const result = await scanSource(
			"case-014.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-015 ENC-01 same-file const key", async () => {
		const result = await scanSource(
			"case-015.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-016 ENC-01 random key clean", async () => {
		const result = await scanSource(
			"case-016.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-017 ENC-02 gcm static iv", async () => {
		const result = await scanSource(
			"case-017.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-018 ENC-02 gcm random iv clean", async () => {
		const result = await scanSource(
			"case-018.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-019 ENC-02 gcm reused iv twice", async () => {
		const result = await scanSource(
			"case-019.ts",
			'import { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-020 DEC-01 createDecipher deprecated", async () => {
		const result = await scanSource(
			"case-020.ts",
			'import { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-021 DEC-01 createCipher deprecated", async () => {
		const result = await scanSource(
			"case-021.ts",
			'import { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-022 DEC-01 createDecipheriv clean", async () => {
		const result = await scanSource(
			"case-022.ts",
			'import { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-023 ENC-01+03 des hardcoded key", async () => {
		const result = await scanSource(
			"case-023.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-024 ENC-01+04 ecb hardcoded key", async () => {
		const result = await scanSource(
			"case-024.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-025 ENC-03 node:crypto des-cbc", async () => {
		const result = await scanSource(
			"case-025.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-026 ENC-03 node:crypto rc4", async () => {
		const result = await scanSource(
			"case-026.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-027 ENC-03 node:crypto aes-256-gcm", async () => {
		const result = await scanSource(
			"case-027.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-028 ENC-01 hardcoded iv 'static-iv-1234", async () => {
		const result = await scanSource(
			"case-028.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-029 ENC-01 hardcoded iv Buffer.from('01", async () => {
		const result = await scanSource(
			"case-029.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-030 weak des-cbc", async () => {
		const result = await scanSource(
			"case-030.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-031 weak des-cbc", async () => {
		const result = await scanSource(
			"case-031.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-032 weak des-ede3", async () => {
		const result = await scanSource(
			"case-032.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-033 weak des-ede3", async () => {
		const result = await scanSource(
			"case-033.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-034 weak rc4", async () => {
		const result = await scanSource(
			"case-034.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-035 weak rc4", async () => {
		const result = await scanSource(
			"case-035.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-036 weak rc2", async () => {
		const result = await scanSource(
			"case-036.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-037 weak rc2", async () => {
		const result = await scanSource(
			"case-037.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-038 weak bf", async () => {
		const result = await scanSource(
			"case-038.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-039 weak bf", async () => {
		const result = await scanSource(
			"case-039.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-040 weak cast5", async () => {
		const result = await scanSource(
			"case-040.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-041 weak cast5", async () => {
		const result = await scanSource(
			"case-041.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-042 weak des", async () => {
		const result = await scanSource(
			"case-042.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-043 weak des", async () => {
		const result = await scanSource(
			"case-043.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-044 ecb aes-128-ecb", async () => {
		const result = await scanSource(
			"case-044.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-045 ecb aes-192-ecb", async () => {
		const result = await scanSource(
			"case-045.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-046 ecb aes-256-ecb", async () => {
		const result = await scanSource(
			"case-046.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-047 safe aes-256-gcm", async () => {
		const result = await scanSource(
			"case-047.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-048 safe aes-256-cbc", async () => {
		const result = await scanSource(
			"case-048.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-049 safe aes-128-gcm", async () => {
		const result = await scanSource(
			"case-049.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-050 safe chacha20-poly1305", async () => {
		const result = await scanSource(
			"case-050.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-051 ENC-03 weak cipher des-cbc wrap0", async () => {
		const result = await scanSource(
			"case-051.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-052 ENC-03 weak cipher des-cbc wrap1", async () => {
		const result = await scanSource(
			"case-052.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-053 ENC-03 weak cipher des-cbc wrap2", async () => {
		const result = await scanSource(
			"case-053.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-054 ENC-03 weak cipher des-cbc wrap3", async () => {
		const result = await scanSource(
			"case-054.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-055 ENC-03 weak cipher des-cbc wrap4", async () => {
		const result = await scanSource(
			"case-055.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-056 ENC-03 weak cipher des-ede3 wrap0", async () => {
		const result = await scanSource(
			"case-056.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-057 ENC-03 weak cipher des-ede3 wrap1", async () => {
		const result = await scanSource(
			"case-057.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-058 ENC-03 weak cipher des-ede3 wrap2", async () => {
		const result = await scanSource(
			"case-058.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-059 ENC-03 weak cipher des-ede3 wrap3", async () => {
		const result = await scanSource(
			"case-059.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-060 ENC-03 weak cipher des-ede3 wrap4", async () => {
		const result = await scanSource(
			"case-060.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-061 ENC-03 weak cipher rc4 wrap0", async () => {
		const result = await scanSource(
			"case-061.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-062 ENC-03 weak cipher rc4 wrap1", async () => {
		const result = await scanSource(
			"case-062.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-063 ENC-03 weak cipher rc4 wrap2", async () => {
		const result = await scanSource(
			"case-063.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-064 ENC-03 weak cipher rc4 wrap3", async () => {
		const result = await scanSource(
			"case-064.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-065 ENC-03 weak cipher rc4 wrap4", async () => {
		const result = await scanSource(
			"case-065.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-066 ENC-03 weak cipher rc2 wrap0", async () => {
		const result = await scanSource(
			"case-066.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-067 ENC-03 weak cipher rc2 wrap1", async () => {
		const result = await scanSource(
			"case-067.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-068 ENC-03 weak cipher rc2 wrap2", async () => {
		const result = await scanSource(
			"case-068.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-069 ENC-03 weak cipher rc2 wrap3", async () => {
		const result = await scanSource(
			"case-069.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-070 ENC-03 weak cipher rc2 wrap4", async () => {
		const result = await scanSource(
			"case-070.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-071 ENC-03 weak cipher bf wrap0", async () => {
		const result = await scanSource(
			"case-071.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-072 ENC-03 weak cipher bf wrap1", async () => {
		const result = await scanSource(
			"case-072.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-073 ENC-03 weak cipher bf wrap2", async () => {
		const result = await scanSource(
			"case-073.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-074 ENC-03 weak cipher bf wrap3", async () => {
		const result = await scanSource(
			"case-074.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-075 ENC-03 weak cipher bf wrap4", async () => {
		const result = await scanSource(
			"case-075.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("bf", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-076 ENC-03 weak cipher cast wrap0", async () => {
		const result = await scanSource(
			"case-076.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-077 ENC-03 weak cipher cast wrap1", async () => {
		const result = await scanSource(
			"case-077.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-078 ENC-03 weak cipher cast wrap2", async () => {
		const result = await scanSource(
			"case-078.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-079 ENC-03 weak cipher cast wrap3", async () => {
		const result = await scanSource(
			"case-079.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-080 ENC-03 weak cipher cast wrap4", async () => {
		const result = await scanSource(
			"case-080.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("cast", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-081 ENC-03 weak cipher des wrap0", async () => {
		const result = await scanSource(
			"case-081.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-082 ENC-03 weak cipher des wrap1", async () => {
		const result = await scanSource(
			"case-082.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-083 ENC-03 weak cipher des wrap2", async () => {
		const result = await scanSource(
			"case-083.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-084 ENC-03 weak cipher des wrap3", async () => {
		const result = await scanSource(
			"case-084.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-085 ENC-03 weak cipher des wrap4", async () => {
		const result = await scanSource(
			"case-085.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("des", key, iv);\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-086 ENC-04 ecb aes-128-ecb wrap0", async () => {
		const result = await scanSource(
			"case-086.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-087 ENC-04 ecb aes-128-ecb wrap1", async () => {
		const result = await scanSource(
			"case-087.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-088 ENC-04 ecb aes-128-ecb wrap2", async () => {
		const result = await scanSource(
			"case-088.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-089 ENC-04 ecb aes-128-ecb wrap3", async () => {
		const result = await scanSource(
			"case-089.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-090 ENC-04 ecb aes-128-ecb wrap4", async () => {
		const result = await scanSource(
			"case-090.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-091 ENC-04 ecb aes-256-ecb wrap0", async () => {
		const result = await scanSource(
			"case-091.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-092 ENC-04 ecb aes-256-ecb wrap1", async () => {
		const result = await scanSource(
			"case-092.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-093 ENC-04 ecb aes-256-ecb wrap2", async () => {
		const result = await scanSource(
			"case-093.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-094 ENC-04 ecb aes-256-ecb wrap3", async () => {
		const result = await scanSource(
			"case-094.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-095 ENC-04 ecb aes-256-ecb wrap4", async () => {
		const result = await scanSource(
			"case-095.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-096 ENC-04 ecb aes-192-ecb wrap0", async () => {
		const result = await scanSource(
			"case-096.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-097 ENC-04 ecb aes-192-ecb wrap1", async () => {
		const result = await scanSource(
			"case-097.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-098 ENC-04 ecb aes-192-ecb wrap2", async () => {
		const result = await scanSource(
			"case-098.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-099 ENC-04 ecb aes-192-ecb wrap3", async () => {
		const result = await scanSource(
			"case-099.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-100 ENC-04 ecb aes-192-ecb wrap4", async () => {
		const result = await scanSource(
			"case-100.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", key, iv);\n}',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-101 ENC-03 safe aes-256-gcm clean wrap0", async () => {
		const result = await scanSource(
			"case-101.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-102 ENC-03 safe aes-256-gcm clean wrap1", async () => {
		const result = await scanSource(
			"case-102.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-103 ENC-03 safe aes-256-gcm clean wrap2", async () => {
		const result = await scanSource(
			"case-103.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-104 ENC-03 safe aes-256-gcm clean wrap3", async () => {
		const result = await scanSource(
			"case-104.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-105 ENC-03 safe aes-256-gcm clean wrap4", async () => {
		const result = await scanSource(
			"case-105.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-106 ENC-03 safe aes-256-cbc clean wrap0", async () => {
		const result = await scanSource(
			"case-106.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-107 ENC-03 safe aes-256-cbc clean wrap1", async () => {
		const result = await scanSource(
			"case-107.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-108 ENC-03 safe aes-256-cbc clean wrap2", async () => {
		const result = await scanSource(
			"case-108.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-109 ENC-03 safe aes-256-cbc clean wrap3", async () => {
		const result = await scanSource(
			"case-109.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-110 ENC-03 safe aes-256-cbc clean wrap4", async () => {
		const result = await scanSource(
			"case-110.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-111 ENC-03 safe aes-128-gcm clean wrap0", async () => {
		const result = await scanSource(
			"case-111.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-112 ENC-03 safe aes-128-gcm clean wrap1", async () => {
		const result = await scanSource(
			"case-112.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-113 ENC-03 safe aes-128-gcm clean wrap2", async () => {
		const result = await scanSource(
			"case-113.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-114 ENC-03 safe aes-128-gcm clean wrap3", async () => {
		const result = await scanSource(
			"case-114.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-115 ENC-03 safe aes-128-gcm clean wrap4", async () => {
		const result = await scanSource(
			"case-115.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-128-gcm", key, randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-116 ENC-01 hardcoded key string wrap0", async () => {
		const result = await scanSource(
			"case-116.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-117 ENC-01 hardcoded key string wrap1", async () => {
		const result = await scanSource(
			"case-117.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-118 ENC-01 hardcoded key string wrap2", async () => {
		const result = await scanSource(
			"case-118.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-119 ENC-01 hardcoded key string wrap3", async () => {
		const result = await scanSource(
			"case-119.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-120 ENC-01 hardcoded key string wrap4", async () => {
		const result = await scanSource(
			"case-120.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", "sixteen-byte-key", randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-121 ENC-01 same-file const key wrap0", async () => {
		const result = await scanSource(
			"case-121.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-122 ENC-01 same-file const key wrap1", async () => {
		const result = await scanSource(
			"case-122.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-123 ENC-01 same-file const key wrap2", async () => {
		const result = await scanSource(
			"case-123.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-124 ENC-01 same-file const key wrap3", async () => {
		const result = await scanSource(
			"case-124.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-125 ENC-01 same-file const key wrap4", async () => {
		const result = await scanSource(
			"case-125.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nconst key = "sixteen-byte-key";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", key, randomBytes(16));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-126 ENC-01 random key clean wrap0", async () => {
		const result = await scanSource(
			"case-126.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-127 ENC-01 random key clean wrap1", async () => {
		const result = await scanSource(
			"case-127.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-128 ENC-01 random key clean wrap2", async () => {
		const result = await scanSource(
			"case-128.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-129 ENC-01 random key clean wrap3", async () => {
		const result = await scanSource(
			"case-129.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-130 ENC-01 random key clean wrap4", async () => {
		const result = await scanSource(
			"case-130.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-256-cbc", randomBytes(32), randomBytes(16));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-131 ENC-02 gcm static iv wrap0", async () => {
		const result = await scanSource(
			"case-131.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-132 ENC-02 gcm static iv wrap1", async () => {
		const result = await scanSource(
			"case-132.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-133 ENC-02 gcm static iv wrap2", async () => {
		const result = await scanSource(
			"case-133.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-134 ENC-02 gcm static iv wrap3", async () => {
		const result = await scanSource(
			"case-134.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-135 ENC-02 gcm static iv wrap4", async () => {
		const result = await scanSource(
			"case-135.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, "twelve-byte!");\n}',
		);
		const expected = { "CS-ENC-02": 1, "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-136 ENC-02 gcm random iv clean wrap0", async () => {
		const result = await scanSource(
			"case-136.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-137 ENC-02 gcm random iv clean wrap1", async () => {
		const result = await scanSource(
			"case-137.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-138 ENC-02 gcm random iv clean wrap2", async () => {
		const result = await scanSource(
			"case-138.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-139 ENC-02 gcm random iv clean wrap3", async () => {
		const result = await scanSource(
			"case-139.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-140 ENC-02 gcm random iv clean wrap4", async () => {
		const result = await scanSource(
			"case-140.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(12));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-141 ENC-02 gcm reused iv twice wrap0", async () => {
		const result = await scanSource(
			"case-141.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-142 ENC-02 gcm reused iv twice wrap1", async () => {
		const result = await scanSource(
			"case-142.ts",
			'import { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-143 ENC-02 gcm reused iv twice wrap2", async () => {
		const result = await scanSource(
			"case-143.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-144 ENC-02 gcm reused iv twice wrap3", async () => {
		const result = await scanSource(
			"case-144.ts",
			'import { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-145 ENC-02 gcm reused iv twice wrap4", async () => {
		const result = await scanSource(
			"case-145.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nconst iv = "twelve-byte!";\nexport function enc(data: Buffer, key: Buffer) {\n  createCipheriv("aes-256-gcm", key, iv);\n  return createCipheriv("aes-256-gcm", key, iv);\n}',
		);
		const expected = { "CS-ENC-01": 2 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-146 DEC-01 createDecipher deprecated wrap0", async () => {
		const result = await scanSource(
			"case-146.ts",
			'"use strict";\nimport { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-147 DEC-01 createDecipher deprecated wrap1", async () => {
		const result = await scanSource(
			"case-147.ts",
			'import { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-148 DEC-01 createDecipher deprecated wrap2", async () => {
		const result = await scanSource(
			"case-148.ts",
			'// password hashing module\nimport { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-149 DEC-01 createDecipher deprecated wrap3", async () => {
		const result = await scanSource(
			"case-149.ts",
			'import { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-150 DEC-01 createDecipher deprecated wrap4", async () => {
		const result = await scanSource(
			"case-150.ts",
			'/* crypto */\nimport { createDecipher } from "crypto";\nexport function dec(data: Buffer, password: string) {\n  return createDecipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-151 DEC-01 createCipher deprecated wrap0", async () => {
		const result = await scanSource(
			"case-151.ts",
			'"use strict";\nimport { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-152 DEC-01 createCipher deprecated wrap1", async () => {
		const result = await scanSource(
			"case-152.ts",
			'import { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-153 DEC-01 createCipher deprecated wrap2", async () => {
		const result = await scanSource(
			"case-153.ts",
			'// password hashing module\nimport { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-154 DEC-01 createCipher deprecated wrap3", async () => {
		const result = await scanSource(
			"case-154.ts",
			'import { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-155 DEC-01 createCipher deprecated wrap4", async () => {
		const result = await scanSource(
			"case-155.ts",
			'/* crypto */\nimport { createCipher } from "crypto";\nexport function enc(data: Buffer, password: string) {\n  return createCipher("aes-256-cbc", password);\n}',
		);
		const expected = { "CS-DEC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-156 DEC-01 createDecipheriv clean wrap0", async () => {
		const result = await scanSource(
			"case-156.ts",
			'"use strict";\nimport { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-157 DEC-01 createDecipheriv clean wrap1", async () => {
		const result = await scanSource(
			"case-157.ts",
			'import { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-158 DEC-01 createDecipheriv clean wrap2", async () => {
		const result = await scanSource(
			"case-158.ts",
			'// password hashing module\nimport { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-159 DEC-01 createDecipheriv clean wrap3", async () => {
		const result = await scanSource(
			"case-159.ts",
			'import { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-160 DEC-01 createDecipheriv clean wrap4", async () => {
		const result = await scanSource(
			"case-160.ts",
			'/* crypto */\nimport { createDecipheriv, randomBytes } from "crypto";\nexport function dec(data: Buffer, key: Buffer, iv: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, iv);\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-161 ENC-01+03 des hardcoded key wrap0", async () => {
		const result = await scanSource(
			"case-161.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-162 ENC-01+03 des hardcoded key wrap1", async () => {
		const result = await scanSource(
			"case-162.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-163 ENC-01+03 des hardcoded key wrap2", async () => {
		const result = await scanSource(
			"case-163.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-164 ENC-01+03 des hardcoded key wrap3", async () => {
		const result = await scanSource(
			"case-164.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-165 ENC-01+03 des hardcoded key wrap4", async () => {
		const result = await scanSource(
			"case-165.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("des-cbc", "12345678", "12345678");\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-166 ENC-01+04 ecb hardcoded key wrap0", async () => {
		const result = await scanSource(
			"case-166.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-167 ENC-01+04 ecb hardcoded key wrap1", async () => {
		const result = await scanSource(
			"case-167.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-168 ENC-01+04 ecb hardcoded key wrap2", async () => {
		const result = await scanSource(
			"case-168.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-169 ENC-01+04 ecb hardcoded key wrap3", async () => {
		const result = await scanSource(
			"case-169.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-170 ENC-01+04 ecb hardcoded key wrap4", async () => {
		const result = await scanSource(
			"case-170.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(data: Buffer) {\n  return createCipheriv("aes-128-ecb", "1234567890123456", Buffer.alloc(0));\n}',
		);
		const expected = { "CS-ENC-01": 1, "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-171 ENC-03 node:crypto des-cbc wrap0", async () => {
		const result = await scanSource(
			"case-171.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-172 ENC-03 node:crypto des-cbc wrap1", async () => {
		const result = await scanSource(
			"case-172.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-173 ENC-03 node:crypto des-cbc wrap2", async () => {
		const result = await scanSource(
			"case-173.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-174 ENC-03 node:crypto des-cbc wrap3", async () => {
		const result = await scanSource(
			"case-174.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-175 ENC-03 node:crypto des-cbc wrap4", async () => {
		const result = await scanSource(
			"case-175.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("des-cbc", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-176 ENC-03 node:crypto rc4 wrap0", async () => {
		const result = await scanSource(
			"case-176.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-177 ENC-03 node:crypto rc4 wrap1", async () => {
		const result = await scanSource(
			"case-177.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-178 ENC-03 node:crypto rc4 wrap2", async () => {
		const result = await scanSource(
			"case-178.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-179 ENC-03 node:crypto rc4 wrap3", async () => {
		const result = await scanSource(
			"case-179.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-180 ENC-03 node:crypto rc4 wrap4", async () => {
		const result = await scanSource(
			"case-180.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("rc4", key, randomBytes(8));\n}',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-181 ENC-03 node:crypto aes-256-gcm wrap0", async () => {
		const result = await scanSource(
			"case-181.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-182 ENC-03 node:crypto aes-256-gcm wrap1", async () => {
		const result = await scanSource(
			"case-182.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-183 ENC-03 node:crypto aes-256-gcm wrap2", async () => {
		const result = await scanSource(
			"case-183.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-184 ENC-03 node:crypto aes-256-gcm wrap3", async () => {
		const result = await scanSource(
			"case-184.ts",
			'import { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-185 ENC-03 node:crypto aes-256-gcm wrap4", async () => {
		const result = await scanSource(
			"case-185.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "node:crypto";\nexport function enc(data: Buffer, key: Buffer) {\n  return createCipheriv("aes-256-gcm", key, randomBytes(8));\n}',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-186 ENC-01 hardcoded iv 'static-iv-1234 wrap0", async () => {
		const result = await scanSource(
			"case-186.ts",
			'"use strict";\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-187 ENC-01 hardcoded iv 'static-iv-1234 wrap1", async () => {
		const result = await scanSource(
			"case-187.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-188 ENC-01 hardcoded iv 'static-iv-1234 wrap2", async () => {
		const result = await scanSource(
			"case-188.ts",
			'// password hashing module\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-189 ENC-01 hardcoded iv 'static-iv-1234 wrap3", async () => {
		const result = await scanSource(
			"case-189.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-190 ENC-01 hardcoded iv 'static-iv-1234 wrap4", async () => {
		const result = await scanSource(
			"case-190.ts",
			'/* crypto */\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, "static-iv-123456");\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-191 ENC-01 hardcoded iv Buffer.from('01 wrap0", async () => {
		const result = await scanSource(
			"case-191.ts",
			'"use strict";\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-192 ENC-01 hardcoded iv Buffer.from('01 wrap1", async () => {
		const result = await scanSource(
			"case-192.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-193 ENC-01 hardcoded iv Buffer.from('01 wrap2", async () => {
		const result = await scanSource(
			"case-193.ts",
			'// password hashing module\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-194 ENC-01 hardcoded iv Buffer.from('01 wrap3", async () => {
		const result = await scanSource(
			"case-194.ts",
			'import { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-195 ENC-01 hardcoded iv Buffer.from('01 wrap4", async () => {
		const result = await scanSource(
			"case-195.ts",
			'/* crypto */\nimport { createDecipheriv } from "crypto";\nexport function dec(data: Buffer, key: Buffer) {\n  return createDecipheriv("aes-256-cbc", key, Buffer.from(\'0123456789012345\'));\n}',
		);
		const expected = { "CS-ENC-01": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-196 weak des-cbc wrap0", async () => {
		const result = await scanSource(
			"case-196.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-197 weak des-cbc wrap1", async () => {
		const result = await scanSource(
			"case-197.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-198 weak des-cbc wrap2", async () => {
		const result = await scanSource(
			"case-198.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-199 weak des-cbc wrap3", async () => {
		const result = await scanSource(
			"case-199.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-200 weak des-cbc wrap4", async () => {
		const result = await scanSource(
			"case-200.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-201 weak des-cbc wrap0", async () => {
		const result = await scanSource(
			"case-201.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-202 weak des-cbc wrap1", async () => {
		const result = await scanSource(
			"case-202.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-203 weak des-cbc wrap2", async () => {
		const result = await scanSource(
			"case-203.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-204 weak des-cbc wrap3", async () => {
		const result = await scanSource(
			"case-204.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-205 weak des-cbc wrap4", async () => {
		const result = await scanSource(
			"case-205.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-cbc", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-206 weak des-ede3 wrap0", async () => {
		const result = await scanSource(
			"case-206.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-207 weak des-ede3 wrap1", async () => {
		const result = await scanSource(
			"case-207.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-208 weak des-ede3 wrap2", async () => {
		const result = await scanSource(
			"case-208.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-209 weak des-ede3 wrap3", async () => {
		const result = await scanSource(
			"case-209.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-210 weak des-ede3 wrap4", async () => {
		const result = await scanSource(
			"case-210.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-211 weak des-ede3 wrap0", async () => {
		const result = await scanSource(
			"case-211.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-212 weak des-ede3 wrap1", async () => {
		const result = await scanSource(
			"case-212.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-213 weak des-ede3 wrap2", async () => {
		const result = await scanSource(
			"case-213.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-214 weak des-ede3 wrap3", async () => {
		const result = await scanSource(
			"case-214.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-215 weak des-ede3 wrap4", async () => {
		const result = await scanSource(
			"case-215.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des-ede3", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-216 weak rc4 wrap0", async () => {
		const result = await scanSource(
			"case-216.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-217 weak rc4 wrap1", async () => {
		const result = await scanSource(
			"case-217.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-218 weak rc4 wrap2", async () => {
		const result = await scanSource(
			"case-218.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-219 weak rc4 wrap3", async () => {
		const result = await scanSource(
			"case-219.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-220 weak rc4 wrap4", async () => {
		const result = await scanSource(
			"case-220.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-221 weak rc4 wrap0", async () => {
		const result = await scanSource(
			"case-221.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-222 weak rc4 wrap1", async () => {
		const result = await scanSource(
			"case-222.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-223 weak rc4 wrap2", async () => {
		const result = await scanSource(
			"case-223.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-224 weak rc4 wrap3", async () => {
		const result = await scanSource(
			"case-224.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-225 weak rc4 wrap4", async () => {
		const result = await scanSource(
			"case-225.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc4", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-226 weak rc2 wrap0", async () => {
		const result = await scanSource(
			"case-226.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-227 weak rc2 wrap1", async () => {
		const result = await scanSource(
			"case-227.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-228 weak rc2 wrap2", async () => {
		const result = await scanSource(
			"case-228.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-229 weak rc2 wrap3", async () => {
		const result = await scanSource(
			"case-229.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-230 weak rc2 wrap4", async () => {
		const result = await scanSource(
			"case-230.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-231 weak rc2 wrap0", async () => {
		const result = await scanSource(
			"case-231.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-232 weak rc2 wrap1", async () => {
		const result = await scanSource(
			"case-232.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-233 weak rc2 wrap2", async () => {
		const result = await scanSource(
			"case-233.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-234 weak rc2 wrap3", async () => {
		const result = await scanSource(
			"case-234.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-235 weak rc2 wrap4", async () => {
		const result = await scanSource(
			"case-235.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("rc2", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-236 weak bf wrap0", async () => {
		const result = await scanSource(
			"case-236.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-237 weak bf wrap1", async () => {
		const result = await scanSource(
			"case-237.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-238 weak bf wrap2", async () => {
		const result = await scanSource(
			"case-238.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-239 weak bf wrap3", async () => {
		const result = await scanSource(
			"case-239.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-240 weak bf wrap4", async () => {
		const result = await scanSource(
			"case-240.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-241 weak bf wrap0", async () => {
		const result = await scanSource(
			"case-241.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-242 weak bf wrap1", async () => {
		const result = await scanSource(
			"case-242.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-243 weak bf wrap2", async () => {
		const result = await scanSource(
			"case-243.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-244 weak bf wrap3", async () => {
		const result = await scanSource(
			"case-244.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-245 weak bf wrap4", async () => {
		const result = await scanSource(
			"case-245.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("bf", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-246 weak cast5 wrap0", async () => {
		const result = await scanSource(
			"case-246.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-247 weak cast5 wrap1", async () => {
		const result = await scanSource(
			"case-247.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-248 weak cast5 wrap2", async () => {
		const result = await scanSource(
			"case-248.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-249 weak cast5 wrap3", async () => {
		const result = await scanSource(
			"case-249.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-250 weak cast5 wrap4", async () => {
		const result = await scanSource(
			"case-250.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-251 weak cast5 wrap0", async () => {
		const result = await scanSource(
			"case-251.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-252 weak cast5 wrap1", async () => {
		const result = await scanSource(
			"case-252.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-253 weak cast5 wrap2", async () => {
		const result = await scanSource(
			"case-253.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-254 weak cast5 wrap3", async () => {
		const result = await scanSource(
			"case-254.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-255 weak cast5 wrap4", async () => {
		const result = await scanSource(
			"case-255.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("cast5", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-256 weak des wrap0", async () => {
		const result = await scanSource(
			"case-256.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-257 weak des wrap1", async () => {
		const result = await scanSource(
			"case-257.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-258 weak des wrap2", async () => {
		const result = await scanSource(
			"case-258.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-259 weak des wrap3", async () => {
		const result = await scanSource(
			"case-259.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-260 weak des wrap4", async () => {
		const result = await scanSource(
			"case-260.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-261 weak des wrap0", async () => {
		const result = await scanSource(
			"case-261.ts",
			'"use strict";\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-262 weak des wrap1", async () => {
		const result = await scanSource(
			"case-262.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-263 weak des wrap2", async () => {
		const result = await scanSource(
			"case-263.ts",
			'// password hashing module\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-264 weak des wrap3", async () => {
		const result = await scanSource(
			"case-264.ts",
			'import { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-265 weak des wrap4", async () => {
		const result = await scanSource(
			"case-265.ts",
			'/* crypto */\nimport { createCipheriv } from "node:crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("des", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-03": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-266 ecb aes-128-ecb wrap0", async () => {
		const result = await scanSource(
			"case-266.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-267 ecb aes-128-ecb wrap1", async () => {
		const result = await scanSource(
			"case-267.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-268 ecb aes-128-ecb wrap2", async () => {
		const result = await scanSource(
			"case-268.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-269 ecb aes-128-ecb wrap3", async () => {
		const result = await scanSource(
			"case-269.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-270 ecb aes-128-ecb wrap4", async () => {
		const result = await scanSource(
			"case-270.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-128-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-271 ecb aes-192-ecb wrap0", async () => {
		const result = await scanSource(
			"case-271.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-272 ecb aes-192-ecb wrap1", async () => {
		const result = await scanSource(
			"case-272.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-273 ecb aes-192-ecb wrap2", async () => {
		const result = await scanSource(
			"case-273.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-274 ecb aes-192-ecb wrap3", async () => {
		const result = await scanSource(
			"case-274.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-275 ecb aes-192-ecb wrap4", async () => {
		const result = await scanSource(
			"case-275.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-192-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-276 ecb aes-256-ecb wrap0", async () => {
		const result = await scanSource(
			"case-276.ts",
			'"use strict";\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-277 ecb aes-256-ecb wrap1", async () => {
		const result = await scanSource(
			"case-277.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-278 ecb aes-256-ecb wrap2", async () => {
		const result = await scanSource(
			"case-278.ts",
			'// password hashing module\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-279 ecb aes-256-ecb wrap3", async () => {
		const result = await scanSource(
			"case-279.ts",
			'import { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n\nexport const __module = true;\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-280 ecb aes-256-ecb wrap4", async () => {
		const result = await scanSource(
			"case-280.ts",
			'/* crypto */\nimport { createCipheriv } from "crypto";\nexport function enc(d: Buffer, k: Buffer, iv: Buffer) {\n  return createCipheriv("aes-256-ecb", k, iv);\n}\n',
		);
		const expected = { "CS-ENC-04": 1 };
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-281 safe aes-256-gcm wrap0", async () => {
		const result = await scanSource(
			"case-281.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-282 safe aes-256-gcm wrap1", async () => {
		const result = await scanSource(
			"case-282.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-283 safe aes-256-gcm wrap2", async () => {
		const result = await scanSource(
			"case-283.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-284 safe aes-256-gcm wrap3", async () => {
		const result = await scanSource(
			"case-284.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-285 safe aes-256-gcm wrap4", async () => {
		const result = await scanSource(
			"case-285.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-286 safe aes-256-cbc wrap0", async () => {
		const result = await scanSource(
			"case-286.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-287 safe aes-256-cbc wrap1", async () => {
		const result = await scanSource(
			"case-287.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-288 safe aes-256-cbc wrap2", async () => {
		const result = await scanSource(
			"case-288.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-289 safe aes-256-cbc wrap3", async () => {
		const result = await scanSource(
			"case-289.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-290 safe aes-256-cbc wrap4", async () => {
		const result = await scanSource(
			"case-290.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-256-cbc", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-291 safe aes-128-gcm wrap0", async () => {
		const result = await scanSource(
			"case-291.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-292 safe aes-128-gcm wrap1", async () => {
		const result = await scanSource(
			"case-292.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-293 safe aes-128-gcm wrap2", async () => {
		const result = await scanSource(
			"case-293.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-294 safe aes-128-gcm wrap3", async () => {
		const result = await scanSource(
			"case-294.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-295 safe aes-128-gcm wrap4", async () => {
		const result = await scanSource(
			"case-295.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("aes-128-gcm", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-296 safe chacha20-poly1305 wrap0", async () => {
		const result = await scanSource(
			"case-296.ts",
			'"use strict";\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-297 safe chacha20-poly1305 wrap1", async () => {
		const result = await scanSource(
			"case-297.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n\nexport const VERSION = 1;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-298 safe chacha20-poly1305 wrap2", async () => {
		const result = await scanSource(
			"case-298.ts",
			'// password hashing module\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-299 safe chacha20-poly1305 wrap3", async () => {
		const result = await scanSource(
			"case-299.ts",
			'import { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n\nexport const __module = true;\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});

	it("CS-V131-ENC-300 safe chacha20-poly1305 wrap4", async () => {
		const result = await scanSource(
			"case-300.ts",
			'/* crypto */\nimport { createCipheriv, randomBytes } from "crypto";\nexport function enc(d: Buffer, k: Buffer) {\n  return createCipheriv("chacha20-poly1305", k, randomBytes(16));\n}\n',
		);
		const expected = {};
		for (const [ruleId, count] of Object.entries(expected)) {
			expect(countRule(result.findings, ruleId)).toBe(count);
		}
		if (Object.keys(expected).length === 0) {
			expect(result.findings).toEqual([]);
		} else {
			const expectedTotal = Object.values(expected).reduce((a, b) => a + b, 0);
			expect(result.findings.length).toBeGreaterThanOrEqual(expectedTotal);
		}
	});
});
