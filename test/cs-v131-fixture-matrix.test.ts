// Auto-generated by scripts/generate-v131-tests.mjs
import path from "node:path";
import { fileURLToPath } from "node:url";
import { describe, expect, it } from "vitest";
import { scan } from "ciphersins";
import { countRule } from "./helpers/v131-scan-source.js";

const testDir = path.dirname(fileURLToPath(import.meta.url));
const rootDir = path.resolve(testDir, "..");

describe("CS-V131 per-fixture file matrix", () => {
	it("CS-V131-FIX-001 CS-CMP-01 bad argon2-import-token-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/argon2-import-token-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "argon2-import-token-compare.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-002 CS-CMP-01 bad bcrypt-import-token-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/bcrypt-import-token-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "bcrypt-import-token-compare.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-003 CS-CMP-01 bad hash-compare-with-crypto-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/hash-compare-with-crypto-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "hash-compare-with-crypto-import.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-004 CS-CMP-01 bad multiple-auth-compares.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/multiple-auth-compares.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "multiple-auth-compares.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-005 CS-CMP-01 bad not-equal-operator.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/not-equal-operator.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "not-equal-operator.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-006 CS-CMP-01 bad optional-chaining-secret.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/optional-chaining-secret.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "optional-chaining-secret.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-007 CS-CMP-01 bad otp-string-literal-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/otp-string-literal-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "otp-string-literal-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-008 CS-CMP-01 bad password-loose-equal.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/password-loose-equal.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "password-loose-equal.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-009 CS-CMP-01 bad property-access-secret.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/property-access-secret.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "property-access-secret.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-010 CS-CMP-01 bad require-crypto-token-compare.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/require-crypto-token-compare.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "require-crypto-token-compare.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-011 CS-CMP-01 bad secret-not-equal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/secret-not-equal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "secret-not-equal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-012 CS-CMP-01 bad signature-header-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/signature-header-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "signature-header-compare.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-013 CS-CMP-01 bad token-compare-in-ternary.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/token-compare-in-ternary.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-compare-in-ternary.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-014 CS-CMP-01 bad token-compare-in-while.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/token-compare-in-while.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-compare-in-while.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-015 CS-CMP-01 bad token-compare-template-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/token-compare-template-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-compare-template-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-016 CS-CMP-01 bad token-not-strict-equal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/token-not-strict-equal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-not-strict-equal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-017 CS-CMP-01 bad token-strict-equal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/bad/token-strict-equal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-strict-equal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-018 CS-CMP-01 good author-vs-publisher.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/author-vs-publisher.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "author-vs-publisher.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-019 CS-CMP-01 good compare-protocol-string.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/compare-protocol-string.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "compare-protocol-string.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-020 CS-CMP-01 good compare-timing-safe-result.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/compare-timing-safe-result.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "compare-timing-safe-result.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-021 CS-CMP-01 good compare-with-typeof.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/compare-with-typeof.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "compare-with-typeof.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-022 CS-CMP-01 good deep-equal-helper.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/deep-equal-helper.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "deep-equal-helper.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-023 CS-CMP-01 good hashmap-key-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/hashmap-key-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "hashmap-key-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-024 CS-CMP-01 good numeric-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/numeric-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "numeric-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-025 CS-CMP-01 good timing-safe-equal-crypto.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/timing-safe-equal-crypto.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "timing-safe-equal-crypto.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-026 CS-CMP-01 good timing-safe-equal-named-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/timing-safe-equal-named-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "timing-safe-equal-named-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-027 CS-CMP-01 good timing-safe-equal-node-crypto.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/timing-safe-equal-node-crypto.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "timing-safe-equal-node-crypto.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-028 CS-CMP-01 good token-compare-no-crypto-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/token-compare-no-crypto-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-compare-no-crypto-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-029 CS-CMP-01 good token-equals-null.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/token-equals-null.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "token-equals-null.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-030 CS-CMP-01 good type-only-crypto-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/type-only-crypto-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "type-only-crypto-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-031 CS-CMP-01 good ui-auth-state-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/ui-auth-state-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "ui-auth-state-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-032 CS-CMP-01 good ui-label-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/ui-label-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "ui-label-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-033 CS-CMP-01 good username-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/username-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "username-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-034 CS-CMP-01 good verify-only-no-compare.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-cmp-01/good/verify-only-no-compare.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-CMP-01");
		const fileName = "verify-only-no-compare.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-035 CS-DEC-01 bad create-cipher-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/bad/create-cipher-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "create-cipher-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-036 CS-DEC-01 bad create-decipher-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/bad/create-decipher-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "create-decipher-alias.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-037 CS-DEC-01 bad create-decipher-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/bad/create-decipher-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "create-decipher-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-038 CS-DEC-01 bad node-crypto-create-decipher.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/bad/node-crypto-create-decipher.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "node-crypto-create-decipher.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-039 CS-DEC-01 bad require-create-decipher.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/bad/require-create-decipher.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "require-create-decipher.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-040 CS-DEC-01 good create-cipheriv-explicit.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/good/create-cipheriv-explicit.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "create-cipheriv-explicit.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-041 CS-DEC-01 good create-decipheriv-explicit.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/good/create-decipheriv-explicit.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "create-decipheriv-explicit.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-042 CS-DEC-01 good no-crypto.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/good/no-crypto.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "no-crypto.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-043 CS-DEC-01 good node-crypto-decipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-dec-01/good/node-crypto-decipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-DEC-01");
		const fileName = "node-crypto-decipheriv.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-044 CS-ENC-01 bad cipheriv-buffer-from-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/cipheriv-buffer-from-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-buffer-from-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-045 CS-ENC-01 bad cipheriv-hardcoded-iv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/cipheriv-hardcoded-iv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-hardcoded-iv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-046 CS-ENC-01 bad cipheriv-hardcoded-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/cipheriv-hardcoded-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-hardcoded-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-047 CS-ENC-01 bad decipheriv-hardcoded-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/decipheriv-hardcoded-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "decipheriv-hardcoded-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-048 CS-ENC-01 bad gcm-hardcoded-key-with-options.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/gcm-hardcoded-key-with-options.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "gcm-hardcoded-key-with-options.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-049 CS-ENC-01 bad namespace-crypto-cipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/namespace-crypto-cipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "namespace-crypto-cipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-050 CS-ENC-01 bad node-crypto-hardcoded-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/node-crypto-hardcoded-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "node-crypto-hardcoded-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-051 CS-ENC-01 bad require-destructured-cipheriv.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/bad/require-destructured-cipheriv.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "require-destructured-cipheriv.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-052 CS-ENC-01 good cipheriv-env-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/cipheriv-env-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-env-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-053 CS-ENC-01 good cipheriv-param-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/cipheriv-param-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-param-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-054 CS-ENC-01 good cipheriv-random-iv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/cipheriv-random-iv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "cipheriv-random-iv.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-055 CS-ENC-01 good no-cipher-calls.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/no-cipher-calls.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "no-cipher-calls.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-056 CS-ENC-01 good no-crypto-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/no-crypto-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "no-crypto-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-057 CS-ENC-01 good node-crypto-env-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-01/good/node-crypto-env-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-01");
		const fileName = "node-crypto-env-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-058 CS-ENC-02 bad gcm-buffer-from-static.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/bad/gcm-buffer-from-static.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-buffer-from-static.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-059 CS-ENC-02 bad gcm-reused-iv-twice.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/bad/gcm-reused-iv-twice.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-reused-iv-twice.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-060 CS-ENC-02 bad gcm-static-iv-and-hardcoded-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/bad/gcm-static-iv-and-hardcoded-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-static-iv-and-hardcoded-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-061 CS-ENC-02 bad gcm-static-iv-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/bad/gcm-static-iv-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-static-iv-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-062 CS-ENC-02 bad node-crypto-gcm-static.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/bad/node-crypto-gcm-static.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "node-crypto-gcm-static.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-063 CS-ENC-02 good gcm-iv-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/good/gcm-iv-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-iv-variable.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-064 CS-ENC-02 good gcm-random-iv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/good/gcm-random-iv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "gcm-random-iv.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-065 CS-ENC-02 good node-crypto-gcm-random.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-02/good/node-crypto-gcm-random.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-02");
		const fileName = "node-crypto-gcm-random.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-066 CS-ENC-03 bad bf-cipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/bad/bf-cipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "bf-cipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-067 CS-ENC-03 bad cast5-decipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/bad/cast5-decipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "cast5-decipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-068 CS-ENC-03 bad des-cbc-cipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/bad/des-cbc-cipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "des-cbc-cipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-069 CS-ENC-03 bad des-hardcoded-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/bad/des-hardcoded-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "des-hardcoded-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-070 CS-ENC-03 bad rc4-cipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/bad/rc4-cipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "rc4-cipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-071 CS-ENC-03 good aes-256-cbc.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/good/aes-256-cbc.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "aes-256-cbc.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-072 CS-ENC-03 good aes-256-gcm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/good/aes-256-gcm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "aes-256-gcm.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-073 CS-ENC-03 good chacha20-poly1305.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/good/chacha20-poly1305.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "chacha20-poly1305.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-074 CS-ENC-03 good node-crypto-aes.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/good/node-crypto-aes.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "node-crypto-aes.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-075 CS-ENC-03 good variable-algorithm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-03/good/variable-algorithm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-03");
		const fileName = "variable-algorithm.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-076 CS-ENC-04 bad aes-128-ecb-cipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/bad/aes-128-ecb-cipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "aes-128-ecb-cipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-077 CS-ENC-04 bad aes-256-ecb-decipheriv.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/bad/aes-256-ecb-decipheriv.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "aes-256-ecb-decipheriv.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-078 CS-ENC-04 bad ecb-require.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/bad/ecb-require.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "ecb-require.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-079 CS-ENC-04 bad node-crypto-ecb.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/bad/node-crypto-ecb.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "node-crypto-ecb.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-080 CS-ENC-04 good aes-256-cbc.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/good/aes-256-cbc.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "aes-256-cbc.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-081 CS-ENC-04 good aes-256-gcm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/good/aes-256-gcm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "aes-256-gcm.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-082 CS-ENC-04 good variable-algorithm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-enc-04/good/variable-algorithm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-ENC-04");
		const fileName = "variable-algorithm.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-083 CS-HASH-01 bad bcrypt-import-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/bcrypt-import-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "bcrypt-import-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-084 CS-HASH-01 bad case-insensitive-algorithm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/case-insensitive-algorithm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "case-insensitive-algorithm.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-085 CS-HASH-01 bad chained-digest-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/chained-digest-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "chained-digest-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-086 CS-HASH-01 bad create-hash-md4-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hash-md4-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hash-md4-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-087 CS-HASH-01 bad create-hash-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hash-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hash-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-088 CS-HASH-01 bad create-hash-ripemd160-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hash-ripemd160-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hash-ripemd160-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-089 CS-HASH-01 bad create-hash-sha-1-hyphen.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hash-sha-1-hyphen.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hash-sha-1-hyphen.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-090 CS-HASH-01 bad create-hash-sha1-param.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hash-sha1-param.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hash-sha1-param.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-091 CS-HASH-01 bad create-hmac-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/create-hmac-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "create-hmac-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-092 CS-HASH-01 bad destructuring-require-md5.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/destructuring-require-md5.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "destructuring-require-md5.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-093 CS-HASH-01 bad getter-password-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/getter-password-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "getter-password-hash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-094 CS-HASH-01 bad hash-credential-md5.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/hash-credential-md5.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "hash-credential-md5.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-095 CS-HASH-01 bad hash-password.jsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/hash-password.jsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "hash-password.jsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-096 CS-HASH-01 bad hashed-password-binding.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/hashed-password-binding.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "hashed-password-binding.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-097 CS-HASH-01 bad inline-require-md5.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/inline-require-md5.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "inline-require-md5.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-098 CS-HASH-01 bad js-sha1-package-import.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/js-sha1-package-import.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "js-sha1-package-import.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-099 CS-HASH-01 bad md5-package-import.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/md5-package-import.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "md5-package-import.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-100 CS-HASH-01 bad method-in-class.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/method-in-class.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "method-in-class.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-101 CS-HASH-01 bad module-level-password-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/module-level-password-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "module-level-password-hash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-102 CS-HASH-01 bad multiple-weak-hashes.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/multiple-weak-hashes.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "multiple-weak-hashes.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-103 CS-HASH-01 bad named-import-createHash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/named-import-createHash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "named-import-createHash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-104 CS-HASH-01 bad namespace-import-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/namespace-import-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "namespace-import-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-105 CS-HASH-01 bad nested-arrow-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/nested-arrow-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "nested-arrow-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-106 CS-HASH-01 bad node-crypto-sha1.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/node-crypto-sha1.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "node-crypto-sha1.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-107 CS-HASH-01 bad pbkdf2-async-sha1-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/pbkdf2-async-sha1-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "pbkdf2-async-sha1-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-108 CS-HASH-01 bad pbkdf2-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/pbkdf2-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "pbkdf2-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-109 CS-HASH-01 bad property-access-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/property-access-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "property-access-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-110 CS-HASH-01 bad require-crypto-md5.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/require-crypto-md5.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "require-crypto-md5.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-111 CS-HASH-01 bad require-sha1-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/require-sha1-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "require-sha1-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-112 CS-HASH-01 bad sha1-package-import.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/sha1-package-import.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "sha1-package-import.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-113 CS-HASH-01 bad store-password-md5.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/bad/store-password-md5.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "store-password-md5.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-114 CS-HASH-01 good argon2-hash-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/argon2-hash-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "argon2-hash-password.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-115 CS-HASH-01 good bcrypt-hash-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/bcrypt-hash-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "bcrypt-hash-password.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-116 CS-HASH-01 good dynamic-algorithm-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/dynamic-algorithm-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "dynamic-algorithm-variable.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-117 CS-HASH-01 good hash-code-ui.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/hash-code-ui.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "hash-code-ui.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-118 CS-HASH-01 good local-createHash-stub.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/local-createHash-stub.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "local-createHash-stub.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-119 CS-HASH-01 good md5-file-checksum.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/md5-file-checksum.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "md5-file-checksum.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-120 CS-HASH-01 good pbkdf2-md5-no-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/pbkdf2-md5-no-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "pbkdf2-md5-no-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-121 CS-HASH-01 good pbkdf2-sha256-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/pbkdf2-sha256-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "pbkdf2-sha256-password.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-122 CS-HASH-01 good scrypt-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/scrypt-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "scrypt-password.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-123 CS-HASH-01 good sha1-cache-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/sha1-cache-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "sha1-cache-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-124 CS-HASH-01 good sha256-password-storage.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/sha256-password-storage.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "sha256-password-storage.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-125 CS-HASH-01 good sha512-named-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/sha512-named-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "sha512-named-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-126 CS-HASH-01 good type-only-crypto-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/type-only-crypto-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "type-only-crypto-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-127 CS-HASH-01 good verify-only-no-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-01/good/verify-only-no-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-01");
		const fileName = "verify-only-no-hash.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-128 CS-HASH-02 bad bcrypt-and-md5-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/bcrypt-and-md5-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "bcrypt-and-md5-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-129 CS-HASH-02 bad bcrypt-hash-cost-9.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/bcrypt-hash-cost-9.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "bcrypt-hash-cost-9.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-130 CS-HASH-02 bad bcryptjs-weak-cost.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/bcryptjs-weak-cost.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "bcryptjs-weak-cost.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-131 CS-HASH-02 bad destructuring-require-hash.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/destructuring-require-hash.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "destructuring-require-hash.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-132 CS-HASH-02 bad gen-salt-callback-weak.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/gen-salt-callback-weak.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-callback-weak.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-133 CS-HASH-02 bad gen-salt-sync-weak.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/gen-salt-sync-weak.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-sync-weak.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-134 CS-HASH-02 bad gen-salt-weak.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/gen-salt-weak.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-weak.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-135 CS-HASH-02 bad getter-password-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/getter-password-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "getter-password-hash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-136 CS-HASH-02 bad hash-async-callback-weak.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-async-callback-weak.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-async-callback-weak.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-137 CS-HASH-02 bad hash-async-cost-4.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-async-cost-4.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-async-cost-4.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-138 CS-HASH-02 bad hash-const-rounds-8.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-const-rounds-8.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-const-rounds-8.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-139 CS-HASH-02 bad hash-password.jsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-password.jsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-password.jsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-140 CS-HASH-02 bad hash-sync-cost-8.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-sync-cost-8.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-sync-cost-8.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-141 CS-HASH-02 bad hash-sync-cost-hex.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-sync-cost-hex.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-sync-cost-hex.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-142 CS-HASH-02 bad hash-sync-cost-zero.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hash-sync-cost-zero.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-sync-cost-zero.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-143 CS-HASH-02 bad hashed-password-binding.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/hashed-password-binding.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hashed-password-binding.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-144 CS-HASH-02 bad inline-require-hash.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/inline-require-hash.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "inline-require-hash.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-145 CS-HASH-02 bad method-in-class.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/method-in-class.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "method-in-class.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-146 CS-HASH-02 bad module-level-password-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/module-level-password-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "module-level-password-hash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-147 CS-HASH-02 bad multiple-weak-bcrypt.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/multiple-weak-bcrypt.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "multiple-weak-bcrypt.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-148 CS-HASH-02 bad named-import-hash-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/named-import-hash-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "named-import-hash-alias.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-149 CS-HASH-02 bad named-import-hashSync.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/named-import-hashSync.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "named-import-hashSync.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-150 CS-HASH-02 bad nested-arrow-password.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/nested-arrow-password.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "nested-arrow-password.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-151 CS-HASH-02 bad node-rs-bcrypt-weak-cost.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/node-rs-bcrypt-weak-cost.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "node-rs-bcrypt-weak-cost.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-152 CS-HASH-02 bad require-bcrypt-hash.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/require-bcrypt-hash.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "require-bcrypt-hash.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-153 CS-HASH-02 bad require-bcryptjs-hash.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/require-bcryptjs-hash.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "require-bcryptjs-hash.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-154 CS-HASH-02 bad store-password-weak.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/store-password-weak.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "store-password-weak.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-155 CS-HASH-02 bad weak-gen-salt-then-strong-hash.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/bad/weak-gen-salt-then-strong-hash.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "weak-gen-salt-then-strong-hash.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-156 CS-HASH-02 good argon2-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/argon2-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "argon2-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-157 CS-HASH-02 good async-hash-with-callback.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/async-hash-with-callback.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "async-hash-with-callback.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-158 CS-HASH-02 good compare-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/compare-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "compare-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-159 CS-HASH-02 good compare-with-weak-hash-naming.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/compare-with-weak-hash-naming.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "compare-with-weak-hash-naming.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-160 CS-HASH-02 good gen-salt-cost-9-not-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/gen-salt-cost-9-not-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-cost-9-not-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-161 CS-HASH-02 good gen-salt-no-args.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/gen-salt-no-args.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-no-args.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-162 CS-HASH-02 good gen-salt-strong.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/gen-salt-strong.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "gen-salt-strong.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-163 CS-HASH-02 good hash-cost-10-boundary.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/hash-cost-10-boundary.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-cost-10-boundary.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-164 CS-HASH-02 good hash-cost-12.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/hash-cost-12.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-cost-12.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-165 CS-HASH-02 good hash-no-cost-uses-default.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/hash-no-cost-uses-default.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-no-cost-uses-default.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-166 CS-HASH-02 good hash-with-salt-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/hash-with-salt-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-with-salt-literal.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-167 CS-HASH-02 good hash-with-salt-string.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/hash-with-salt-string.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "hash-with-salt-string.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-168 CS-HASH-02 good local-hash-stub.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/local-hash-stub.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "local-hash-stub.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-169 CS-HASH-02 good namespace-import-strong.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/namespace-import-strong.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "namespace-import-strong.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-170 CS-HASH-02 good no-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/no-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "no-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-171 CS-HASH-02 good node-rs-bcrypt-untracked.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/node-rs-bcrypt-untracked.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "node-rs-bcrypt-untracked.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-172 CS-HASH-02 good rounds-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/rounds-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "rounds-variable.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-173 CS-HASH-02 good sha256-password-no-bcrypt.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/sha256-password-no-bcrypt.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "sha256-password-no-bcrypt.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-174 CS-HASH-02 good type-only-bcrypt-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-02/good/type-only-bcrypt-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-02");
		const fileName = "type-only-bcrypt-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-175 CS-HASH-03 bad pbkdf2-async-low.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/bad/pbkdf2-async-low.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-async-low.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-176 CS-HASH-03 bad pbkdf2-boundary-99999.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/bad/pbkdf2-boundary-99999.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-boundary-99999.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-177 CS-HASH-03 bad pbkdf2-low-iterations-sha256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/bad/pbkdf2-low-iterations-sha256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-low-iterations-sha256.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-178 CS-HASH-03 bad pbkdf2-sync-4096.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/bad/pbkdf2-sync-4096.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-sync-4096.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-179 CS-HASH-03 bad pbkdf2-variable-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/bad/pbkdf2-variable-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-variable-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-180 CS-HASH-03 good bcrypt-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/good/bcrypt-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "bcrypt-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-181 CS-HASH-03 good pbkdf2-100k-sha256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/good/pbkdf2-100k-sha256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-100k-sha256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-182 CS-HASH-03 good pbkdf2-above-min.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/good/pbkdf2-above-min.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-above-min.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-183 CS-HASH-03 good pbkdf2-indirect-config.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/good/pbkdf2-indirect-config.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-indirect-config.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-184 CS-HASH-03 good pbkdf2-low-no-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-03/good/pbkdf2-low-no-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-03");
		const fileName = "pbkdf2-low-no-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-185 CS-HASH-04 bad scrypt-async-low.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/bad/scrypt-async-low.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-async-low.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-186 CS-HASH-04 bad scrypt-low-blocksize.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/bad/scrypt-low-blocksize.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-low-blocksize.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-187 CS-HASH-04 bad scrypt-low-cost.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/bad/scrypt-low-cost.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-low-cost.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-188 CS-HASH-04 bad scrypt-low-parallelization.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/bad/scrypt-low-parallelization.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-low-parallelization.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-189 CS-HASH-04 bad scrypt-variable-cost.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/bad/scrypt-variable-cost.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-variable-cost.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-190 CS-HASH-04 good bcrypt-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/bcrypt-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "bcrypt-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-191 CS-HASH-04 good scrypt-async-callback-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/scrypt-async-callback-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-async-callback-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-192 CS-HASH-04 good scrypt-default-no-options.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/scrypt-default-no-options.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-default-no-options.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-193 CS-HASH-04 good scrypt-explicit-16384.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/scrypt-explicit-16384.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-explicit-16384.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-194 CS-HASH-04 good scrypt-indirect-config.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/scrypt-indirect-config.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-indirect-config.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-195 CS-HASH-04 good scrypt-no-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-04/good/scrypt-no-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-04");
		const fileName = "scrypt-no-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-196 CS-HASH-05 bad argon2-low-memory.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/bad/argon2-low-memory.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-low-memory.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-197 CS-HASH-05 bad argon2-low-timecost.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/bad/argon2-low-timecost.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-low-timecost.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-198 CS-HASH-05 bad argon2-sync-low.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/bad/argon2-sync-low.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-sync-low.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-199 CS-HASH-05 bad argon2-variable-timecost.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/bad/argon2-variable-timecost.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-variable-timecost.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-200 CS-HASH-05 bad node-rs-argon2-low.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/bad/node-rs-argon2-low.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "node-rs-argon2-low.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-201 CS-HASH-05 good argon2-above-min.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/good/argon2-above-min.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-above-min.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-202 CS-HASH-05 good argon2-indirect-config.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/good/argon2-indirect-config.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-indirect-config.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-203 CS-HASH-05 good argon2-low-no-password-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/good/argon2-low-no-password-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-low-no-password-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-204 CS-HASH-05 good argon2-recommended.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/good/argon2-recommended.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "argon2-recommended.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-205 CS-HASH-05 good bcrypt-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-hash-05/good/bcrypt-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-HASH-05");
		const fileName = "bcrypt-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-206 CS-JWT-01 bad decode-and-verify-separated-functions.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/decode-and-verify-separated-functions.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-and-verify-separated-functions.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-207 CS-JWT-01 bad decode-in-component.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/decode-in-component.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-in-component.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-208 CS-JWT-01 bad decode-only-with-type-annotation.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/decode-only-with-type-annotation.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-only-with-type-annotation.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-209 CS-JWT-01 bad decode-reexport-no-verify-call.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/decode-reexport-no-verify-call.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-reexport-no-verify-call.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-210 CS-JWT-01 bad decode-via-local-wrapper.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/decode-via-local-wrapper.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-via-local-wrapper.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-211 CS-JWT-01 bad default-import-decode-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/default-import-decode-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "default-import-decode-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-212 CS-JWT-01 bad inline-require-decode-only.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/inline-require-decode-only.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "inline-require-decode-only.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-213 CS-JWT-01 bad multiple-decode-no-verify.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/multiple-decode-no-verify.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "multiple-decode-no-verify.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-214 CS-JWT-01 bad named-import-decode-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/named-import-decode-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "named-import-decode-alias.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-215 CS-JWT-01 bad named-import-decode-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/named-import-decode-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "named-import-decode-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-216 CS-JWT-01 bad named-import-verify-unused.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/named-import-verify-unused.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "named-import-verify-unused.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-217 CS-JWT-01 bad namespace-import-decode-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/namespace-import-decode-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "namespace-import-decode-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-218 CS-JWT-01 bad optional-chaining-decode.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/optional-chaining-decode.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "optional-chaining-decode.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-219 CS-JWT-01 bad require-decode-only.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/require-decode-only.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "require-decode-only.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-220 CS-JWT-01 bad require-destructured-decode-only.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/require-destructured-decode-only.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "require-destructured-decode-only.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-221 CS-JWT-01 bad verify-in-comment-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/verify-in-comment-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-in-comment-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-222 CS-JWT-01 bad verify-in-unrelated-helper.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/bad/verify-in-unrelated-helper.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-in-unrelated-helper.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-223 CS-JWT-01 good decode-and-verify-default.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/decode-and-verify-default.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-and-verify-default.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-224 CS-JWT-01 good decode-and-verify-named.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/decode-and-verify-named.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-and-verify-named.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-225 CS-JWT-01 good decode-in-comment.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/decode-in-comment.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-in-comment.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-226 CS-JWT-01 good decode-in-string-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/decode-in-string-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-in-string-literal.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-227 CS-JWT-01 good decode-with-reexport-verify.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/decode-with-reexport-verify.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "decode-with-reexport-verify.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-228 CS-JWT-01 good dynamic-import-decode.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/dynamic-import-decode.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "dynamic-import-decode.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-229 CS-JWT-01 good indirect-decode-ref.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/indirect-decode-ref.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "indirect-decode-ref.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-230 CS-JWT-01 good inline-require-verify.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/inline-require-verify.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "inline-require-verify.js";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-231 CS-JWT-01 good named-import-verify-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/named-import-verify-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "named-import-verify-alias.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-232 CS-JWT-01 good no-jsonwebtoken.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/no-jsonwebtoken.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "no-jsonwebtoken.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-233 CS-JWT-01 good sign-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/sign-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "sign-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-234 CS-JWT-01 good type-only-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/type-only-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "type-only-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-235 CS-JWT-01 good verify-in-dead-code-unreachable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/verify-in-dead-code-unreachable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-in-dead-code-unreachable.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-236 CS-JWT-01 good verify-in-direct-callee.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/verify-in-direct-callee.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-in-direct-callee.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-237 CS-JWT-01 good verify-in-nested-function.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/verify-in-nested-function.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-in-nested-function.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-238 CS-JWT-01 good verify-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-01/good/verify-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-01");
		const fileName = "verify-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-239 CS-JWT-02 bad decode-and-verify-no-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/decode-and-verify-no-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "decode-and-verify-no-algorithms.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-240 CS-JWT-02 bad destructuring-require-verify.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/destructuring-require-verify.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "destructuring-require-verify.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-241 CS-JWT-02 bad inline-require-verify.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/inline-require-verify.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "inline-require-verify.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-242 CS-JWT-02 bad method-in-class.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/method-in-class.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "method-in-class.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-243 CS-JWT-02 bad multiple-weak-verify.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/multiple-weak-verify.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "multiple-weak-verify.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-244 CS-JWT-02 bad namespace-verify-no-alg.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/namespace-verify-no-alg.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "namespace-verify-no-alg.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-245 CS-JWT-02 bad require-verify.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/require-verify.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "require-verify.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-246 CS-JWT-02 bad verify-algorithms-null.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-algorithms-null.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-null.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-247 CS-JWT-02 bad verify-algorithms-undefined.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-algorithms-undefined.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-undefined.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-248 CS-JWT-02 bad verify-empty-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-empty-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-empty-algorithms.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-249 CS-JWT-02 bad verify-explicit-undefined-options.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-explicit-undefined-options.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-explicit-undefined-options.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-250 CS-JWT-02 bad verify-four-args-no-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-four-args-no-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-four-args-no-algorithms.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-251 CS-JWT-02 bad verify-ignore-expiration-no-alg.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-ignore-expiration-no-alg.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-ignore-expiration-no-alg.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-252 CS-JWT-02 bad verify-in-dead-code.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-in-dead-code.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-in-dead-code.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-253 CS-JWT-02 bad verify-in-nested-function.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-in-nested-function.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-in-nested-function.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-254 CS-JWT-02 bad verify-in-two-functions.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-in-two-functions.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-in-two-functions.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-255 CS-JWT-02 bad verify-named-import-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-named-import-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-named-import-alias.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-256 CS-JWT-02 bad verify-optional-chaining.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-optional-chaining.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-optional-chaining.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-257 CS-JWT-02 bad verify-other-options-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-other-options-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-other-options-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-258 CS-JWT-02 bad verify-public-key-rs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-public-key-rs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-public-key-rs256.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-259 CS-JWT-02 bad verify-shorthand-empty-algorithms-var.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-shorthand-empty-algorithms-var.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-shorthand-empty-algorithms-var.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-260 CS-JWT-02 bad verify-spread-options-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-spread-options-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-spread-options-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-261 CS-JWT-02 bad verify-sync-secret-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-sync-secret-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-sync-secret-variable.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-262 CS-JWT-02 bad verify-two-args-default.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-two-args-default.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-two-args-default.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-263 CS-JWT-02 bad verify-type-only-verify-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-type-only-verify-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-type-only-verify-import.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-264 CS-JWT-02 bad verify-with-callback.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-with-callback.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-with-callback.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-265 CS-JWT-02 bad verify-wrong-option-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/bad/verify-wrong-option-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-wrong-option-key.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-266 CS-JWT-02 good indirect-verify-ref.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/indirect-verify-ref.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "indirect-verify-ref.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-267 CS-JWT-02 good inline-require-with-algorithms.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/inline-require-with-algorithms.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "inline-require-with-algorithms.js";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-268 CS-JWT-02 good jose-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/jose-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "jose-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-269 CS-JWT-02 good local-verify-stub.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/local-verify-stub.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "local-verify-stub.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-270 CS-JWT-02 good named-import-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/named-import-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "named-import-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-271 CS-JWT-02 good namespace-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/namespace-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "namespace-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-272 CS-JWT-02 good no-jsonwebtoken.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/no-jsonwebtoken.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "no-jsonwebtoken.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-273 CS-JWT-02 good require-with-algorithms.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/require-with-algorithms.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "require-with-algorithms.js";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-274 CS-JWT-02 good sign-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/sign-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "sign-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-275 CS-JWT-02 good type-only-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/type-only-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "type-only-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-276 CS-JWT-02 good verify-algorithms-asymmetric.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-asymmetric.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-asymmetric.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-277 CS-JWT-02 good verify-algorithms-hs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-hs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-hs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-278 CS-JWT-02 good verify-algorithms-multiple.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-multiple.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-multiple.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-279 CS-JWT-02 good verify-algorithms-quoted-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-quoted-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-quoted-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-280 CS-JWT-02 good verify-algorithms-rs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-rs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-rs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-281 CS-JWT-02 good verify-algorithms-variable-ref.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-algorithms-variable-ref.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-algorithms-variable-ref.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-282 CS-JWT-02 good verify-callback-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-callback-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-callback-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-283 CS-JWT-02 good verify-computed-algorithms-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-computed-algorithms-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-computed-algorithms-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-284 CS-JWT-02 good verify-optional-chaining-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-optional-chaining-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-optional-chaining-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-285 CS-JWT-02 good verify-options-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-options-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-options-variable.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-286 CS-JWT-02 good verify-shorthand-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-shorthand-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-shorthand-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-287 CS-JWT-02 good verify-spread-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-spread-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-spread-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-288 CS-JWT-02 good verify-third-arg-string-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-third-arg-string-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-third-arg-string-literal.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-289 CS-JWT-02 good verify-with-complete-and-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-02/good/verify-with-complete-and-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-02");
		const fileName = "verify-with-complete-and-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-290 CS-JWT-03 bad decode-and-verify-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/decode-and-verify-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "decode-and-verify-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-291 CS-JWT-03 bad destructuring-require-verify-none.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/destructuring-require-verify-none.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "destructuring-require-verify-none.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-292 CS-JWT-03 bad inline-require-verify-none.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/inline-require-verify-none.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "inline-require-verify-none.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-293 CS-JWT-03 bad method-in-class-sign-none.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/method-in-class-sign-none.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "method-in-class-sign-none.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-294 CS-JWT-03 bad multiple-none-verify.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/multiple-none-verify.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "multiple-none-verify.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-295 CS-JWT-03 bad namespace-verify-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/namespace-verify-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "namespace-verify-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-296 CS-JWT-03 bad require-verify-none.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/require-verify-none.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "require-verify-none.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-297 CS-JWT-03 bad sign-algorithm-none-mixed-case.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-algorithm-none-mixed-case.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-algorithm-none-mixed-case.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-298 CS-JWT-03 bad sign-algorithm-none-uppercase.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-algorithm-none-uppercase.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-algorithm-none-uppercase.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-299 CS-JWT-03 bad sign-algorithm-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-algorithm-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-algorithm-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-300 CS-JWT-03 bad sign-and-verify-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-and-verify-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-and-verify-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-301 CS-JWT-03 bad sign-named-import-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-named-import-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-named-import-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-302 CS-JWT-03 bad sign-none-algorithm-via-variable.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-none-algorithm-via-variable.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-none-algorithm-via-variable.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-303 CS-JWT-03 bad sign-with-callback-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/sign-with-callback-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-with-callback-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-304 CS-JWT-03 bad verify-algorithms-none-and-hs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-algorithms-none-and-hs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-none-and-hs256.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-305 CS-JWT-03 bad verify-algorithms-none-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-algorithms-none-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-none-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-306 CS-JWT-03 bad verify-algorithms-none-quoted.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-algorithms-none-quoted.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-none-quoted.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-307 CS-JWT-03 bad verify-algorithms-none-uppercase.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-algorithms-none-uppercase.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-none-uppercase.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-308 CS-JWT-03 bad verify-computed-algorithms-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-computed-algorithms-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-computed-algorithms-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-309 CS-JWT-03 bad verify-four-args-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-four-args-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-four-args-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-310 CS-JWT-03 bad verify-named-import-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-named-import-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-named-import-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-311 CS-JWT-03 bad verify-none-and-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-none-and-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-none-and-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-312 CS-JWT-03 bad verify-none-via-variable-options.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-none-via-variable-options.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-none-via-variable-options.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-313 CS-JWT-03 bad verify-optional-chaining-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-optional-chaining-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-optional-chaining-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-314 CS-JWT-03 bad verify-spread-with-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-spread-with-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-spread-with-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-315 CS-JWT-03 bad verify-type-only-import-with-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-type-only-import-with-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-type-only-import-with-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-316 CS-JWT-03 bad verify-with-callback-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/bad/verify-with-callback-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-with-callback-none.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-317 CS-JWT-03 good indirect-verify-ref.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/indirect-verify-ref.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "indirect-verify-ref.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-318 CS-JWT-03 good jose-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/jose-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "jose-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-319 CS-JWT-03 good local-sign-stub.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/local-sign-stub.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "local-sign-stub.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-320 CS-JWT-03 good no-jsonwebtoken.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/no-jsonwebtoken.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "no-jsonwebtoken.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-321 CS-JWT-03 good sign-algorithm-hs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/sign-algorithm-hs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-algorithm-hs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-322 CS-JWT-03 good sign-default-two-arg.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/sign-default-two-arg.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "sign-default-two-arg.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-323 CS-JWT-03 good type-only-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/type-only-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "type-only-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-324 CS-JWT-03 good verify-algorithms-dynamic-element.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-algorithms-dynamic-element.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-dynamic-element.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-325 CS-JWT-03 good verify-algorithms-hs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-algorithms-hs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-hs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-326 CS-JWT-03 good verify-algorithms-multiple-safe.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-algorithms-multiple-safe.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-multiple-safe.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-327 CS-JWT-03 good verify-algorithms-none-template-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-algorithms-none-template-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-none-template-literal.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-328 CS-JWT-03 good verify-algorithms-rs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-algorithms-rs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-algorithms-rs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-329 CS-JWT-03 good verify-options-variable-none.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-options-variable-none.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-options-variable-none.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-330 CS-JWT-03 good verify-shorthand-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-shorthand-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-shorthand-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-331 CS-JWT-03 good verify-wrong-key-algorithm.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-03/good/verify-wrong-key-algorithm.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-03");
		const fileName = "verify-wrong-key-algorithm.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-332 CS-JWT-04 bad method-in-class-ignore-expiration.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/method-in-class-ignore-expiration.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "method-in-class-ignore-expiration.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-333 CS-JWT-04 bad multiple-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/multiple-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "multiple-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-334 CS-JWT-04 bad namespace-verify-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/namespace-verify-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "namespace-verify-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-335 CS-JWT-04 bad require-verify-ignore-expiration.js", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/require-verify-ignore-expiration.js")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "require-verify-ignore-expiration.js";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-336 CS-JWT-04 bad verify-algorithms-and-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-algorithms-and-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-algorithms-and-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-337 CS-JWT-04 bad verify-ignore-expiration-callback.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-ignore-expiration-callback.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-callback.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-338 CS-JWT-04 bad verify-ignore-expiration-complete.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-ignore-expiration-complete.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-complete.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-339 CS-JWT-04 bad verify-ignore-expiration-double-negation.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-ignore-expiration-double-negation.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-double-negation.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-340 CS-JWT-04 bad verify-ignore-expiration-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-ignore-expiration-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-341 CS-JWT-04 bad verify-ignore-expiration-truthy-number.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-ignore-expiration-truthy-number.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-truthy-number.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-342 CS-JWT-04 bad verify-in-dead-code-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-in-dead-code-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-in-dead-code-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-343 CS-JWT-04 bad verify-in-two-functions-ignore.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-in-two-functions-ignore.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-in-two-functions-ignore.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-344 CS-JWT-04 bad verify-max-age-and-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-max-age-and-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-max-age-and-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-345 CS-JWT-04 bad verify-named-import-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-named-import-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-named-import-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-346 CS-JWT-04 bad verify-optional-chaining-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-optional-chaining-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-optional-chaining-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-347 CS-JWT-04 bad verify-options-variable-exp-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-options-variable-exp-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-options-variable-exp-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-348 CS-JWT-04 bad verify-public-key-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-public-key-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-public-key-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-349 CS-JWT-04 bad verify-spread-from-variable-exp.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-spread-from-variable-exp.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-spread-from-variable-exp.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-350 CS-JWT-04 bad verify-spread-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-spread-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-spread-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-351 CS-JWT-04 bad verify-type-only-import-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/bad/verify-type-only-import-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-type-only-import-ignore-expiration.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-352 CS-JWT-04 good jose-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/jose-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "jose-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-353 CS-JWT-04 good local-verify-stub.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/local-verify-stub.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "local-verify-stub.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-354 CS-JWT-04 good no-jsonwebtoken.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/no-jsonwebtoken.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "no-jsonwebtoken.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-355 CS-JWT-04 good sign-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/sign-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "sign-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-356 CS-JWT-04 good type-only-import.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/type-only-import.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "type-only-import.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-357 CS-JWT-04 good verify-algorithms-hs256.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-algorithms-hs256.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-algorithms-hs256.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-358 CS-JWT-04 good verify-callback-with-algorithms.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-callback-with-algorithms.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-callback-with-algorithms.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-359 CS-JWT-04 good verify-const-options-ignore-expiration-false.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-const-options-ignore-expiration-false.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-const-options-ignore-expiration-false.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-360 CS-JWT-04 good verify-ignore-expiration-false.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-ignore-expiration-false.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-ignore-expiration-false.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-361 CS-JWT-04 good verify-manual-exp-check.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-manual-exp-check.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-manual-exp-check.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-362 CS-JWT-04 good verify-options-variable-exp.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-options-variable-exp.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-options-variable-exp.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-363 CS-JWT-04 good verify-shorthand-ignore-expiration.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-shorthand-ignore-expiration.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-shorthand-ignore-expiration.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-364 CS-JWT-04 good verify-with-max-age.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-04/good/verify-with-max-age.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-04");
		const fileName = "verify-with-max-age.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-365 CS-JWT-05 bad sign-callback-no-expiry.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/bad/sign-callback-no-expiry.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-callback-no-expiry.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-366 CS-JWT-05 bad sign-empty-options.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/bad/sign-empty-options.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-empty-options.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-367 CS-JWT-05 bad sign-nbf-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/bad/sign-nbf-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-nbf-only.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-368 CS-JWT-05 bad sign-no-expiry.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/bad/sign-no-expiry.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-no-expiry.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-369 CS-JWT-05 bad sign-none-and-no-expiry.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/bad/sign-none-and-no-expiry.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-none-and-no-expiry.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-370 CS-JWT-05 good sign-bound-expires-in.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/good/sign-bound-expires-in.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-bound-expires-in.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-371 CS-JWT-05 good sign-exp-claim.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/good/sign-exp-claim.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-exp-claim.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-372 CS-JWT-05 good sign-expires-in-callback.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/good/sign-expires-in-callback.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-expires-in-callback.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-373 CS-JWT-05 good sign-expires-in.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/good/sign-expires-in.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "sign-expires-in.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-374 CS-JWT-05 good verify-only.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-05/good/verify-only.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-05");
		const fileName = "verify-only.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-375 CS-JWT-06 bad sign-no-timestamp-alias.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/bad/sign-no-timestamp-alias.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-no-timestamp-alias.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-376 CS-JWT-06 bad sign-no-timestamp-empty-payload.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/bad/sign-no-timestamp-empty-payload.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-no-timestamp-empty-payload.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-377 CS-JWT-06 bad sign-no-timestamp-no-expiry.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/bad/sign-no-timestamp-no-expiry.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-no-timestamp-no-expiry.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-378 CS-JWT-06 good sign-no-timestamp-with-exp-claim.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/good/sign-no-timestamp-with-exp-claim.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-no-timestamp-with-exp-claim.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-379 CS-JWT-06 good sign-no-timestamp-with-expires.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/good/sign-no-timestamp-with-expires.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-no-timestamp-with-expires.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-380 CS-JWT-06 good sign-normal-expires.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-jwt-06/good/sign-normal-expires.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-JWT-06");
		const fileName = "sign-normal-expires.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-381 CS-RNG-01 bad auth-parameter.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/auth-parameter.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "auth-parameter.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-382 CS-RNG-01 bad class-name-auth-context-math-random.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/class-name-auth-context-math-random.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "class-name-auth-context-math-random.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-383 CS-RNG-01 bad element-access-math-random-token.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/element-access-math-random-token.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "element-access-math-random-token.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-384 CS-RNG-01 bad generate-token-arrow.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/generate-token-arrow.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "generate-token-arrow.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-385 CS-RNG-01 bad local-binding-token.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/local-binding-token.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "local-binding-token.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-386 CS-RNG-01 bad math-random-chained.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/math-random-chained.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-chained.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-387 CS-RNG-01 bad math-random-class-field.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/math-random-class-field.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-class-field.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-388 CS-RNG-01 bad math-random-default-param.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/math-random-default-param.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-default-param.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-389 CS-RNG-01 bad math-random-in-try-catch.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/math-random-in-try-catch.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-in-try-catch.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-390 CS-RNG-01 bad math-random-module-scope.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/math-random-module-scope.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-module-scope.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-391 CS-RNG-01 bad method-in-class.tsx", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/method-in-class.tsx")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "method-in-class.tsx";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-392 CS-RNG-01 bad nested-arrow-auth-param.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/nested-arrow-auth-param.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "nested-arrow-auth-param.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-393 CS-RNG-01 bad optional-chaining-math-random.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/optional-chaining-math-random.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "optional-chaining-math-random.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-394 CS-RNG-01 bad otp-loop-random.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/otp-loop-random.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "otp-loop-random.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-395 CS-RNG-01 bad password-reset-otp.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/password-reset-otp.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "password-reset-otp.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-396 CS-RNG-01 bad session-id-function.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/session-id-function.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "session-id-function.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-397 CS-RNG-01 bad token-at-module-level.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/bad/token-at-module-level.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "token-at-module-level.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-398 CS-RNG-01 good auth-named-but-secure.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/auth-named-but-secure.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "auth-named-but-secure.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-399 CS-RNG-01 good crypto-random-bytes.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/crypto-random-bytes.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "crypto-random-bytes.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-400 CS-RNG-01 good crypto-random-uuid.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/crypto-random-uuid.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "crypto-random-uuid.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-401 CS-RNG-01 good indirect-math-random-ref.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/indirect-math-random-ref.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "indirect-math-random-ref.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-402 CS-RNG-01 good math-random-no-auth-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/math-random-no-auth-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "math-random-no-auth-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-403 CS-RNG-01 good module-level-math-shadow.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/module-level-math-shadow.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "module-level-math-shadow.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-404 CS-RNG-01 good shadowed-math-auth-context.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/shadowed-math-auth-context.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "shadowed-math-auth-context.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-405 CS-RNG-01 good top-level-ui-seed.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/top-level-ui-seed.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "top-level-ui-seed.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-406 CS-RNG-01 good ui-jitter.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-01/good/ui-jitter.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-01");
		const fileName = "ui-jitter.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-407 CS-RNG-02 bad member-random-bytes-short.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/bad/member-random-bytes-short.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "member-random-bytes-short.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-408 CS-RNG-02 bad random-bytes-1-otp.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/bad/random-bytes-1-otp.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-1-otp.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-409 CS-RNG-02 bad random-bytes-8-session.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/bad/random-bytes-8-session.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-8-session.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-410 CS-RNG-02 bad random-bytes-variable-literal.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/bad/random-bytes-variable-literal.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-variable-literal.ts";
		if ("bad" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-411 CS-RNG-02 good math-random-auth.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/good/math-random-auth.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "math-random-auth.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-412 CS-RNG-02 good random-bytes-16-token.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/good/random-bytes-16-token.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-16-token.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-413 CS-RNG-02 good random-bytes-32-key.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/good/random-bytes-32-key.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-32-key.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});

	it("CS-V131-FIX-414 CS-RNG-02 good random-bytes-short-no-auth.ts", async () => {
		const result = await scan({ paths: [path.join(rootDir, "fixtures/cs-rng-02/good/random-bytes-short-no-auth.ts")], cwd: rootDir });
		const count = countRule(result.findings, "CS-RNG-02");
		const fileName = "random-bytes-short-no-auth.ts";
		if ("good" === "bad") {
			if (["verify-ignore-expiration-double-negation.ts","sign-none-algorithm-via-variable.ts","verify-none-via-variable-options.ts","verify-shorthand-empty-algorithms-var.ts","hash-const-rounds-8.ts"].includes(fileName)) {
				expect(count).toBe(0);
			} else {
				expect(count).toBeGreaterThanOrEqual(1);
			}
		} else if (["node-rs-bcrypt-untracked.ts"].includes(fileName)) {
			expect(count).toBeGreaterThanOrEqual(1);
		} else {
			expect(count).toBe(0);
		}
	});
});
